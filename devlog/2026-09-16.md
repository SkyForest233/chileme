# 2026-09-16 开发日志

> 本轮性质：**纯文档对账（doc reconciliation）**，不改任何运行时行为。
> 起因：用户要求「重新核查待办的真实性，有没有已经做了没标记的」。核查方式是把浅克隆 unshallow 到全部 154 个提交，
> 用 `git diff 41a728f..origin/master` 精确比对两份审查报告的基线与现状，再对每条待办回到源码 grep 取证。
> 结论：**09-15 之后的 8 个提交里，`app/src` 只删过一个空行**，所以 `chileme-review.md` 的代码级条目基本都还成立；
> 但**文档侧有大量「已做未标」「已过期」「自相矛盾」**，本轮集中修掉。
>
> 构建状态：⚠️ 沙箱无 JDK/Android SDK，且 ktlint 二进制下载被网络策略拦截（`release-assets.githubusercontent.com` 不可达），
> 本轮**未能本地跑门禁与构建**，以 CI 为准。唯一的代码文件改动是 `MiuixSettingsScreen.kt` 的 KDoc 注释（无编译影响）。

---

## ✅ 已完成

### 1. 先把工作区同步到最新 master（前置动作，也是最关键的纠错）

- 上一轮核查时本地 HEAD 停在 `fbab91a`，**落后 `origin/master` 6 个提交**，导致把 PR #7/#8 已修的东西全报成「未修」。
  根因是 `git fetch --unshallow` 的输出被 `tail -3` 截断，只看到 tag 行就误判远端没动。
- 本轮 `git fetch origin` + `git merge --ff-only origin/master`，当前分支 `arena/01a0a550-chileme` 已快进到 `fbeb5dc`（无本地提交、无冲突）。
- **教训写进流程**：任何「核查现状」类任务，第一步必须是 `git fetch` + `git rev-list --count HEAD..origin/master` 确认自己在最新提交上，再开始取证。

### 2. `docs/ARCHITECTURE.md`（8 处）

| 位置 | 原内容 | 改为 |
|---|---|---|
| §2 分层结构树 · `data/` | 只列 5 个文件 | 补 `BackupFile.kt`（备份读写 + `previewBackup` + `fileStamp`）、`CsvExport.kt`、`LocalSnapshotStore.kt`；`FoodRepository.kt` 补注 `Decoded` 三态 / 写守卫 / `DecodeCache` |
| §2 树 · `ui/theme/` | 缺 2 个文件 | 补 `Motion.kt`（MD3 缓动 token）、`TodayProvider.kt`（`LocalToday`） |
| §2 树 · `ui/screens/` | 「编辑页与**统计页**刻意保留 MD3+桥接」 | 改为两层结构说明：8 个 `*State.kt` 状态容器 + **8 对**渲染层（含 Stats / ConsumptionLog）；只有编辑页与 `CheckSwitch` 保留 MD3 |
| §4 路由表 | 只写 MD3 屏幕名 | 表后加注：除编辑页外每个路由都有成对 `Miuix*Screen.kt`，按 `LocalThemeStyle` 分流，必须共用状态容器 |
| §5 消耗记录压缩 | 「按『月×名称』聚合」 | 「按**年×月×名称×单位**聚合」+ `aggregated=true` + `isDeletable()` 双重拦截；函数名订正为 `compactConsumptionAt` |
| §5 备份排除规则 | 「两份规则排除 `datastore/`」（不完整） | 精确列出 `backup_rules.xml`（3 项）与 `data_extraction_rules.xml` 两条通道各自排除项，并登记**两点已知不一致**（见下「⚠️ 新发现」②③） |
| §5 主题渐变 | 「全部 **35** 个颜色角色」 | **37**（实测 `animatedColor(target.*)` 调用数） |
| §5 图片存储 | 「FoodItem 只存绝对路径」（读起来像约定） | 保留事实 + 标注为**已知待办**：绝对路径应改存文件名、`File.exists()` 应移出组合期（`Common.kt:258`、`EditFoodScreen.kt:284`） |
| §5 拍照 | 「FileProvider authority **固定 `com.agon.app.fileprovider`**」 | **这是错的且危险**：改为「一律用占位符 `${applicationId}.fileprovider`（实际值 `com.chileme.pantry.fileprovider`）/ 代码用 `${context.packageName}.fileprovider`，禁止硬编码」——namespace 与 applicationId 分离，硬编码会在运行时抛 `IllegalArgumentException`。与 `CLAUDE.md` §5 的既有约束对齐 |
| §5 应用图标 | 「legacy **兰容**图标在 `mipmap-*/ic_launcher.png`」 | 错别字 + 已过期：legacy PNG 于 2026-08-22（B14）删除，`@mipmap/ic_launcher` 现只解析到 `anydpi-v26`；补 `monochrome` 层说明 |
| §1 Release 构建 | 「release APK ≈ 2.6 MB」 | ≈ **2.4 MB**（2026-09-15 CI `release-r8` job 实测） |

### 3. `docs/DESIGN_SPEC.md`（2 处）

- §1 标题「**7** 套配色方案」→ **15** 套（`AppPalette` 枚举实测 15 项；正文表格与「v2.2 后共 15 套」的脚注本来就是对的，只有标题错）。
- §7 迁移进度：把**统计页**从「刻意保留 MD3+桥接」移到「已 Miuix 化」，并补上同样漏登记的**消耗记录页**；新增「双实现铁律」（必须调 `remember*UiState`，`MiuixParityTest` 拦截）与「已知缺口」（MIUIX 无配色入口 → KDoc 的「功能对等」不成立，用户指示暂缓）。

### 4. `CLAUDE.md`（3 处）

- §5 Miuix 迁移条目：删掉「统计页刻意保留 MD3」的错误约束（这条会直接误导后续 AI「不要动统计页」），改为「除编辑页外 8 对屏幕全部已 Miuix 化」+ 双实现铁律。
- §5 数据写入守卫：从「必须先 `isCorrupt(...)` 并在损坏时放弃写入」升级为 **09-15 的 key 粒度语义**（主数据先判、辅助数据按需判、禁止 `isCorrupt(a,b,c)` 一起判、`CorruptGuardTest` 拦截），并补「读流一律走 `rawFlow()`/`lightFlow()`（内含 `resilientRead()`）」。
- §5 新增「双主题铁律」条（`MiuixParityTest` / `ImeHandlingTest` 两个静态守卫）。
- §2 文档索引表：补 `docs/MIUIX_UPGRADE.md` 一行；`WORKFLOW.md` 行补「CI 静态门禁」与「提交前跑 `bash tools/ci-gates.sh`」。

### 5. `docs/REQUIREMENTS.md`（补登记 F22–F43 + 修正 §4 自相矛盾）

- **§4「明确不做」自相矛盾**：原文写「❌ 多人协作 / **云同步** / 任何后端」，而同文件 F20 就是坚果云 WebDAV 云同步。
  改为「❌ 多人协作 / **自建**云同步服务 / 任何自有后端（无账号体系、无服务端）」+ 显式登记 F20 为**用户已确认的例外**（用户自有网盘通道，凭据自填、加密存本机）。
  同时给「❌ 推送通知」补一句「临期提示只存在于应用内」，与 README 的措辞修正对齐。
- **补登记 22 项已上线但从未登记的功能**（违反本文档「新需求必须先登记再开发」的既定规则）：
  - §3.2 v2.5–v2.8：F22 到期日历、F23 消耗记录页、F24 归档搜索、F25 自定义矢量图标、F26 悬浮胶囊导航+开关、F27 双主题风格、F28 15 套配色、F29 预测性返回、F30 名称联想扩面
  - §3.3 2026-08-22 / 09-15：F31 批量改位置、F32 CSV 导出、F33 本地滚动快照、F34 卡片紧凑布局+`yyyy.MM.dd`、F35 清理撤销+触感振动、F36 MIUIX 独立 Window 弹窗、F37 数据损坏守卫与自救、F38 导入备份加固、F39 恢复前置快照、F40 跨零点刷新、F41 IME 键盘避让、F42 月度聚合保护、F43 统计口径按件数
  - 每条都回源码核过存在性（如 `updateLocationBatch`、`buildCsvExport`、`LocalSnapshotStore.MAX_SNAPSHOTS=3`、`DataCorruptBanner`/`discardCorrupt`、`previewBackup`、`snapshotBeforeRestore`、`LocalToday`、`imePadding`×23、`aggregated`/`isDeletable`、`calculateWastedTotal`）
- F13 统计页下补一行**已知口径待办**（「本周」实为近 7 天 / TOP5 未按单位 / 分类占比只算当前库存，用户指示暂缓），避免下一个人以为是 bug 重新报一遍。

### 6. `docs/MIUIX_UPGRADE.md`（5 处，这份是给 AI 执行升级用的手册，参数错会直接带偏）

- §0.2 工具链：Gradle **9.6.1 → 9.7.1**、minSdk **24 → 26**，并注明是 2026-09-16 校正。
- 第 2 步「改依赖版本」：原文教人在 `app/build.gradle.kts` 写死三行坐标 —— v2.8.1 起已走 Version Catalog，改为「只改 `libs.versions.toml` 的 `miuix = "..."` 一处，四个坐标共用 `version.ref`」，并贴出实际的四条 catalog 条目（含 `miuix-nav-android`，原文漏了它）。
- 第 3 步：插件版本也在 catalog（`[plugins]` 的 `kotlin-compose`/`kotlin-serialization` 都 `version.ref = "kotlin"`）；补「升 `composeBom` 与升 `miuix` 有连带关系，不要与功能改动混在同一 PR」。
- §2.7「minSdk 24 不变」→「minSdk 26 不变」并写明提升原因（28 处 `java.time` 未脱糖）。
- §5 文件索引表：`app/build.gradle.kts`「Miuix 依赖」→ 改为 `gradle/libs.versions.toml` 是**版本的唯一位点**；§4 交付清单补两条（跑 `tools/ci-gates.sh`、跑单测并特别关注 `MiuixParityTest` / `ImeHandlingTest`）。

### 7. `README.md`（对外门面，4 处）

- 首屏「**智能**临期提醒」→「应用内临期提示」，并加一句说明**没有系统推送**、这是明确的产品取舍（指向 `REQUIREMENTS.md` §4）。这条是 `chileme-review.md` P3-1 提的预期落差问题——它建议改做桌面小组件，但 Widget 在 §4「明确不做」里，所以只改措辞、不动范围。
- 核心特性补齐已上线能力：15 套配色、悬浮导航可切全宽、预测性返回、消耗记录页、归档搜索、批量归档/批量改位置、月度聚合保护、导入预览+二次确认+导入前快照、本地滚动快照、CSV 导出、数据健壮性（损坏守卫与自救入口）。
- 统计口径措辞与实际对齐：「本周/本月」→「近 7 天 / 本月」，并注明浪费按**件数**计。
- 隐私段落补一句 Android **系统备份**通道的准确描述（每日快照会进用户自己的 Google 账号；DataStore 与坚果云凭据已排除），修构建章节的 JDK 说明（`jvmToolchain(21)`）、补 targetSdk、补静态门禁跑法与贡献者文档入口。

### 8. 三份历史审查报告加「状态批注」（一律不改写原文，只在标题/表格加现状）

- `docs/audits/chileme-review.md`：文首加**复核批注表**，逐条标注 P0-1~P0-6 / P1 / P2 / P3 的 2026-09-16 状态；**明确写出 P0-6 主体论点不成立**（本文称 `device-transfer` 未排除 `datastore/`，实测该文件明确有这一条，与注释一致，凭据不会随换机直传外泄——第三方复核 §6 得出同一结论），只保留其成立的次要点；标注 **P3-1 的 Widget 建议撞 `REQUIREMENTS.md` §4 红线**，不得当普通待办推进；订正 4 处计数（37 个角色动画 / 34 个图标 / `isCorrupt` 现 18 次 / 双主题 16 文件基线 6,903 行、现 7,205 行）与单测数（73 → 116）。
- `docs/audits/2026-08-21-project-review.md`：§1.1–§1.4 四段散文当时没随 §2 表格更新，读起来仍像未修 —— 各标题加 ✅ 已修（B3/B10/B11/B5+B6）并注明落地证据（如 `restoreArchivedBatch` 现为单次 `dataStore.edit`、`deleteConsumption(record)` 已改传 record）；表格里 **B5 / B6 / B7 的「待做」订正为 ✅ 已完成**（2026-08-22 `41a728f`，README/LICENSE 已补但截图仍缺）；**B9 订正为「第一步已完成」**（状态层 8 个 `*State.kt` 已抽离 + `MiuixStatsScreen` 已接回，渲染层两套平行实现约 7,200 行仍在）。
- `docs/audits/2026-08-21-backlog.md`：文首加**校订对照表**（1.1 依赖已升 / catalog 已建 / 1.4 五个 PNG 已删 / P1 测试基线已完成且远超计划 / 1.3 与 1.5 仍未决策未清理）；§三「工程基建缺口」表加第四列逐行校订，并补一行「静态门禁（当时不存在）→ ✅ 2026-09-15 新增」。
- `docs/audits/2026-09-15-code-review.md` §5 路线图：第一批 10 项加状态列（**8 项已在 PR #7 落地**，第 5、6 项统计口径用户指示暂缓）；第二批 8 项逐条标注（2/3/4/6 ✅，1/5/8 ❌，7 🟡 部分）；第三批加「全部未动」批注。

### 9. `devlog/2026-09-15.md` 与 `devlog/INDEX.md`

- 09-15 日志「已知问题」里「静态门禁**两者都是 report 模式**」已过期：最后一个提交 `fbeb5dc` 把 workflow 里那行 `KTLINT_MODE=report DETEKT_MODE=report` 覆盖删掉了，现跑脚本默认值 = **ktlint block + detekt report**。改为记录完整演进过程（① 用户临时降 report → ② `795f02c` 修键名/开 `exceptions`、run 34983343813 验证双零基线 → ③ `fbeb5dc` 恢复拦截），并把「仍未做的一行改动」写明：`tools/ci-gates.sh:43` 的 `DETEKT_MODE:-report` → `block`。
- `INDEX.md`：B-07 行「仓库现有单测 **91** 例」→ **116** 例（实测 `grep -rh '@Test' app/src/test | wc -l`）；2026-08-20 行的「⚠️ 未验证（需本地 assembleDebug）」补注「此后已由 CI 多轮验证，Gradle 亦已升到 9.7.1」（该行状态与「当前待办总览」里同一事项早已标 ✅，两处此前互相矛盾）。

### 10. 唯一一处代码改动：`MiuixSettingsScreen.kt` 的 KDoc（注释，无编译影响）

原 KDoc 写「与 `SettingsScreen` 功能对等：外观（主题风格/深浅/动态取色/**配色**/悬浮导航）」，但本页没有配色入口。
`chileme-review.md` P3-6 的评价是「这类『声称对等但不对等』比缺功能本身更伤信任」，给的修法是「补上，或把 KDoc 改掉」二选一；
补入口已被用户暂缓，故本轮改 KDoc：措辞降为「**基本**对等」并从清单里去掉「配色」，另加一段「已知差异（勿再声明『完全对等』）」写明根因（`MiuixRootTheme` 只消费 `darkMode`+`dynamicColor`，Miuix 侧无种子色通道）与暂缓状态。

### 11. `Common.kt`（983 行）拆成 8 个按职责命名的文件（同包、纯搬运）

**为什么现在做**：`ui/components/Common.kt` 是仓库里唯一按「不知道放哪就先放这儿」组织的文件——状态语义层、小徽章、头像、步进器、218 行的 `FoodCard`、通用控件、损坏告警条、弹窗封装混在一起：18 个顶层声明、129 条 import。三个具体代价：① 改任何一个组件都要在 983 行里定位；② 它是 `detekt` 复杂度规则（`TooManyFunctions` / 文件长度）开不了的直接原因之一（`chileme-review.md` P2-1 把它当「有价值的拆分信号」点了名）；③ 第三批的「双主题去重 / App 级组件层」要在这些组件上动刀，先按职责分文件，那一轮的 diff 才读得下去。

**怎么拆（机械搬运，一行实现都没改）**：一次性脚本按「列 0 的顶层声明」切块，KDoc 与注解随块走，逐块写入目标文件；import 按「该文件代码里出现的符号」从原 129 条里挑（匹配前剥离注释与字符串，但保留 `${...}` 模板内容；别名 import 按别名匹配）。拆完用第二个脚本做**逐字节回归**：18 个声明块与原文比对全部相同，无重复、无遗漏、无丢失声明。

**目标文件（同包 `com.agon.app.ui.components`，因此 27 个调用方的 import 一行未改）**：

| 文件 | 行数 | 职责 | 声明 |
|---|---|---|---|
| `StatusUi.kt` | 127 | 状态语义层（唯一取色入口） | `StatusUi` / `rememberStatusUi` / `ExpiryUrgency` / `FoodItem.urgencyForAt` / `urgencyDotColor` |
| `Badges.kt` | 126 | 小胶囊标签 | `StatusBadge` / `LocationTag` |
| `FoodAvatar.kt` | 84 | 食品头像（照片 > coverText > emoji） | `FoodAvatar` / `EmojiAvatar` |
| `QuantityStepper.kt` | 184 | 数量步进器 + 它的私有转场 | `quantityChangeTransition`（private）/ `QuantityStepper` |
| `FoodCard.kt` | 284 | 列表主卡片（单个 composable 就 231 行） | `FoodCard` |
| `Controls.kt` | 227 | 通用交互控件 | `SelectIndicator` / `CheckSwitch` / `EmptyState` |
| `DataCorrupt.kt` | 102 | 数据损坏告警 | `corruptKeyNames` / `DataCorruptBanner` |
| `MiuixDialog.kt` | 44 | `WindowDialog` 封装 | `MiuixDialog` |
| 合计 | 1178 | 原 983 行 + 8 份文件头与各自 import 的开销 | 18 个声明 |

**import 账目**：原 129 条 → 8 个文件合计 245 条（同一 import 被多个文件各自持有，属正常）。逐条核对「原文有、8 个新文件全都没有」的只有 2 条：`androidx.compose.foundation.isSystemInDarkTheme`（只在注释里被提到，是死 import）与 **`androidx.compose.runtime.getValue`（必须补回，见下）**。

**本次最危险的一处**：`val progress by animateFloatAsState(...)` 这类**属性委托**依赖 `getValue` 这个操作符 import，而它在源码里**从不以名字出现**——任何「按符号名匹配」的机械拆分都会把它丢掉，结果是 `FoodCard.kt` / `Controls.kt` 编译失败（`Property delegate provider 'State<T>' does not have getValue`）。已按「文件内是否出现 `by` 委托」人工补回 2 处（`FoodCard.kt` 2 个委托、`Controls.kt` 5 个）。同一类陷阱（看着像成员访问、其实是需要 import 的扩展属性）：`FoodItem.productionDate` / `expiryDate`（`FoodModels.kt:183/:186` 是扩展属性）、`Int.dp` / `Double.sp`——这几处均已保留。

**顺手清掉的 5 处死 import**（拆分后按文件才暴露出来）：`FoodCard.kt` 的 `foundation.border`（只有 MiuixCard 的 `border =` 命名参数，`BorderStroke` 另有 import）、`foundation.background`（只有 `background = ui.container` 命名参数）；`Controls.kt` 的 `layout.width`（只有 `.size(width = ...)` 命名参数；`layout.height` 确实在用，保留）；`StatusUi.kt` 的 `data.dot`（`.dot()` 只在 `FoodCard.kt` 调）与 `foundation.background`（`colorScheme.background` 是 `ColorScheme` 的成员属性，不需要扩展 import）。**注**：detekt 的 `UnusedImports` 按标识符文本匹配，这 5 处它同样看不出来（命名参数 / 同名成员属性），所以门禁不会因它们变红——清掉是为了不让死 import 扩散进新文件。

**门禁自查**：`.editorconfig` 实际启用 6 条 ktlint 规则，与拆分相关的 4 条（`final-newline` / `no-trailing-spaces` / `no-consecutive-blank-lines` / `no-blank-line-before-rbrace`）已对 8 个新文件逐条扫描，零违规；`import-ordering` 未启用，故新文件保留原文件的 import 相对顺序（**刻意不做全量排序**，理由见决策记录）。沙箱无 JDK / Android SDK，**编译与 116 例单测以 CI 为准**。

**同步更新的引用（18 处）**：`CLAUDE.md`（`CheckSwitch` 路径 → `Controls.kt`）、`docs/ARCHITECTURE.md`（分层树 `components/` 一行改成 8 文件清单、`DataCorruptBanner` 路径、`Common.kt:258` → `FoodAvatar.kt:43`）、`docs/DESIGN_SPEC.md`（§4 标题；表格加「文件」列并补登此前漏掉的 `SelectIndicator` / `CheckSwitch` / `rememberStatusUi` / `DataCorruptBanner` / `MiuixDialog`；「新组件加入 Common.kt」改成按职责落文件）、`docs/MIUIX_UPGRADE.md`（升级位点表）、`docs/audits/chileme-review.md`（**只改文首的现状批注表**，正文与行号原样保留）、`devlog/INDEX.md`（3 处）、本日日志（2 处活待办行）、代码 KDoc 3 处（`MiuixHomeScreen.kt:82`、`MiuixRootTheme.kt:33/:90`）。**历史 devlog（07-31 / 08-20 / 08-21 / 08-22 / 09-15）与历史审查报告正文里的 `Common.kt` 一律不动**——那是当时的记录，改了反而无法追溯。本日 §2/§3 里引用的 `Common.kt:258` 亦保留原文，对应新位置是 `FoodAvatar.kt:43`。

**消费方零改动**：8 个新文件与原文件同包，27 个引用 `com.agon.app.ui.components` 的文件 import 一行未改；本轮源码改动除「删 `Common.kt` + 加 8 个文件」外，只有上面那 3 处 KDoc。

**CI 结果（run [35060113017](https://github.com/SkyForest233/chileme/actions/runs/35060113017)，含 §12 那个提交）：三个 job 全部 `success`** —— 静态门禁（ktlint block / detekt report）、debug APK + 单测 + lintDebug、release + R8 + lintRelease。即：拆分后的 8 个文件**编译通过**、`getValue` 那处补回是对的、ktlint 已启用的 6 条零违规、单测（含未被拆分触碰的 `MiuixParityTest` / `CorruptGuardTest` / `CompactConsumptionTest`）全绿。逐条日志没取到（沙箱访问 `results-receiver.actions.githubusercontent.com` 被拦），故单测总数未逐例复核，只以 job 结论为准。


### 12. 修掉 ⚠️⑧：批量「移动存放位置」弹窗的键盘避让（单独一个提交）

- **改动**：`MainActivity.kt` 的 MD3 分支 `AlertDialog`（`:650`）补 `properties = DialogProperties(decorFitsSystemWindows = false)` + `modifier = Modifier.imePadding()`（外加 1 条 `androidx.compose.ui.window.DialogProperties` import），写法与注释口径照抄既有的三处（`SettingsScreen.kt:769`、`ManageScreens.kt:311`/`:431`）；Miuix 分支不动（`WindowDialog` 的 `DialogContent` 由库自理 IME）。
- **守卫**：`ImeHandlingTest` 第 3 条的清单加入 `com/agon/app/MainActivity.kt`（原清单只有 `SettingsScreen.kt` 与 `ManageScreens.kt`，**这正是漏网原因**），KDoc 里写明「本清单按文件点名，带输入框的 MD3 弹窗出现在哪个文件就得把那个文件加进来；若按路线图搬去 `AppDialogs.kt`，清单同步改」。
- **连带发现（值得单记一笔）**：第 2 条守卫原来用「`.imePadding()` 的数量**严格少于** `.navigationBarsPadding()` 的数量」来间接表达 A 方案（底栏不随键盘抬升）。补上弹窗这一处后两边都是 4、数量差归零 → **这个代理指标当场失效，会把一次正确的修复判成违规**。已改成直接断言「`private fun MiuixBottomNav` 起到文件尾（4 套底栏实现所在段）不含 `.imePadding()`」，两条计数阈值同步 3→4，并把 4 处 imePadding / 4 处 navigationBarsPadding 各自对应哪个浮层写进注释。教训：**用「两个计数的大小关系」表达约束，等于把所有无关改动都变成破坏性改动**；守卫应当指着被约束的那段代码说话。
- **验证到哪一步**：CI run 35060113017 三个 job 全绿 —— 编译通过、改写后的 `ImeHandlingTest` 两条断言（阈值 ≥4 + 导航栏段不含 imePadding）与第 3 条新清单都跑过了。**真机复测已由用户完成（2026-09-16）：弹窗键盘避让实测无问题** —— 本项待办关闭。


### 13. 第二批执行完毕：`MainActivity.kt`（1,123 行）→ 6 个文件（4 个搬运提交 + 1 个修 CI 的提交 + 1 个文档提交）

**最终结构（实测 `wc -l`，合计 1,371 行；差额 248 行 = 6 份 `package`/import/文件头 + 拆分说明注释）**

| 文件 | 行数 | 内容 | 提交 |
|---|---|---|---|
| `MainActivity.kt` | 118 | `READY_TIMEOUT_MS` + `class MainActivity`（深浅色/风格分流、启动放行超时、splash、`CompositionLocalProvider`） | `e3c0081` ② |
| `MainApp.kt` | 335 | 状态与副作用（`backStack`/`pagerState`/多选/3×Snackbar 收集/`nestedScroll`）+ `Scaffold`（底栏槽位、FAB、内容槽位一次 `AppNavHost` 调用）+ Snackbar 覆盖层 | `e3c0081` ② → `c67a73a` ③ → `8a6a382` ④ |
| `AppNavGraph.kt` | 170 | 全 App 唯一的 `NavDisplay` + 8 个 `entry<AppRoute.*>` + 外层 840dp 限宽 Box | `8a6a382` ④ |
| `AppDialogs.kt` | 211 | 批量「移动存放位置」弹窗（MD3 `AlertDialog` / Miuix `WindowDialog` 双实现） | `c67a73a` ③ |
| `BatchBars.kt` | 213 | `BatchActionBar`（internal）+ 3 个按钮（仍 private，只有它自己用） | `4aabb72` ① |
| `NavChrome.kt` | 324 | `TabSpec`/`MainTabs`/`MiuixMainTabs`/`MainTabsPager` + 4 套底栏 | `4aabb72` ① + `2a71bc6` 修 |

**可见性放宽的最终账**：计划估「11 个」，实测只需 **8 个** —— `BatchActionBar`、`MainTabs`、`MainTabsPager`、4 套底栏（步骤①，7 个）+ `TabSpec`（修 CI 那个提交，见下）。计划里列的 3 个批量按钮与 `MiuixMainTabs` **没有**放宽：它们只被同文件的声明使用，顶层 `private` 照旧，`detekt` 的 `UnusedPrivateMember` 对它们的覆盖也保住了。`EmphasizedDecelerate`/`Accelerate` 同理留在 `MainApp.kt` 里 private。

**执行顺序与计划的差别**：计划是 ①`BatchBars`+`NavChrome` → ②`AppDialogs` → ③`AppNavGraph` → ④`MainApp`；实际把 `MainApp` 提到了第二步。理由：`MainApp` 是**声明级**整块搬（函数整体挪走），弹窗与路由表是 `MainApp` **内部**的块 —— 先把外层落定，后两步的比对基准就永远只有一个文件，不必在「原始 1,123 行」与「中间态」之间来回换算。提交标题里的 ①②③④ 是执行序。

**「纯搬运」的证据（每步都在抽取脚本里做断言，不是事后目测）**
- ①：7 处 `private→internal` 之外，声明块与原文**逐字节相同**。
- ③：与**原始** `MainActivity.kt`（`36c33ca`）的对应块比对，diff 恰好 **19 行** = 8 处「被捕获的 `var showMoveLocationDialog` → `show` + `onDismiss()` 形参」改写（两侧各 8 行）+ 1 行段落注释 + 1 个新函数的收尾 `}`；其余 100 余行逐字节相同。
- ④：**形参名照抄被捕获的局部名**（`navigate`/`popRoute`/`openList`/`selectTab`/`chromeScrollConnection`/`backStack`/`viewModel`/`pagerState`/`listFilter`），于是搬过去的 101 行**一个字都没改**，只整体反缩进 4 空格；脚本断言「去空白后逐行相同」，`git diff -w` 里这一段是空的。调用点用 `::局部函数` 传引用 —— 同一手法 `MainApp` 里本来就有（`MiuixFloatingNav(selectedTabIndex, ::selectTab)`），不是新赌注。
- 新增 import 只有 3 条，全部是「原来靠推断、成了形参类型就必须显式写」：`CoroutineScope`（③）、`NavBackStack` + `PagerState`（④）。`NavBackStack` 的确切定义查了上游 miuix `v0.9.4-rc01` 源码确认：`public typealias NavBackStack = SnapshotStateList<NavKey>`，`NavDisplay` 的形参正是它 —— 本地无 JDK，类型名只能靠上游源码定，不能靠编译器兜。

**⚠️ CI 红了一次（run [35062328443](https://github.com/SkyForest233/chileme/actions/runs/35062328443)，①② 推上去之后）。两处根因都属「本地不能编译才可能漏」，是本轮最值钱的两条教训：**

1. **`NavChrome.kt`：`internal val MainTabs = listOf(TabSpec(...))` 的推断类型是 `List<TabSpec>`，而 `TabSpec` 当时还是 `private data class`** → Kotlin 拒绝「被暴露类型的可见性低于声明本身」（与 *public function exposes its private return type* 同一条规则）。**只有 release job 会撞到**（release 只编 main 源集），debug job 的失败被第 2 条盖住了，所以两个 job 的报错位置看起来还不一样。修：`TabSpec` 一并放宽为 `internal`（`2a71bc6`）。
   - **教训**：判断「哪些 `private` 要放宽」不能只 grep **谁被别的文件引用**，还要看**谁的类型被放宽后的声明暴露出去**。前者机械脚本能做，后者要顺着推断类型链走，脚本必然漏。已补一条全量审计（扫每个 `private` 顶层类型是否出现在 `internal`/`public` 声明的签名或初始化式里），本轮跑完结果是「private 顶层类型 0 个、暴露问题 0 处」。
2. **`ImeHandlingTest.kt`：`Regex("/\*.*?\*/")` 里的 `\*` 在 Kotlin 字符串中是非法转义** —— 拆分脚本用 Python 写文件时反斜杠没加倍（quoted heredoc 只保证 Python 侧不解释，不保证写出来的 Kotlin 合法）。修：改 raw string `"""/\*.*?\*/"""`，语义不变。
   - **教训**：**脚本生成的代码里，凡含反斜杠的字面量都要 grep 复核一遍**。ktlint/detekt 这类静态门**不编译**，拦不住非法转义与可见性错误 —— CI 的 build job 是唯一防线，所以「拆分步骤要小、每步一个提交」不是为了好看，是为了红了以后能二分。

**顺带清掉的死 import**：`androidx.lifecycle.viewmodel.compose.viewModel` 在 `MainApp`/`NavChrome`/`AppDialogs` 三处都只有**同名形参**、从未调用工厂（真调用点只有 `MainActivity.kt:61` 一处）。根因与 §11 那条「`getValue`/`setValue` 漏配」是同一个问题的两面：按符号名匹配 import，既会**漏**（委托操作符不以名字出现在代码里），也会**误配**（形参名与导入名相同）。两类都得人工过一遍 —— 已在抽取脚本里对 `viewModel` 特判为「必须出现 `viewModel<` 或 `viewModel()` 才算用到」。

**守卫测试**：`ImeHandlingTest` 三条断言在每一步的**同一个提交**里跟着搬（文件清单 + 期望计数），中间态不留红。终态：chrome 清单 5 个文件求和 ime=4 / nav=4、`NavChrome.kt` 不含 `.imePadding()`（A 方案：底栏不跟键盘抬升）、弹窗宿主清单 `SettingsScreen`+`ManageScreens`+`AppDialogs`。`AppNavGraph.kt` 不进清单（路由表里没有任何 inset 调用，实测 0 处）。

**CI 结果**：run [35063573632](https://github.com/SkyForest233/chileme/actions/runs/35063573632)（①②③ + 修复）三 job 全绿；run [35064714882](https://github.com/SkyForest233/chileme/actions/runs/35064714882)（④）三 job 全绿（含 release + R8 + lintRelease）。日志正文仍取不到（`results-receiver` / `productionresultssa19.blob.core.windows.net` 均被网络策略拦），以 job 结论 + step 级失败位置为准。

**刻意没做的两件事**：① `CHROME_HIDE_THRESHOLD_PX` 具名常量（路线图点名的唯一「顺手改善」）—— 它会让「逐字节相同」这条最强证据失效，留给第三批单独提交；② `AppNavHost`（9 形参）与 `BatchMoveLocationDialog`（8 形参）的收窄 —— 收窄要动 `MainApp` 里 FAB/底栏共用的局部函数、或把 VM+Snackbar 逻辑挪回调用方，属**行为/接口改动**，与「可证明的纯搬运」不能混在一个提交里。两处的形参由来都写进了各自文件头。

### 14. detekt 一直是**空转**的：`--config` 不带 `--build-upon-default-config`，一条规则都不激活（第三批 #1 + #2 一并做完）

**现象**：给 `detekt.yml` 开了 `complexity: active: true` 之后，CI 仍报「0 number of total code smells」—— 而同一份输出里 detekt 自己统计出 76 files / 559 functions / 17,701 loc。**解析正常、规则没跑**。工程里 `MainTabsPager` 实打实有 11 个形参，`LongParameterList` 阈值 6，0 条发现不成立。

**取证**（沙箱下载不到 CI 日志与 artifact，全程靠 annotation 通道；四步各一个提交）：
1. `0a09043`：给 `tools/ci-gates.sh` 加 `emit_annotations`（发现按规则聚合成 check-run annotation）+ 三条健康度探针（自测结果 / 控制台尾部 / 报告文件行数字节数）—— **这是能在受限网络里看见 CI 内部状态的唯一办法**；
2. `e6455ee`：放一个 canary 文件（`UnusedPrivateMember` / `LongParameterList` 8 形参 / `EmptyCatchBlock` + `SwallowedException` / `LongMethod` 72 行，4 处必然命中）→ 仍 **0 条**；
3. `7dd9735`：二分 —— 同一个 canary 用 detekt **内置默认配置**（不带 `--config`）跑 → **4 条全中、exit=2**。至此确定问题不在 detekt 本身，而在「只给 `--config`」。

**根因**：detekt CLI 只用 `--config detekt.yml` 时**不会**把默认配置当基线，于是「本仓库配置里没逐条列出的规则」一律不激活 —— `style: active: true` / `complexity: active: true` 只开了**规则集**，集内规则一条都没跑。官方参数是 `--build-upon-default-config`（v1.23.8 `CliArgs.kt:104`：*Preconfigures detekt with a bunch of rules and some opinionated defaults… Allows additional provided configurations to override the defaults*）。

- **推论（已写进 `detekt.yml` 头部）**：**这里没写 ≠ 关闭**，没写 = 用 detekt 默认值；要关必须显式 `active: false`。
- **连带更正一处口径**：2026-09-15 引入门禁以来 detekt 从未产生过任何发现，所以 `ImageStore.kt:114`/`:133` 那两处 catch 的注释写「detekt `SwallowedException` 指出」是不成立的 —— 那是**人工审查**的功劳。detekt 当时只是「没有反对」。

**防止再犯**：`detekt_selftest`（`54f4317`）—— 每次门禁先造一个含必然命中违规的临时 Kotlin 文件（生成在 `build/reports/gates/selftest/`，不在 `app/src` 下，故不会被 ktlint 主扫描收到），用与主运行完全相同的配置跑一遍；**命中 0 条就 `::error::` 并判红，与 `DETEKT_MODE` 无关**（哨兵码 91，与 90「缺配置文件」、3「配置无效」并列）。canary 文件随即删除（已内化为脚本自测）。

**第一次拿到的真实清单：73 条发现 / 6 个规则**

| 规则 | 条数 | 处理 |
|---|---|---|
| `LongMethod`（阈值 60） | 33 | ❌ 显式关闭 —— 命中几乎全是 Compose 屏幕（`MiuixSettingsScreen` 863 行、`EditFoodScreen` 557 行…）。归口是路线图 **#3**（双主题去重 + App 级组件层），目标是可数的「16 个渲染层文件 → 8 个、7,205 行 → 4,500 量级」，不是让 detekt 天天报 33 条永远不拦的噪声 |
| `LongParameterList`（阈值 6） | 24 | ❌ 显式关闭 —— 命中的多是 `*State.kt` 状态容器（`FoodDetailUiState` 13 形参，**刻意设计**：精确订阅要求字段摊平）与 App 级组件（`AppNavHost` 9 / `MainTabsPager` 11 / `BatchMoveLocationDialog` 8，由 **#3** 收窄成回调后再评估阈值） |
| `TooManyFunctions`（阈值 11） | 7 | ❌ 显式关闭 —— `AppViewModel` 53 个、`FoodRepository` 47 个，正是 **#5**（Repository 拆分 + 轻量 DI）要拆的对象 |
| `CyclomaticComplexMethod`（阈值 15） | 7 | ❌ 显式关闭 —— 与 `LongMethod` 同一批屏幕文件，同一归口 |
| `ComplexCondition`（阈值 4） | 1 | ✅ **改代码**（`c0190a0`）：`CsvExport.escapeCsvField` 的 4 个 `contains` 串联 → `any { it in ",\"\n\r" }`，条件数 4→1；`CsvExportTest` 6 条断言覆盖 |
| `LoopWithTooManyJumpStatements`（阈值 1） | 1 | ✅ **改代码**（`c0190a0`）：`NutstoreSync.parsePropfind` 的 `for` + 2 个 `continue` → `mapNotNull { … return@mapNotNull null }`，跳转 0 条、可变累加器消失；`CloudBackupTest` 3 条断言覆盖 |

**关键结论**：`potential-bugs` / `coroutines` / `empty-blocks` / `performance` / `exceptions` / `style`（含 `UnusedImports`、`UnusedPrivateMember`、`SwallowedException`）**六类全为 0 条** —— 「疑似缺陷」那部分本来就是干净的，73 条全部是体量与复杂度指标。所以 2 条修掉 + 71 条按实测数字显式关闭（理由与归口写进 `detekt.yml`）之后清单归零，`DETEKT_MODE` 才能从 report 切成 **block**（第三批 **#1 完成**、**#2 完成**）。噪声的代价是具体的：71 条永远红的发现会让人不再读这份报告，剩下那 2 条真问题反而被埋掉。

**顺带踩到的第二个坑（`fdd00c0`）**：annotation 解析先按 detekt 的 **txt 报告**格式写（`path:RuleId - 实测/阈值 - [实体] at path:行:列 - Signature=…`），本地 gawk 跑得通、CI 的 **mawk** 却把 `Signature=` 里的 `(String)`/`(Int)`/`(AppRoute)` 当成规则名，清单变成「String=14, unknown=49, Int=4」这种没法用的东西。改成解析**控制台**输出、以行尾 `[RuleId]` 为唯一锚点后两种 awk 一致。教训：跨 awk 实现别依赖贪婪/回溯细节，选一个唯一且稳定的锚点；**能在本地跑通不等于在 CI 跑通**（这条与「本地无 JDK」是同一类问题的两个面）。

---

### 15. 第三批 #3 开工：App 级组件层落地 + 第 1 对（消耗记录页）合并

**做了什么**：新建 `ui/components/app/`（App 级组件层，3 个文件 389 行），把 `ConsumptionLogScreen.kt`（MD3，206 行）与 `MiuixConsumptionLogScreen.kt`（190 行）合并成一份 113 行的双主题屏幕，删掉 Miuix 双胞胎；`AppNavGraph.kt` 的 `entry<AppRoute.Consumption>` 从 if/else 两分支变成一行调用。

**组件层首发的三个文件**（照抄 `Badges.kt` 里 `StatusBadge` 的分流样板 —— 上一轮把它单独成文件，就是给这一步当模板）：

| 文件 | 行数 | 内容 | 吸收掉的两版差异 |
|---|---|---|---|
| `AppChrome.kt` | 173 | `AppScaffold` / `AppTopBar` / `AppSnackbarHost`(internal) / `AppSnackbarHostState` + `rememberAppSnackbarHostState()` | Scaffold 容器色（MD3 `background` / Miuix 默认 `surface`）、顶栏标题形态（MD3 传 composable 并加粗 / Miuix 吃 String）、返回图标字形（`ArrowBack` / `Back`）、撤销条宿主（MD3 自绘 `SwipeDismissSnackbarHost` + `navigationBarsPadding` + 24dp / Miuix 库默认 `SnackbarHost`） |
| `AppSurface.kt` | 65 | `AppCard` / `AppHintText` | 卡片圆角与色板（`shapes.large` + `surfaceContainer` / `RoundedCornerShape(16.dp)` + 同名色）、弱化文字档位（`bodySmall` + `onSurfaceVariant` / `footnote2` + `onSurfaceVariantSummary`） |
| `AppListRow.kt` | 151 | `AppListRow` + 5 个私有叶子（emoji / 标题 / 强调文本 / 标注 / 删除按钮） | emoji 字号（`20.sp` / `title3`）、标题档位（`bodyLarge` / `body1`）、强调文本（`bodyMedium` / `body2`）、标注（`labelSmall` / `footnote2`）、删除图标（`DeleteForever` / `Delete`） |

九处差异逐项有归属，**没有一处是「顺手统一掉」**：每一项都保留了原来两版各自的取值。`AppListRow` 刻意不认识领域类型 ——「这一行可不可删」由屏幕按 `ConsumptionRecord.isDeletable()` 判断后，传 `onDelete` 或 `trailingTag` 二选一。

**三个设计上躲开的坑（都是"合并"这个动作才会暴露的）**：

1. **`LaunchedEffect(Unit)` + 切主题 = 撤销条弹到已经卸载的宿主上。** 合并前两份文件是两个不同的 composable，切主题会换掉整棵子树，`LaunchedEffect(Unit)` 天然重启；合并后同一个 composable 留在原地，而 MD3 与 Miuix 的 `SnackbarHostState` 是**两个互不相干的类型**，协程里那个引用就僵住了。做法：`rememberAppSnackbarHostState()` 用 `remember(isMiuix)` 换容器，屏幕侧 `LaunchedEffect(snackbar)` 跟着重启。
2. **两个主题的 `SnackbarResult` 会顺着签名漏回屏幕层。** 容器对外只给 `showUndoSnackbar(): Boolean`（true = 用户点了撤销）。否则屏幕里又要出现 `if (isMiuix)` —— 双实现只是换个地方长回来。（同一类错误的既有先例：第二批 CI 红过一次的「internal val 暴露 private 类型」。）
3. **`AppHintText` 差点吃掉一处真实的视觉差异。** 行内副标题是 MD3 `bodySmall` / Miuix `footnote2`，而「月度合计」标注是 MD3 **`labelSmall`**（比副标题再小一档）/ Miuix `footnote2`。两者在 Miuix 侧同档、MD3 侧不同档 —— 若图省事都走 `AppHintText`，MD3 的标注会悄悄变大一号。故标注另留私有 `RowTag`。

**行数如实记账（这一对是净增）**：

| | 合并前 | 合并后 |
|---|---|---|
| 屏幕本体 | 396（206 + 190） | **113**（-71%） |
| 共享骨架 | 0 | **389**（新目录） |
| 合计 | 396 | **502（+106）** |

第一对必然净增：骨架是一次性投入。回报在后面 7 对 —— 每合并一对只增屏幕本体、不再增等量的外壳代码。按 396→113 的比例外推，8 对全合完时渲染层约 4,400–4,600 行（含骨架），落在路线图 #3「7,205 → 4,500 量级」的验收区间内。**这个外推是估计、不是承诺**：后 7 对里有图表（统计页 Canvas 自绘）、表单（设置页 1,142 行）这类骨架复用率低的页面，实际落点靠每对实测说话（每合并一对，往本节这张表追加一行）。

**两个静态守卫的迁移（与代码同一个提交）**：

- `MiuixParityTest` → **`ScreenParityTest`**（`git mv`，保留历史）。改名不是洁癖：它按 `name.startsWith("Miuix")` 枚举，而合并后的文件名不再带这个前缀 —— **刚合并完的屏幕会当场逃出守卫**（Miuix 分支还在文件里，却没人查它）。范围改为 screens 目录下所有 `*Screen.kt` / `*Screens.kt`（`*State.kt` 是状态层，不在此列），并补两条：
  - 规则 1 的豁免位 `NoSharedStateScreens = setOf("EditFoodScreen.kt")`：编辑页是表单，字段本身就是 `rememberSaveable` 的本地编辑态，没有也不需要共享状态容器（它也没有 Miuix 双胞胎，见 `DESIGN_SPEC.md` §7「刻意保留 MD3+桥接」）。规则 2（禁止内联聚合）对它照样生效。**扩围前先实测过全部 17 个屏幕文件**：内联聚合 0 处、`remember*UiState` 只有编辑页 1 处缺失 —— 所以扩围当场就是绿的，不是「改完再看谁红」。
  - 新增 `MergedScreens` 反向守卫：已合并的屏幕不许把 `Miuix<同名>.kt` 加回来，也不许把屏幕本体删掉（合并 ≠ 删屏）。逐对合并时往这个集合加一项。
  - 统计页那条专项断言从「只查 `MiuixStatsScreen.kt`」推广成「查所有 `*StatsScreen.kt`」（实测 MD3 版同样满足 `rememberStatsUiState(viewModel)` 且无内联统计），这样后面合并统计页时断言自动跟着走，不必再改测试。
- `CompactConsumptionTest`：原来同时读两个文件、`listOf(...)` 分别断言；文件一删 `read()` 返回 null，`md3!!` 直接 NPE。改成只读合并后的一份 —— 断言语义也从「两套实现都得拦住聚合记录删除」变成「一套就够」，这正是合并要的效果（漏改一个主题从此不可能）。
- `ImeHandlingTest` **本对无需改动**：它点名的 5 个屏幕文件（编辑页 / 列表页 / 归档页 ×2 主题）里没有消耗记录页，两版旧文件也确实一处 `imePadding()` 都没有（这一页没有输入框）。但**后面几对会撞**：列表页与归档页都在名单里，等 `AppScaffold` 统一吃 inset 时，按路线图第 3 条改成「查 `AppScaffold` 一处 + 屏幕不得自己再加」。

**刻意没做的四件事**（理由都写进了代码注释）：① `AppTopBar` 不开 `subtitle` 参数 —— Miuix 有、MD3 `TopAppBar` 没有对应参数，开了就是「一个主题静默忽略参数」；等首页那一对合并时连 MD3 的第二行一起补。② 不开 `actions` 槽位 —— 只有食品列表页需要（归档 / 统计入口），到那一对再加，避免现在凭空设计。③ 不删 `ManageScreens.kt` / `MiuixManageScreens.kt` 里那两份私有 `*ManageScaffold` —— `AppScaffold` 已能取代它们，但那是管理页那一对的事，跨提交删会让本轮 diff 混进无关页面。④ 不动 `AppNavHost` 的 9 形参与 `BatchMoveLocationDialog` 的 8 形参 —— 参数收窄是接口改动，与本轮「可证明的等价搬运」不能混在一个提交里（与第二批 §13 同一条原则）。

**验证方式**：沙箱无 JDK，CI 是唯一编译判据。落笔前把 Miuix 的 API 面**从上游 pinned source 核对过**（`CLAUDE.md` 明令禁止凭记忆臆造签名）：`gh api` 取 `compose-miuix-ui/miuix` 的 `v0.9.4-rc01`（= `libs.versions.toml` 的 `miuix = "0.9.4-rc01"`），确认 `Scaffold(modifier, topBar, snackbarHost, containerColor, content: @Composable (PaddingValues) -> Unit)` 与 `Text(text, modifier, color, fontWeight, style)` 的参数名与类型；`content = content` 可以直传（仓库里 `MiuixManageScaffold` 已经这么写，属既有证据）。另外核对了 `detekt.yml`：`MaxLineLength` / `MagicNumber` 都是显式 `active: false`（仓库现存最长行 242 字符），所以长中文注释与 `.dp` 字面量不会撞上刚转成 block 的门禁。

**CI 验证（run 35072498458，一次通过，三 job 全绿）**：静态门禁 22s（detekt **block** 模式下 0 发现 —— 新目录 3 个文件过 `UnusedImports`/`UnusedPrivateMember`；annotations 里探针输出 `0 行 / 0 字节` + `（无输出，exit=0）—— 零发现，正常`，自检 `命中 2 条已知违规（exit=2）—— 规则确实在跑`）；debug + 单测 + lintDebug 2m14s（**117 例**，比合并前 +1：`ScreenParityTest` 4 条 vs 原 `MiuixParityTest` 3 条，多的是「已合并的屏幕不许再出现 Miuix 第二实现」）；release + R8 + lintRelease 4m13s（新的公开 API 没有暴露 internal/private 类型 —— `AppSnackbarHostState` 是 public 类 + internal 构造 + internal 成员，第二批 CI 红过的那个坑这次没踩）。落笔前做的三项本地预演（括号平衡、逐文件 import 使用率、把两个守卫的断言照测试逻辑跑一遍）**当场抓到 1 个真问题**：`AppChrome.kt` 导入了 `MiuixText` 却没用到 —— Miuix 顶栏吃 String、MD3 标题用 material3 `Text`，那个别名 import 是照着 `Badges.kt` 模板抄来的残留。detekt 的 `UnusedImports` 是激活的、门禁又刚转 block，这一条会让 CI 直接红。

~~需真机复测~~ ✅ **用户已真机复测通过（2026-09-16，两套主题）**：消耗记录页 —— ① 顶栏标题与返回键（MD3 加粗标题 + `ArrowBack`；Miuix 字符串标题 + `Back`）；② 列表行：emoji、名称、中文日期、右侧「×N 单位」主题色、卡片圆角；③ **删除一条逐笔记录 → 撤销条出现（MD3 可滑掉、位于手势区上方 24dp；Miuix 库默认样式）→ 点撤销能插回原位置**；④ 6 秒后自动消失；⑤ **月度合计行没有删除按钮、只显示「月度合计」标注**（MD3 标注字号应比副标题小一档）；⑥ 空态；⑦ **在设置页切换主题风格后回到本页再删一条，撤销条仍要弹得出、撤销得回来**（对应上面第 1 个坑）。

### 16. 第三批 #3 第 2 对：归档页合并（566 → 186 行），组件层长到 5 个文件

**做了什么**：`ArchiveScreen.kt`(284) + `MiuixArchiveScreen.kt`(282) → 一份 `ArchiveScreen.kt`(186)，Miuix 文件删除，`AppNavGraph` 的 `entry<AppRoute.Archive>` 从 if/else 变成一行。这一对是第一对的 1.4 倍体量，但屏幕本体降了 **67%**（比第一对的 71% 略低，因为归档页的业务分支更多：搜索/筛选/空态三态/两种弹窗）。

**组件层新增 4 个组件 + 1 个槽位**（389 → 760 行）：

| 落点 | 新增 | 吸收掉的差异 |
|---|---|---|
| `AppControls.kt`（新文件，102） | `AppSearchField` | MD3 = 圆角 50 的 `OutlinedTextField` + 左侧放大镜 + placeholder；Miuix = 库的 `InputField`，用 `label`、无前导图标 —— 两边**不是换个皮，是两套 API**，一个 `label` 参数同时喂 |
| 同上 | `AppFilterChip` | 两版原来用的都是 **material3 `FilterChip`**（Miuix 侧靠 `MiuixRootTheme` 的 MaterialTheme 桥接取色），故只分流色板与标签字号（Miuix 加 `body2`），控件本身不换 |
| `AppConfirmDialog.kt`（新文件，113） | `AppConfirmDialog` | 结构差异最大的一处：MD3 是 `AlertDialog` 的 `confirmButton`/`dismissButton` 槽位；Miuix 是 `WindowDialog` + 自己排一行两个等宽 `TextButton`。**原来两版各写 2 个弹窗 = 4 份文案与回调**，现在各写 1 次 |
| `AppListRow.kt`（151 → 260） | `AppActionRow` + 私有 `RowHeading` / `RowRestoreButton`（复用第 1 对的 `RowDeleteButton`） | 头像槽位行：标题档位（MD3 `titleSmall` / Miuix `subtitle`，都 SemiBold + 单行省略）、恢复图标（`RestartAlt` / `Refresh`，primary 色）、删除图标（`DeleteForever` / `Delete`，error 色 20dp） |
| `AppChrome.kt`（173 → 220） | `AppTopBar`/`AppScaffold` 的 `actions` 槽位 + `AppDestructiveAction` | 顶栏右侧动作区（第 1 对明确推迟的那一项，按需补上）；清空归档入口两主题各自字形 + error 色 |

**行数记账（第 2 对开始回本）**：

| | 合并前 | 合并后 | 差 |
|---|---|---|---|
| 屏幕本体 | 566（284 + 282） | **186** | **-380（-67%）** |
| 共享骨架 | 389 | **760** | +371 |
| 合计 | 955 | 946 | **-9** |

第 1 对净增 106，第 2 对净减 9 —— 骨架的边际成本在快速下降（这一对新增的 371 行里，`AppConfirmDialog` 与 `AppSearchField` 后面设置页/管理页/列表页还要复用）。累计：两对 962 行屏幕 → 299 行，渲染层文件 16 → **14**（+ 组件层 5 个文件）。

**从上游源码核出来的两条硬约束**（都写进了代码注释，不是靠印象）：

1. **Miuix 的 `Card` 与 `Surface` 不等价。** 归档行的 Miuix 外壳用的是 `Card`，而第 1 对抽的 `AppCard` 在 Miuix 侧是 `Surface(shape = RoundedCornerShape(16.dp), color = surfaceContainer)`。取 pinned source（`v0.9.4-rc01`）看 `Card.kt:50`：圆角默认值 `CardDefaults.CornerRadius = 16.dp`（**一样**），但它还会把内容套进 `Column(Modifier.padding(CardDefaults.InsideMargin))`，且 content 的接收者是 `ColumnScope` 而不是普通 lambda。**所以直接把归档行塞进 `AppCard` 会让 Miuix 归档行少掉一层内衬** —— 这是那种"编译通过、CI 全绿、只有真机看得出来"的改动。处置：外壳在 `AppActionRow` 内部各自分流（MD3 `Surface` / Miuix `Card`），不并进 `AppCard`，理由与行号写在 KDoc 里。
2. **弹窗必须在 Scaffold 的 content lambda 内。** `MiuixDialog.kt` 的 KDoc 写着库的硬约束：`WindowDialog` 要在 Miuix Scaffold 的 content lambda 内**无条件调用**、靠 `show` 控制显隐，否则不显示。而 MD3 原版把两个 `AlertDialog` 写在 `Scaffold` **外面**。合并后统一放 content 里面（满足 Miuix；MD3 的 `AlertDialog` 是独立窗口，放里放外渲染无差别）。`AppConfirmDialog` 的 KDoc 里把这条写成了对调用方的要求。

**两处"不猜"的决定**：① Miuix `InputField` 的参数没在上游 `TextField.kt` 里搜到（`grep "fun InputField("` 无命中，它可能在别的文件或别的声明形式里）—— 没有继续猜，而是**照搬仓库里已经编译通过、且真机验过的调用形态**（`query`/`onQueryChange`/`onSearch`/`expanded`/`onExpandedChange`/`label`/`modifier`），这比上游源码更贴近本项目实际构建的那个版本。② `AppConfirmDialog` 的非危险确认按钮，Miuix 侧**不传 `colors`**（用库默认值），而不是写 `textButtonColors()` 去猜默认色 —— 合并前两版只有危险操作这一种用法，非危险分支照库默认走最稳。

**`ImeHandlingTest` 的迁移方式与路线图设想的不同**：路线图第 3 条写的是「`AppScaffold` 统一吃掉 inset，守卫改成检查 `AppScaffold` 一处 + 屏幕不得自己再加」。实际选了另一条：**屏幕自己传 `modifier = Modifier.imePadding()`**。三个理由：① 无条件加 inset 对没有输入框的屏幕（消耗记录页、首页、统计页）是行为改动，违背「纯搬运」；② 显式声明让「哪一屏要避让」在屏幕文件里看得见，读代码时不用跳到组件层；③ 守卫清单反而从 5 项缩到 4 项 —— 合并后 `imePadding()` 只有一份，**一个条目就覆盖两套主题**。清单同时抽成 `imeScreens` 属性并在 KDoc 里写明「每合并一对就删掉对应的 `Miuix*Screen.kt` 条目」（文件已不存在时 `read()` 返回 null，会被判成「缺少 imePadding」而误报 —— 这正是本轮必须同批改测试的原因）。

**守卫改动**：`ImeHandlingTest` 第 1 条清单 5 → 4（删 `MiuixArchiveScreen.kt`，加注释说明合并后的语义）；`ScreenParityTest` 的 `MergedScreens` 加 `ArchiveScreen.kt`（不许把双胞胎加回来）。第 2、3 条与 `CompactConsumptionTest` 本对无关，未动 —— 落笔前实测过：App 外壳 5 个文件的 `.imePadding()` 仍是 4 处、`.navigationBarsPadding()` 仍是 4 处，底栏仍不含 `imePadding()`，三个弹窗宿主仍有 `decorFitsSystemWindows = false`。

**本地预演（8 个受检文件）**：括号平衡、逐文件 import 使用率、把三个守卫共 8 条断言照测试逻辑跑一遍 —— **这一轮 0 命中**（第 1 对时抓到的 `MiuixText` 未使用 import 那类问题没有再犯；`AppChrome.kt` 这次新增的 `RowScope`/`DeleteForever`/`Delete`/`MiuixTheme` 四个 import 全部有实际使用点）。

~~需真机复测~~ ✅ **用户已真机复测通过（2026-09-16，两套主题）**（两套主题各一遍归档页）：① 搜索框形态（MD3 圆角 50 + 放大镜 + placeholder「搜索归档食品…」；Miuix `InputField` 的 label 形态）与输入即时筛选；② 筛选 chip 选中态配色（MD3 走 `MaterialTheme`，Miuix 走桥接后的 `MiuixTheme.primaryContainer`）+「全部」项；③ 归档行：头像 44dp、名称单行省略、「原因 emoji 原因名 · 中文日期」、恢复按钮 primary 色、删除按钮 error 色 20dp；④ **Miuix 归档行的卡片内衬**（`Card` 的 `insideMargin`，即上面第 1 条约束，最容易看走眼）；⑤ 恢复 → 撤销条（文案分「已合并数量」/「已恢复到零食柜」两种）→ 点撤销要重新归档回原位；⑥ 单条彻底删除弹窗、⑦ 清空归档弹窗（顶栏 error 色删除图标**仅在归档非空时出现**）；⑧ 键盘弹出时搜索框与列表末尾的避让；⑨ 切主题后重复 ①–⑧（弹窗与撤销条的宿主都会换）。

### 17. 第三批 #3 第 3 对：食品详情页合并（697 → 270 行），组件层补上「语义字号档位表」

**做了什么**：`FoodDetailScreen.kt`(354) + `MiuixFoodDetailScreen.kt`(343) → 一份 `FoodDetailScreen.kt`(270)，Miuix 文件删除，`AppNavGraph` 的 `entry<AppRoute.Detail>` 从 if/else 变成一次调用（两个分支的参数列表原本**逐字相同**）。这一对是「结构完全同构、叶子全都不同」的典型：动画（`bounceScale` / `floatOffset` / `floatAlpha` / `burstCount`）、触觉反馈、`delay(750)` 后消耗再返回的时序、信息卡的 7 行标签值，两版逐行一致；不同的只有文字档位、按钮形态、卡片外壳、进度条 API。

**组件层 760 → 1,230 行（注释 249 / 代码 981），新增 3 个文件 + 6 个组件**：

| 落点 | 新增 | 吸收掉的差异 |
|---|---|---|
| `AppText.kt`（新，171） | **`AppTextScale` 档位表** + `AppText` + `AppEmojiText` + 4 个主题色访问器 | 一页用到 5 种档位；逐个包函数会长出一排只差字号的组件，一张表反而好逐行核对（下表） |
| `AppInfo.kt`（新，96） | `AppDetailRow` / `AppDivider` / `AppLinearProgress` | 标签 88dp + 元信息档位 + 弱化色；分隔线两主题各有 `HorizontalDivider`；进度条 MD3 要 `fillMaxWidth + height(8dp) + clip(圆角50)`、Miuix 只给 `height` 与 colors |
| `AppButtons.kt`（新，121） | `AppBigButton` / `AppEditButton` | 主按钮 MD3 = 圆角 50 胶囊 + `secondaryContainer`，Miuix = 库 `buttonColorsPrimary()`；次级按钮 MD3 = 带 18dp 铅笔图标的 `OutlinedButton`，Miuix = **无图标**的 `TextButton` |
| `AppSurface.kt`（81 → 108） | `AppStatusCard`、`AppPaddedCard` | 状态色大卡的圆角（MD3 `shapes.extraLarge` / Miuix 24dp）；以及下面第 1 条那个 `Card` vs `Surface` 的问题 |
| `AppChrome.kt`（220 → 304） | `AppMessageScreen`、`AppEditAction`、`AppDeleteAction`、私有 `AppBarIconButton` | 「该食品已归档或移除」兜底屏（无顶栏 + 居中一句话 + 返回按钮，两版各写一份）；顶栏编辑/删除图标字形与 tint |
| `AppListRow.kt`（233 → 215） | 三个文字叶子改为委托 `AppText`；`AppActionRow` 外壳改用 `AppPaddedCard` | 委托后取值逐字不变（见下表），文件反而短了 18 行 |

**档位表（每个档位都登记了合并前两版各自的取值，便于逐行核对等价性）**：

| `AppTextScale` | MD3 | Miuix | 合并前的用处 |
|---|---|---|---|
| `Hero` | `headlineSmall` | `title2` | 详情页食品名 |
| `Emphasis` | `titleMedium` | `body1` | 剩余天数、兜底屏那句话 |
| `Heading` | `titleSmall` | `subtitle` | 「库存数量」、归档行标题（`RowHeading`） |
| `Body` | `bodyLarge` | `body1` | 详情行的值、消耗记录行标题（`RowTitle`） |
| `Meta` | `bodyMedium` | `body2` | 详情行的标签、行尾「×3 份」（`RowTrailing`） |
| `Hint` | `bodySmall` | `footnote2` | 弱化说明（`AppHintText`） |

**注意 Miuix 的 `body1` 同时承接 MD3 的 `titleMedium` 与 `bodyLarge`** —— 不是偷懒：合并前 `MiuixFoodDetailScreen` 里剩余天数、详情行的值、按钮文案本来就全是 `body1`。档位表保留了 MD3 的区分度，Miuix 侧照它原来的样子塌缩。`AppText` 的默认值也逐项对齐过两主题的 `Text`（`color = Color.Unspecified` 两边都表示「用默认内容色」、`fontWeight = null`、`fontSize = Unspecified`、`maxLines = Int.MAX_VALUE`、`overflow = Clip`），所以「不传 = 与合并前一样」。

**这一对挖出的四件事**：

1. **`Card` vs `Surface` 的坑比第 2 对时以为的更广。** 上次只在归档行遇到，这次发现详情页两张信息卡在 Miuix 侧也是 `Card`、MD3 侧才是 `Surface` —— 也就是说**合并前两版两种都在用**（消耗记录行 `Surface`，归档行 + 详情两张卡 `Card`）。所以不再把它藏在 `AppActionRow` 内部，而是提成公开的 `AppPaddedCard`（Miuix 侧官方 `Card`，多一层 `CardDefaults.InsideMargin` 内衬），并把第 2 对的 `AppActionRow` 改为复用它（去掉那段内联分支，行为不变）。**规矩写进 KDoc：谁用 `AppCard` 谁用 `AppPaddedCard`，由「合并前那一版用的是什么」决定，不许顺手统一** —— 统一掉是只有真机看得出来的视觉改动。
2. **emoji 文本不能套档位。** 详情页那颗飘起来的「😋」两版都只传 `fontSize = 28.sp`、**不指定 style**；而两主题「不传 style」时的默认正文样式并不相同（MD3 取 `LocalTextStyle`，Miuix 取 `LocalTextStyles.current.main`）。套任何一档都等于悄悄改掉其中一边的原样 → 单开 `AppEmojiText`（只传 fontSize）。列表行的 emoji 不走它：Miuix 侧原本显式用了 `title3`，仍由 `RowEmoji` 处理。
3. **非危险操作不要传 `Color.Unspecified` 去赌。** 顶栏「编辑」图标两版都不写 `tint`（用各自库的默认内容色）。`AppBarIconButton` 的 `tint` 因此是 `Color?`，为 null 时**整个参数不传**，而不是传一个「表示默认」的值 —— material3 与 Miuix 是否同样处理 `Unspecified` 没必要去赌。同一思路也用在 `AppConfirmDialog` 的非危险确认按钮上（Miuix 侧不传 `colors`）。
4. **进度条宽度不用调用方操心，但理由要写下来。** 上游 `ProgressIndicator.kt:88-91`（v0.9.4-rc01）的确定性分支内部自带 `.fillMaxWidth().height(height)`，所以 Miuix 版原来不传 modifier 也是满宽；MD3 版的 `fillMaxWidth + 8dp + clip` 则收进 `AppLinearProgress` 内部。抽象前后宽度一致 —— 这条是查了源码才敢做的，凭印象很可能给 Miuix 侧多加一个 `fillMaxWidth` 或反过来漏掉。

**唯一的非等价改动（刻意，且写进了文件 KDoc）**：不再创建 `SnackbarHostState` 与 `SnackbarHost`。两版都建了宿主却**从未弹过任何一条**（`showSnackbar` 全文件 0 处调用）—— 正是待办清单里「详情页『吃掉一份』无撤销」那个缺口的现场。空宿主渲染出来是零尺寸，删掉不改变任何可见行为；将来补撤销时给 `AppScaffold` 传 `snackbar = rememberAppSnackbarHostState()` 即可。除此之外，两版的动画、时序、触觉反馈、文案、间距逐项照搬。

**行数记账**：

| | 合并前 | 合并后 | 差 |
|---|---|---|---|
| 屏幕本体 | 697（354 + 343） | **270** | **-427（-61%）** |
| 共享骨架 | 760 | **1,230** | +470 |
| 合计 | 1,457 | 1,500 | **+43** |

三对累计：屏幕本体 1,659 → **569**（-66%），组件层 1,230 行（注释 249 / 代码 981），渲染层文件 **16 → 13**。净行数暂时还是正的（+140），因为组件层仍在长；**剩余 5 对合计 5,546 行屏幕**（首页 824、列表 830、管理 892、统计 900、设置 2,100），它们主要是复用而不是新建（首页要补顶栏 `subtitle`、列表要多选顶栏与搜索、管理要用 `AppActionRow` + `AppScaffold` 取代那两份私有 `*ManageScaffold`、统计页的图表本来就共用 Canvas、设置页最大但结构是偏好项列表）。按 -60% 的屏幕收缩率与组件层再增 300–500 行估，8 对全合完约落在 4,400–4,600 行，即路线图 #3 的「7,205 → 4,500 量级」；**这是估计不是承诺**，不确定项集中在设置页（1,142 行、偏好项形态两主题差异最大）与统计页图表。

**守卫**：`ScreenParityTest.MergedScreens` 加 `FoodDetailScreen.kt`。`ImeHandlingTest` 本对无关（详情页没有输入框，不在第 1 条清单里；两个弹窗也不含输入框，故第 3 条清单也不用动 —— `AppConfirmDialog` 的 KDoc 里写明了将来若加带输入框的弹窗，MD3 分支必须补 `decorFitsSystemWindows = false` 并登记宿主文件）。

**本地预演抓到 2 个真问题**：`AppInfo.kt` 导入的 `MaterialTheme` / `MiuixTheme` 未被使用（弱化色改由 `appMutedColor()` 提供后就成了残留 —— detekt `UnusedImports` 是激活的、门禁在 block 模式，会直接红）；详情页的 `components.app.*` import 排在 `rememberStatusUi` 之后（ASCII 乱序，ktlint 未启用排序规则故不会红，但不留毛刺）。另有 2 处「乱序」是仓库既有约定（`java.*` 排最后），未动。

**需真机复测（两套主题各一遍详情页）**：① 顶栏编辑/删除图标（MD3 `Edit` + `Delete`；Miuix 两个 `Edit`/`Delete`，删除为 error 色）；② 状态卡：圆角（MD3 extraLarge / Miuix 24dp）、底色为状态语义色、头像 80dp 底色、食品名档位、状态徽章、**进度条满宽且 8dp 高、轨道色 = surface**、剩余天数文案；③ **「吃掉一份」大按钮**：非最后一份 → 弹跳 + 😋 上浮 + 角标 `×N` 累加 + 轻触觉；最后一份 → 长触觉 + 先播动画 750ms 再消耗并返回；吃光后按钮禁用且文案变「已经吃光啦」；④ 信息卡 7 行（分类 / 存放位置 / 生产日期 / 保质期 / 预计过期 / 临期提醒的「单独设置 vs 分类默认」/ 有备注时才出现的备注行）与分隔线间距；⑤ **Miuix 信息卡的内衬**（`AppPaddedCard`，即上面第 1 条，最容易看走眼）；⑥ 库存卡标题 + 提示语 + 步进器加减；⑦ 编辑入口（MD3 有铅笔图标的胶囊 / Miuix 无图标的文字按钮）；⑧ 「移入归档」弹窗 → 确认后返回上一页且归档页能查到；⑨ **食品已归档/移除时的兜底屏**（无顶栏、居中一句话 + 返回按钮）；⑩ 切主题后重复 ①–⑨。

### 18. 第三批 #3 第 4 对：首页合并（824 → 333 行），净行数首次明显为负

**做了什么**：`HomeScreen.kt`(423) + `MiuixHomeScreen.kt`(401) → 一份 `HomeScreen.kt`(333)，Miuix 文件删除，`NavChrome.kt` 的 pager `page 0` 从 if/else 两分支变成一次调用（两分支参数逐字相同，324 → 316 行）。这是**第一个 Tab 页**合并（前三对都是二级页），于是撞上两处此前没遇到的外壳差异：撤销条要躲悬浮导航栏、顶栏带副标题。

**组件层 1,230 → 1,587 行（注释 331 / 代码 1,256）**：

| 落点 | 新增 | 吸收掉的差异 |
|---|---|---|
| `AppChrome.kt`（304 → 393） | `AppSnackbarPlacement`、`AppSnackbarHostState.showMessage()`、`AppTopBar` 的 `subtitle` + `scrollBehavior`、MD3 折叠式 `LargeTopAppBar` 分支 | 撤销条落位；带副标题的顶栏（首页顶栏两版形态完全不同） |
| `AppSurface.kt`（108 → 215） | `AppStatCard` + `AppStatTone`；`AppPaddedCard` 加**可点形态** | 三张统计卡（配色按语义在内部取）；紧急行整行可点 |
| `AppListRow.kt`（215 → 271） | `AppSectionHeader` | 「需要处理 …… 全部食品 →」（箭头字形 MD3 `ArrowForward` / Miuix `Forward`，点击区是胶囊） |
| `AppButtons.kt`（121 → 166） | `AppWideButton` | 「一键清理」：MD3 `FilledTonalButton` 圆角 50 + 图标继承内容色 / Miuix `buttonColorsPrimary()` + 图标**必须**显式 `onPrimary` |
| `AppText.kt`（171 → 223） | 档位表 +5 项；容器色访问器 +2 | 见下表 |
| `AppConfirmDialog.kt`（113 → 122） | `contentTopPadding` | 首页那个 Miuix 弹窗原本在摘要与按钮行之间留了 8dp，归档/详情页的两个弹窗没有（不能顺手统一，否则动到已真机验证过的页面） |
| `AppInfo.kt`（96 → 95） | 删掉一个只被 KDoc 引用的 import | 预演抓到：代码里没用 `padding`，只有 KDoc 提到它（detekt 把 KDoc 引用算作「用过」，所以第 3 对没红） |

**档位表新增 5 项，交叉映射正式出现**：

| 档位 | MD3 | Miuix | 用处 |
|---|---|---|---|
| `Value` | `headlineMedium` | `title2` | 统计卡数字（比 `Hero` 在 MD3 侧大一级、Miuix 侧同档） |
| `Label` | `labelMedium` | `footnote2` | 统计卡标签、「今日提醒」 |
| `SectionTitle` | `titleMedium` | `title4` | 「需要处理」 |
| `Link` | `labelLarge` | `body2` | 「全部食品」 |
| `ItemTitle` | `titleSmall` | `body2` | 紧急行的食品名 |

`Heading` 与 `ItemTitle` 在 MD3 侧都是 `titleSmall`，Miuix 侧却是 `subtitle` 与 `body2`；`Emphasis` 与 `SectionTitle` 在 MD3 侧都是 `titleMedium`，Miuix 侧是 `body1` 与 `title4`。**这不是表设计得乱，是原版就这样**：同一个 MD3 样式在不同页面被 Miuix 侧映到不同档位。按字号推导去「统一」它们才是改行为 —— 枚举 KDoc 已明写这条（登记表 ≠ 换算表）。

**四处结论**：

1. **撤销条落位有两种，一个 `Dp` 参数表达不了**：Tab 页（首页/设置）两版都抬 `84dp` 躲悬浮导航栏，且 MD3 侧**不再**叠 `navigationBarsPadding()`（原版本来就没叠，叠上去会推得更高）；二级页 MD3 侧是 `navigationBarsPadding() + 24dp`、Miuix 侧不抬。做成具名枚举 `AppSnackbarPlacement`，因为这两种各有道理，写成一个数字下次没人记得 84 是怎么来的。**顺带把第 8 对的坑记进 KDoc**：MD3 `SettingsScreen` 用的是普通 `SnackbarHost` 而非可滑掉的 `SwipeDismissSnackbarHost`，届时要么给宿主形态再加一个维度，要么接受「设置页的条也能滑掉」这个行为变化并单独说明 —— 别默认套 `FloatingNav` 就算完。
2. **一个 `subtitle` 参数喂两边，不开「一个主题静默忽略参数」的口子**（这正是第 2 对时在 `AppTopBar` KDoc 里给自己留的约束）：Miuix 直接吃（上游 v0.9.4-rc01 `TopAppBar.kt:100` 已核对 `subtitle: String = ""`，传空串与不传逐字等价）；MD3 的 `TopAppBar` 没有这个参数，所以「带副标题」在 MD3 侧就是折叠式 `LargeTopAppBar` + `nestedScroll` + `fillMaxSize` —— 合并前 MD3 首页正是这么写的。`fillMaxSize` 也只加在 MD3 分支（Miuix 首页的 Scaffold 原本没有 modifier，不替它加）。Miuix 同样有 `largeTitle` + `scrollBehavior` 能做折叠顶栏，但合并前没用，**不替它开**。折叠状态因此与主题绑定、切主题会重建 —— 与 `AppSnackbarHostState` 换宿主是同一类取舍。
3. **可点卡片别用 `Modifier.clickable` 自己套**：`AppPaddedCard` 加了 `onClick` 形态，MD3 侧走 `Card(onClick)`（= 带 shape 裁剪涟漪 + button 语义的 Surface，配色/elevation 照抄原 `UrgentRow`），Miuix 侧走库的 `Card(onClick)`。自己套 clickable 会丢掉形状裁剪的涟漪与语义。
4. **统计卡的配色按语义传（`AppStatTone`），不让屏幕取色再传进来**：合并前两版都是「按语义挑容器色」（primary/secondary/error Container），若把 `container/content: Color` 留在签名里，屏幕就又得写一遍 `if (isMiuix)`。**刻意为统计页的 `MiniStat` 没有预先抽象**：它同构但不同参（emoji 20sp、间隔 6dp、数值走 `Hero` 档、`value` 是 String、不可点），等第 7 对到手、两边都看得见时再决定加参数还是各自保留 —— 现在替一个没合并的调用点定 API 是猜。

**唯一的非等价改动（刻意，写进文件 KDoc）**：Miuix 首页原本把「放弃损坏数据」弹窗写成 `if (show) { MiuixDialog(show = true) }`，与 `MiuixDialog.kt` KDoc 里的库约束（**无条件调用 + `show` 控制**）相反；合并后走 `AppConfirmDialog`，规矩自动满足。MD3 侧那个 `AlertDialog` 从 Scaffold 外挪进 content lambda（独立窗口、渲染无差别，与第 2/3 对同一处理）。

**守卫：这一对撞出第三个「点名屏幕文件」的静态守卫**。`CorruptGuardTest` 的 `损坏数据有放弃入口且两个首页都接上` 会 `read("...MiuixHomeScreen.kt")` 并对两份各断言一遍 —— 文件删掉后 `miuix!!` 直接 NPE。已同批迁移成「一份覆盖两套主题」（断言强度不变：仍查 `onDiscard = {` 与「放弃损坏的数据？」；并注明若 `MiuixHomeScreen.kt` 复活，`ScreenParityTest` 会先红）。**流程教训**：前两对的守卫清单里只有 `ImeHandlingTest` / `ScreenParityTest`，因为消耗记录/归档/详情都不涉及损坏数据 —— 靠记忆列清单迟早漏。从此每对合并前都跑一遍 `grep -rn "Miuix<屏幕名>" app/src/test/`，已写进 `CLAUDE.md`。`ImeHandlingTest` 本对无关（首页无输入框；`NavChrome.kt` 只删了调用点，chrome 5 文件求和仍 ime=4 / nav=4、底栏无 `imePadding()`，已按测试逻辑模拟验证）。`ScreenParityTest.MergedScreens` += `HomeScreen.kt`。

**行数记账：本对净 -142 行**（屏幕 -491、组件层 +357、chrome -8）。四对累计：屏幕本体 **2,483 → 902**（-64%），组件层 1,587，渲染层文件 **16 → 12**；合起来 2,489 vs 合并前 2,483，加 chrome -8 → **净 -2，走到一半账打平**（第 1 对 +106、第 2 对 -9、第 3 对 +43、第 4 对 -142：边际成本确实在掉）。

**剩下的账（说实话）**：剩余 4 对 4,722 行屏幕（列表 830 / 管理 892 / 统计 900 / 设置 2,100）。按 -60% 屏幕收缩 + 组件层再增 400–600 行估，全合完约 **4,900 行**；设置页若能到 -70%（它是偏好项列表、重复度最高）约 **4,600 行**。路线图 #3 写的「7,205 → 4,500 量级」现在看是乐观端，主要因为组件层的 KDoc 占 21%（331/1,587），当初估 4,500 时想的是代码行。**要不要为达标削注释：不削** —— 这一轮的每条结论（`Card` vs `Surface`、`subtitle` 不静默忽略、84dp 的来历、`MiniStat` 为何先不抽象）都是靠注释才没在下一对里被推翻。

**⚠️ CI 红过一次（run 35081356433，三个 job 全红），根因是我的编辑脚本、不是 Kotlin**：ktlint 一句话定位 `AppChrome.kt:178:35: Not a valid Kotlin file (178:35 expecting an element)`，现场是 `subtitle = subtitle ?: ,` —— 本该是 `subtitle = subtitle ?: ""`（后面那个逗号是参数分隔符）。新文本以 `?: ""` 结尾、紧接 Python 三引号结束符，`""` 被当成结束符吃掉。**本地预演为什么没抓到**：它查括号平衡、import 使用率、排序、尾随空白 —— 花括号一个不少，token 级损伤看不出来；本地没有 JDK，唯一能解析 Kotlin 的就是 CI 里的 ktlint，这一轮它正是照设计当了第一道关（也说明「门禁从只跑样式升级为真正阻断」那次修复值回票价：换成从前，这条会以编译错误的形式在 debug job 里炸，定位信息更绕）。

**处置**：① 只改那一行（`c52312b`），无行为改动（空串本就是上游默认值）；② 给本地预演加一条「Kotlin 里不可能合法出现的 token 序列」扫描（`?: ,` / `= ,` / `( ,` / `, ,` / `-> ,` / `to ,`），全仓扫过一遍确认仅此一处损伤 —— 第一版模式写宽了，把合法的尾随逗号 `),\n)` 也算进去，误报 39 处，已收窄；③ 从此**用 Python 三引号写含空串字面量的 Kotlin 时，字面量用 `chr(34)` 拼**（修复那一行就是这么改的）。修复后 run 35081737723 三 job 全绿（detekt 零发现、自检仍命中 2 条已知违规证明规则在跑）。

**需真机复测（两套主题各一遍首页）**：① 顶栏「吃了么」+ 当天中文日期；**MD3 侧上滑折叠**（`LargeTopAppBar` 收起、底色转 `surfaceContainer`），Miuix 侧顶栏不折叠但要有副标题；② 三张统计卡：三套容器色、emoji 22sp、数字 ExtraBold、标签档位、圆角（MD3 24dp = `shapes.large`）与 **Miuix 卡片内衬**、点击分别跳「全部 / 即将过期 / 已过期」筛选；③ 今日提醒条：四种文案（空柜 / N 件过期 / N 件临期 / 全新鲜）、44dp 圆形图标底色（primaryContainer）、**Miuix 内衬**；④ 一键清理按钮：仅在有已过期记录时展开（300/200ms 动画）、**Miuix 侧图标必须是 `onPrimary` 色**（压在库主色底上，不传就看不清）、点击归档后**撤销条落在悬浮导航栏之上（84dp）**、点撤销整批恢复；⑤ 「需要处理」标题 + 「全部食品 →」链接（胶囊点击区、箭头字形两主题不同）；⑥ 紧急行：头像 44dp（状态色底）、名称档位（MD3 `titleSmall` / Miuix `body2`，且**允许换行**不做单行省略）、副标题「剩余天数 · 数量 单位」用**状态语义色**、右侧状态徽章、整行点击进详情、增删时的 `animateItem` 动画；⑦ 空态 🎉；⑧ 自动同步完成后的提示条（同样 84dp 落位）；⑨ 损坏数据横幅 +「放弃损坏的数据？」弹窗（**要有损坏数据才出得来**，不便构造就靠 `CorruptGuardTest` 守着代码路径）；⑩ 切主题后重复 ①–⑨。

### 19. 第三批 #3 第 5 对：食品列表页合并（830 → 267 行），累计净行数首次为负

**做了什么**：`FoodListScreen.kt`(419) + `MiuixFoodListScreen.kt`(411) → 一份 `FoodListScreen.kt`(267，-68%)，Miuix 文件删除，`NavChrome.kt` 的 pager `page 1` 从 if/else 两分支变成一次调用（两分支参数逐字相同，316 → 307 行）。这是**第二个 Tab 页**（第 4 对是首页），也是目前最复杂的一页：顶栏有「浏览 / 多选」两态、搜索框常驻、筛选面板可折叠、列表尾部还要拼归档搜索结果。两版 830 行里只有 166 行不同，且差异**全在叶子上**（顶栏形态、搜索框控件、chip 与胶囊的取色/字形/弹簧、归档行外壳）；业务结构（`BackHandler`、`AnimatedVisibility`、空态 `Crossfade`、`animateItem` 的 tween 规格、库存卡片 `FoodCard`）两版逐字相同 —— 库存卡片本来就是共用组件，这也是它比首页那对收缩更多的原因。

**组件层 1,587 → 1,898 行（注释 406 / 代码 1,492，注释占 21.4%）**：

| 落点 | 变化 | 新增 | 吸收掉的差异 |
|---|---|---|---|
| `AppControls.kt` | 102 → 278 | `AppChipTone`、`AppFilterSectionLabel`、`AppFilterToggle`（+3 个私有件 `FilterToggleShell`/`Row`/`Icon`） | 筛选胶囊（图标 + 计数文案 + 转 180° 的箭头 + 激活态配色 + `AnimatedContent` 换字形）；chip 三排各自的容器色与标签字号；分组小标题 |
| `AppChrome.kt` | 393 → 463 | `AppTopBar(onClose, selectionMode)`、私有 `AppBarNavIcon`、`AppSelectAllAction`、`AppArchiveAction` | 多选态顶栏（标题换成「已选 N 项」、左上角 ✕ 取代 ←、MD3 底色转 `surfaceContainer`）；全选按钮；归档入口（MD3 `History` / Miuix `Recent`） |
| `AppInfo.kt` | 95 → 133 | `AppHistoryNote` | 「归档中找到 N 条」的图标（MD3 `History` / Miuix `Recent`，16dp）+ `Label` 档 + 弱化色 |
| `AppSurface.kt` | 215 → 234 | `AppCardTone` + 私有 `md3CardColor`，`AppPaddedCard(tone)` | 归档匹配行比归档页的行**低一档底色**（MD3 `surfaceContainerLow`） |
| `AppText.kt` | 223 → 237 | 档位 `Tag`；`Link` → `Action` 改名 | 筛选面板分组小标题与列表行「月度合计」标注共用一档（`labelSmall`/`footnote2`） |
| `AppListRow.kt` | 271 → 265 | `AppActionRow(tone)` 转发；私有 `RowTag` 改为委托 `AppText(Tag)` | 归档匹配行的底色档；顺带少一处硬编码样式（值逐字相同，无行为变化） |

**档位表 +1 项、改名 1 项**：

| 档位 | MD3 | Miuix | 用处 |
|---|---|---|---|
| `Tag`（新） | `labelSmall` | `footnote2` | 列表行的来源标注「月度合计」、筛选面板的「状态/分类/位置」小标题 —— 比 `Hint` 在 MD3 侧再小一档 |
| `Action`（原 `Link`） | `labelLarge` | `body2` | 首页「全部食品」链接 **+** 列表页筛选胶囊的「筛选(N)」文案 |

**六处结论**：

1. **`Link` 改名 `Action`，这是本轮唯一一处 API 重命名**：第 4 对为首页链接加的档位，第 5 对发现筛选胶囊的文案是**同一映射**（`labelLarge` → `body2`）。档位表登记的是「MD3 样式 ↔ Miuix 样式」这对关系，名字若绑死在「链接」上，下一个调用点就会误判成要新开一档，然后表里长出两个同值不同名的档位 —— 那才是真的乱。映射值一字未动，两个调用点都在 KDoc 里点名。
2. **`AppCardTone` 只影响 MD3，但不是静默忽略**（沿用第 2 对给 `AppTopBar.subtitle` 立的规矩）：MD3 侧 `AppPalette` 的 ColorScheme 是 seed 生成的，`surfaceContainerLow` ≠ `surfaceContainer`，归档匹配行确实要比归档页的行低一档；Miuix 侧两版原来都是库 `Card` 的默认底色，上游 `Card` 的 `colors` 默认走 `CardDefaults.cardColors()`，**不替它猜「低一档」对应哪个色** —— KDoc 明写这一点，而不是留个只在单边生效的参数让人以为两边都变了。
3. **chip 的 `tone` 只管容器色，选中态文字色只有 `Primary` 显式指定**：合并前状态排两版都写了 `selectedLabelColor = onPrimaryContainer`，分类/位置排两版都只给容器色（文字色吃 material3 `filterChipColors` 的默认 `onSecondaryContainer`）。保持原样，不按「三排应该一致」去补 —— 补上去就是改行为。
4. **筛选胶囊的箭头弹簧两套手感保留**：MD3 用 `MotionSpring.expand<Float>()`，Miuix 用库的 `folmeSpring<Float>(damping = 0.95f, response = if (expanded) 0.2f else 0.3f)`（展开比收起快）。`AnimatedContent` 的图标切换只写一遍，差异收进一个 `spring: MotionSpec<Float>` 分支。**显式类型参数 `<Float>` 照抄原文**：本地没有编译器，泛型推断出错只能等 CI，能少一处推断就少一处。
5. **顶栏两态的优先级与空槽等价性**：`AppBarNavIcon` 里 `onClose` 优先于 `onBack`，两者都为 null 时**不渲染任何东西** —— 与两版原来「不给 `navigationIcon` 槽」逐字等价（MD3 默认槽是空 lambda，Miuix 上游默认也是空）。`selectionMode` 只让 MD3 换底色（`surfaceContainer` / `background`），Miuix 两态同色。全选按钮的**字形不对称也保留**：MD3 恒用 `SelectAll` 字形只换 contentDescription，Miuix 已全选时换成 `Close` 字形 —— 这是合并前两版各自的写法，`AppSelectAllAction` 内部消化并写进 KDoc。
6. **`LargeTopAppBar` 分支顺带补上 `navigationIcon`**：第 4 对加的折叠分支原本没接导航图标（当时首页顶栏两版都没有返回键）。这一对多选态要用 ✕，所以两个分支都接 `AppBarNavIcon`。折叠态与多选态在列表页不会同时出现（列表页不传 `subtitle`），但接上之后组合起来也不会漏图标。

**两处非等价改动（都是文本/命名层面，刻意，已写进 KDoc）**：① 空态 `Crossfade` 的 `label` 统一成 `foodListEmptyCrossfade`（Miuix 版原本带 `miuix` 前缀，只是为了在双胞胎文件里区分），它只是转场调试标识，不影响渲染；② `RowTag` 从「私有硬编码 `labelSmall` + 弱化色」改成委托 `AppText(AppTextScale.Tag)`，取值逐字相同，好处是这一档以后只有一个定义点。

**编辑脚本又抓到自己一处，这次是括号预演**：`AppFilterChip` 的替换用「`index(锚点)+200` 再 `rindex('}')`」切出旧函数体，切在了结构中间，尾部留下 3 行残渣（`},` / `        ),` / `    )`），花括号 -1、圆括号 -2。第 4 对之后加的「不可能合法 token」扫描这次**没用上**（残渣本身是合法 token，只是位置错），抓到它的是最早就有的括号平衡检查。**教训写死**：替换整个代码块时用完整文本锚点（旧块从 `@Composable` 到函数收尾整段贴进脚本），不用下标切片 —— 切片等于把「找边界」这件事交给运气。另两处自纠：删掉自己引入的未用 import（`Spacer`/`width`）、把内联全限定类型换成 import（`RowScope`/`ImageVector`）。

**守卫**：`ImeHandlingTest` 的 `imeScreens` 从 4 项回到 3 项（`MiuixFoodListScreen.kt` 那条本来就注着「食品列表合并后删掉这一条」）。**差点写出重复条目**：清单里原本已有 MD3 的 `FoodListScreen.kt`，把 Miuix 那条原地替换成同名条目会让清单出现两次同一路径 —— 断言不会失败（同一文件查两遍），但清单是给人读的，重复意味着以后删文件时必漏一处。改成合并为一条 + 注明「一个条目覆盖两套主题」。第 2/3 条按测试逻辑模拟仍过（chrome 5 文件求和 ime=4 / nav=4、底栏无 `imePadding()`；`NavChrome.kt` 只删了调用点）。`ScreenParityTest.MergedScreens` += `FoodListScreen.kt`。`CorruptGuardTest` 本对无关，但按 §18 立的流程仍跑了 `grep -rn "MiuixFoodListScreen" app/src/test/` —— 命中只有 `ImeHandlingTest` 一处，符合预期。

**行数记账：本对净 -261 行**（屏幕 -563、组件层 +311、chrome -9）。五对累计：8 对屏幕本体 **7,205 → 5,061**，组件层 1,898，合计 6,959（chrome 另 -17 不计入基线）；已合并的 5 对屏幕本体 **3,313 → 1,169（-65%）**。每对净：+106 / -9 / +43 / -142 / **-261** —— **累计首次为负**（-263），且第 5 对的边际收益跳了一档：组件层这一对只长了 311 行就吃掉 563 行重复，因为「顶栏两态」「筛选胶囊」「chip 取色」都是后面 Manage/Stats/Settings 还要再用的件。

**剩下的账**：Manage 892 / Stats 900 / Settings 2,100 = 3,892 行屏幕。按 -60%（Manage/Stats）与 -65%（Settings，偏好项列表重复度最高）估，剩 3 对约 1,470 行屏幕 + 组件层再增 350–450 行 → 全合完约 **4,900–5,000 行**。与 §18 的估算一致（路线图写的「4,500 量级」仍是乐观端，差距就是组件层那 406 行注释，**不削**的理由同 §18：本轮六处结论全靠注释才没在下一对被推翻）。

**⚠️ CI 又红过一次（run 35086912287），但这次只红在格式，实质全绿**：`debug APK + 单测 + lintDebug` 与 `release + R8 + lintRelease` 两个 job **都 success**（合并后的 Kotlin 编译通过、117 例单测含迁移过的三个守卫全绿、R8 与 Android lint 干净），失败的是 `静态门禁（ktlint / detekt）`，且只有一条：`AppText.kt:85:1: Needless blank line(s) (standard:no-consecutive-blank-lines)` —— 加 `Tag` 档位时脚本在新文本尾部多写了一个空行，与原有空行叠成两行（detekt 仍是 0 发现）。

**性质与第 4 对那次不同，这点必须写清**：第 4 对是三引号吃掉空串字面量造成 **token 级损伤**，本地没有 Kotlin 解析器，客观上查不出来；这次是**纯文本格式**，本地完全查得出来 —— 只是预演清单里没有这条规则。**处置**：① 只删那一个空行（紧跟 `b74d572` 之后那个提交；不写它自己的 hash —— amend 会改 hash，自指必然过期），无任何行为改动；② 预演清单补 4 条「不需要编译器就能查」的 ktlint 规则：连续空行、`}` 前空行、文件末尾恰好一个换行、不含 Tab —— 补完全仓 100+ 个 `.kt` 扫一遍，命中 0 处，说明这几条在 HEAD 上本来是干净的（扫描有意义，不是空转）；③ **教训**：ktlint 的规则集里能静态查的那部分应该一次补齐，而不是每红一次补一条。下一对（管理页）开工前先把 `.editorconfig` 里启用的 standard 规则过一遍，凡是纯文本可判定的都进预演。

**需真机复测（两套主题各一遍列表页，第 3/4 对的复测项一并带上）**：① 顶栏两态：浏览态「食品列表」+ 归档入口（MD3 `History` / Miuix `Recent`）；多选态标题「已选 N 项」、左上角变 ✕（点了只退出多选、不切页）、**MD3 底色转 `surfaceContainer`、Miuix 两态同色**；② 系统返回键：多选时只退出多选，非多选时正常返回；③ 全选/取消全选按钮：**MD3 恒为全选字形、只换无障碍描述**，Miuix 已全选时字形变 ✕；④ 搜索框（圆角 50；MD3 `OutlinedTextField` + 前导放大镜 / Miuix `InputField`），**键盘弹起后列表可视区止于键盘顶边**（`imePadding()`），底部留白 = 导航栏 + 96dp，末尾条目滚得出来；⑤「筛选(N)」胶囊：N 随激活筛选数变化、箭头转 180°（**MD3 `MotionSpring.expand` / Miuix `folmeSpring`，展开 response 0.2、收起 0.3**）、有激活筛选时整颗转 `primaryContainer` 且图标转 `onPrimaryContainer`、无激活时弱化色；⑥ 筛选面板展开/收起动画（`filterPanelEnter`/`Exit`）；⑦ 三排 chip：状态排 `primaryContainer` + **选中态文字色显式为 `onPrimaryContainer`**、分类排 `secondaryContainer`（带 emoji 前缀、再点一次取消）、位置排 `tertiaryContainer`（**仅在有使用过的位置时出现**）、后两排选中态文字色吃库默认；⑧ 分组小标题「状态/分类/位置」：`labelSmall`/`footnote2` 档 + 弱化色 + 左右 20dp；⑨ 增删分类后 chip 排不闪（`key = c.id`）；⑩ 库存卡片：长按进多选、浏览态单击进详情、多选态单击切换选中、加减号、增删时 `animateItem`；⑪ 空态 `Crossfade`：空柜 🧺「零食柜还是空的」vs 无匹配 🔍「没有符合条件的食品」，两者切换有淡入淡出；⑫ 搜索命中归档时列表尾部：「归档中找到 N 条」（图标 MD3 `History` / Miuix `Recent`，16dp）+ 归档行（头像 40dp、副标题「emoji + 归档原因」、**MD3 底色 `surfaceContainerLow` 比归档页的行低一档 / Miuix 与归档页同色**、恢复与彻底删除两个图标按钮及其描述）+ 恢复后的撤销条；⑬ 切主题后重复 ①–⑫。

### 20. 第三批 #3 第 6 对：管理页三合一（892 → 282 行），`AppNavGraph` 变成零主题分支

**做了什么**：`ManageScreens.kt`(458) + `MiuixManageScreens.kt`(434) → 一份 `ManageScreens.kt`(282，一个文件里三个二级页：临期阈值 / 分类 / 存放位置)，Miuix 文件删除。**`AppNavGraph.kt` 从此不含任何主题分支**（153 → 143）：三个 `entry` 的 if/else 变成一次调用，`LocalThemeStyle` / `ThemeStyle` 与三个 Miuix 屏幕的 import 一并删掉。两份私有的 `*ManageScaffold` 也删了 —— `ARCHITECTURE.md` 从第 1 对起就写着「待管理页那一对合并时删除」，这次兑现，`AppChrome.kt` 的 KDoc 也从「等…删掉它们」改成已兑现的口气。三页在两版里逐字同构（连 `contentPadding`、`spacedBy(10.dp)`、「N 条食品使用中」的文案都一样），差异集中在五处叶子：私有脚手架、输入弹窗的机制、添加按钮的形态、步进器的字形、禁用删除按钮的取色。

**组件层 1,897 → 2,331 行（注释 515 / 代码 1,816，注释占 22%）**：

| 落点 | 变化 | 新增 | 吸收掉的差异 |
|---|---|---|---|
| `AppFormDialog.kt` | 0 → 148（新文件） | `AppFormFieldSpec` + `AppFormDialog` | 「添加/编辑分类」（两字段）与「添加存放位置」（一字段）四个弹窗；MD3 `AlertDialog` + `OutlinedTextField` vs Miuix `WindowDialog` + `TextField(TextFieldState)` |
| `AppListRow.kt` | 265 → 406 | `AppCardRow`、`AppLocationIcon`、`AppEditRowAction`、`AppDeleteRowAction`（+ 私有 `RowIconButton`） | 管理页三行的外壳（前导槽 + 标题 + 可选副标题 + 尾部槽、Miuix 侧 24dp 圆角）；位置图标字形；行内编辑/删除按钮的字形与取色 |
| `AppControls.kt` | 278 → 377 | `AppStepperPill`（+ 私有 `StepperValue`） | 阈值行的步进器胶囊：容器、字形、数值档位 |
| `AppButtons.kt` | 166 → 188 | `AppAddItemButton` | 「添加分类 / 添加位置」：MD3 `OutlinedButton` 胶囊 + 加号 vs Miuix `TextButton(text = …)` |
| `AppText.kt` | 236 → 250 | `appFaintColor()` | 禁用图标色：MD3 `outlineVariant` / Miuix `dividerLine` |
| `AppSurface.kt` | 234 → 244 | `AppCard(miuixCornerRadius)` | Miuix 侧 16dp（库默认）与 24dp（管理页三行原本的值）两种圆角 |

**七处结论**：

1. **输入弹窗的两套机制不桥接**（`AppFormDialog` 的文件头注释与 KDoc 都写了）：MD3 用 `OutlinedTextField(value, onValueChange)`，**边打字边截断**，用户根本打不进第 9 个字符；Miuix 用 `TextField(state: TextFieldState)`，**允许超长输入、点确认时才 `take(maxLength)`**。要统一成一个「value + onValueChange」外壳，就得在 Miuix 侧用 `LaunchedEffect` 把截断后的文本回写 `TextFieldState` —— 那会动到光标位置与状态重建时机，是只有真机看得出来的行为改动。所以保留两套机制，只保证**对外读到的东西一致**：`onConfirm` 拿到的永远是截断后的值，确认按钮的可用条件永远是「第一个字段非空白」（两版合并前都是这个口径）。`AppFormFieldSpec.placeholder` **只在 MD3 侧生效**（Miuix 传的是 `useLabelAsPlaceholder = true`，没有独立占位符形参）—— 按约束 ⑥ 点名，不替 Miuix 造一个它没有的槽。
2. **`remember` 的 key 必须是 `fields` 本身（所以 `AppFormFieldSpec` 是 `data class`）**：调用方每次重组都 `listOf(…)` 新建一个 List，按引用比较会在每次重组时把用户已经打进去的字清空。合并前 Miuix 版 key 的是 `remember(show, initialLabel)`，同一个道理，只是那边只有一个字段。
3. **卡片圆角差 8dp 属于「只有真机看得出来」的那类改动**：`AppCard` 的 Miuix 侧默认 16dp（库 `Surface` 的默认值，也是消耗记录行等既有调用点的取值），而管理页三行两版都是 24dp（MD3 `shapes.large`、Miuix 显式 `RoundedCornerShape(24.dp)`）。给 `AppCard` 加 `miuixCornerRadius: Dp = 16.dp`（默认值保证既有调用点不变），`AppCardRow` 传 24dp。**参数名直接点明只影响 Miuix**，不用枚举绕一圈 —— MD3 侧恒走 `shapes.large` 这个 token（值就是 24dp），若改成 `RoundedCornerShape(24.dp)` 反而是把 token 化那次修复退回去。
4. **步进器的减号两版都是 material 字形**：合并前 Miuix 版原注释写着「Miuix `Remove` 是『移除/退出』形状，减号与列表步进器一样回退 material」，所以只有加号分主题。照抄，不顺手统一成两个都用 Miuix 字形（那会把减号变成「退出」的样子）。**也不与 `ui/components/QuantityStepper.kt` 合并**：那个带触感反馈与数字竖直滑动动画、单位固定不动，形态与本胶囊不同，合并前就是两套东西。设置页也有步进器（第 8 对），届时两边都看得见再决定复用还是各留一份 —— 与首页 `MiniStat` 同一原则：不替还没合并的调用点定 API。
5. **行内图标按钮刻意不接 `modifier` 参数**：合并前两版都没往这两个 `IconButton` 上传 modifier，而 Miuix 的 `IconButton` 是否有该形参本轮**没有按 pinned source 核过** —— `CLAUDE.md` 明令不得凭记忆臆造 Miuix 签名，所以不需要的参数就不加（真要加时先 `gh api` 核上游）。这条写进了 `RowIconButton` 的 KDoc：组件 API 不是越全越好，多一个没核过的形参就是多一个 CI 才能发现的赌注。
6. **禁用删除按钮的取色收进组件**（`appFaintColor()`：MD3 `outlineVariant` / Miuix `dividerLine`）：两版规则完全相同（可点 = 危险色、禁用 = 最弱色），只是色板角色名不同，所以规则进组件、屏幕只传 `enabled`。名字按共同语义起，不叫 `appOutlineVariantColor` —— Miuix 色板里没有那个角色，叫那名会暗示错。
7. **两处非等价改动（弹窗落位，与第 2/3/4/5 对同一处理）**：三个弹窗全部挪进 `AppScaffold` 的 content lambda。合并前 MD3 版写在 Scaffold **外面**；Miuix 版把「编辑分类」写成 `state.editing?.let { MiuixCategoryEditDialog(show = true, …) }` —— **两边都不合** `MiuixDialog` 的库约束（无条件调用 + `show` 控制），与第 4 对首页那个损坏数据弹窗同一类自动修正。MD3 侧的删除确认弹窗因此从「`state.deleting?.let { … }` 才组合」变成「`AppConfirmDialog(show = …)`」，文案改用 `deleting?.let { … }.orEmpty()` 计算；`show = false` 时 MD3 分支不组合 `AlertDialog`，与合并前等价（`AppConfirmDialog` 从第 2 对起就是这个结构）。

**守卫：这次轮到 `ImeHandlingTest` 第 3 条**（`decorFitsSystemWindows` 按文件点名），也是它第一次因为「弹窗搬进组件层」而必须改清单：`ui/screens/ManageScreens.kt` → `ui/components/app/AppFormDialog.kt`（该测试的 KDoc 本来就写了「弹窗再搬家就改这一行」，照着办）。第 1 条不受影响：管理页**页面内**没有输入框，输入框都在弹窗里。`ScreenParityTest.MergedScreens` += `ManageScreens.kt`（一个条目覆盖三个页面；该测试按文件枚举，不按页面）。`CorruptGuardTest` 本对无关，仍按 §18 立的流程 `grep -rn "MiuixManage" app/src/test/` —— 0 命中。

**行数记账：本对净 -186 行**（屏幕 -610、组件层 +434、`AppNavGraph` -10）。六对累计：8 对屏幕本体 **7,205 → 4,451**，组件层 2,331，合计 6,782（navgraph/chrome 另 -27）；已合并 6 对屏幕本体 **4,205 → 1,451（-65%）**。每对净：+106 / -9 / +43 / -142 / -261 / **-186**，累计 **-349**。

**剩下的账（说实话，比 §19 又上调一点）**：剩 Stats 900 + Settings 2,100 = 3,000 行屏幕。组件层这两对长得比预估快（第 5 对 +311、第 6 对 +434），因为列表页与管理页各自带来了此前没有的**控件种类**（筛选胶囊 / chip 色调 / 步进器 / 表单弹窗）。按统计 -60%、设置 -65%、组件层再 +300~400 估：全合完约 **5,200 行（≈ -28%）**，比 §18/§19 说的 4,900–5,000 又高一点，比路线图写的「4,500 量级」高约 15%。**差距的三块来源说清**：① 组件层 KDoc 515 行（占 22%）不削，理由同 §18（本轮七处结论全靠注释才没在下一对被推翻）；② 组件层每对都在长，因为「同构但不同参」的控件宁可各留一份也不提前抽象（`MiniStat`、`QuantityStepper` vs `AppStepperPill`）；③ 屏幕本体的收缩率稳定在 -65%~-68%，没有再高的空间 —— 剩下的都是业务结构，两版本来就逐字相同。**要不要为达标把组件层里形态相近的控件强行合并：不** —— 那正是把「只有真机看得出来的差异」变成改动，与这一轮的验收口径冲突。

**需真机复测（两套主题各一遍三个管理页，第 3/4/5 对的复测项一并带上）**：① 顶栏标题 + 返回箭头（MD3 `ArrowBack` / Miuix `Back`），底色两主题各自默认（MD3 `background`、Miuix `surface`）—— 两份私有 `*ManageScaffold` 删掉后**不应出现色差**；② 阈值页：顶部说明文字（Hint 档 + 弱化色 + 下 6dp）、每行 emoji 20sp + 分类名（Body 档 + Medium）+ 尾部步进器胶囊（`surfaceContainerHighest` 全圆角、减号/加号 18dp、「N 天」Action 档 + Bold、左右 6dp）、**Miuix 下减号应是 material 的横线而不是「移除/退出」形状**、1 天时减号禁用、365 天时加号禁用、点一下改一档且列表不重排（key = 分类 id）、**行卡片圆角 Miuix 侧应是 24dp 而不是库默认 16dp**、增删分类时有 `animateItem`；③ 分类页：每行 emoji + 名称 + 「N 条食品使用中」（只在 N>0 时出现）、编辑按钮主色、删除按钮**只剩一个分类时禁用且变最弱色**（MD3 `outlineVariant` / Miuix `dividerLine`）、底部「添加分类」（MD3 胶囊 + 加号图标 / Miuix 纯文字按钮）；④ 分类弹窗：两个输入框（分类名称 / Emoji 图标）、**MD3 打到第 9 个字符就打不进去、Miuix 能打进去但确认后只留前 8 个**（这是刻意保留的原差异）、MD3 有占位符「例如：烘焙 / 例如：🍞」而 Miuix 用 label 当占位符、名称为空时确认按钮禁用、**键盘弹起时「取消/确定」不被盖住**（`decorFitsSystemWindows = false` + `imePadding`，已搬进 `AppFormDialog.kt`）、关掉再编辑另一个分类时输入框内容跟着换（`remember` key = `show` + `fields`）；⑤ 删除分类确认弹窗：文案分「有 N 条在用」与「没有在用」两种、删除按钮危险色、Miuix 侧两个按钮等宽、删除后那些记录显示为「其他」；⑥ 位置页：每行位置图标（MD3 `Place` / Miuix `Location`，18dp + 弱化色）+ 名称 + 在用条数、删除按钮**恒为危险色**（位置没有「至少留一个」规则）、「添加位置」按钮、添加弹窗（位置名最多 12 字：MD3 边打边截断 / Miuix 确认时截断，占位符「例如：车里、办公室」）；⑦ 三个页面的弹窗都在 Scaffold 内部弹出（合并后统一落位，MD3 侧渲染应无差别）；⑧ 切主题后重复 ①–⑦。

### 21. 第三批 #3 第 7 对：统计页合并（900 → 409 行），图表本来就是单份，无处可缩

**做了什么**：`StatsScreen.kt`(455) + `MiuixStatsScreen.kt`(445) → 一份 `StatsScreen.kt`(409)，Miuix 文件删除；`NavChrome.kt` 第 2 页从 if/else 变一次调用（307 → 299），`isMiuix` 只剩设置页那一处在用。两份去掉 package/import/注释后是 383 / 362 行代码，其中 **271 行逐字相同**（把 `MaterialTheme`↔`MiuixTheme`、`.typography.`↔`.textStyles.`、`Miuix` 前缀这三类纯词汇差异归一化后是 294 行）—— **图表是 `Canvas` + `layout` 自绘的，本来就与主题无关**，剩下的差异只在三类：文字档位、取色、「节标题放哪儿」。

**重复度数字的口径顺手统一了**：前六对的 devlog 里写「XX% 相同」用的是肉眼归类的估计，本对起改成可复算的口径（`Counter` 多重集交集 + 一条明确的归一化规则），因为这一对的收缩率明显低于前六对，需要拿数字说明「低是因为绘图代码无处可缩」而不是「合并不彻底」—— 271 / 383 = 71% 的代码行本来就一模一样，剩下的 112 行 MD3 独有里，绝大多数是节标题文案、字号档位与 `Surface` vs `Card` 这三类真差异。`DonutChart` / `LegendRow` 两份并一份（连 `Stroke(width = 30f)`、`topLeft = Offset(15f, 15f)`、每段之间留 3° 缝隙、最小 1° 兜底都一样）；柱状图与排行榜行留在屏幕文件里（只此一屏用，且合并后已不含任何主题分支）。

**收缩率 -55%，低于前六对的 -65%~-68%，这是本对的特点不是失误**：那 271 行逐字相同的代码里有一大半是绘图逻辑（环图扫弧、柱高用 `layout` 手工测量、比例条动画），合并前就已经是「一份语义、两份拷贝」，合并只是删掉拷贝 —— 它无处可缩，也不该往组件层搬（只此一屏用，搬进 `ui/components/app/` 等于给通用层加一个专属控件，与第 6 对「宁可各留一份也不提前抽象」的口径一致）。所以本对屏幕本体的降幅天然低于「外壳重复度高」的列表页 / 管理页。

**组件层 2,331 → 2,526 行（注释 580 / 代码 1,837，注释占 24%）**：

| 落点 | 变化 | 新增 | 吸收掉的差异 |
|---|---|---|---|
| `AppText.kt` | 250 → 337 | `AppMutedText`、`appHighestContainerColor()`、`appChartColors()`；`appPrimaryColor` / `appPrimaryContainerColor` 由 internal 放宽为 public | 「弱化正文」这一个语义（两版分别是 `onSurfaceVariant` / `onSurfaceVariantSummary` + 各自档位；`AppHintText` 变成它的 `Hint` 档特例，改为委托）；柱状图空柱底色；**图表 8 色板两份清单**（Miuix 色板没有 `tertiary` / `inversePrimary`，合并前就用容器色与容器前景色替代） |
| `AppSurface.kt` | 244 → 353 | `AppSection`、`AppStatsListMetrics` + `appStatsListMetrics()`；`AppStatCard` 加 `onClick`(可空) / `emojiSize` / `valueSpacing` / `valueScale` | 节标题（MD3 在卡片内 `titleMedium`+Bold+间隔 / Miuix 是卡片外的库 `SmallTitle`、卡片内无标题，**连文案都不同**）；列表度量三值（MD3 `contentPadding` 20dp + 项距 16dp / Miuix 0dp + 12dp + 非卡片 item 自带 20dp）；mini 统计卡与首页统计卡的同构不同参 |
| `AppListRow.kt` | 406 → 406 | 无（私有 `RowTag` 改为委托 `AppMutedText`） | — |
| `AppControls.kt` | 377 → 376 | 无（`AppFilterSectionLabel` 改为委托 `AppMutedText`） | — |

**七处结论**：

1. **第 4 对留的悬案这次判了：`MiniStat` vs `AppStatCard` 是「加参数」而不是「各留一份」**。首页三张统计卡与统计页三张 mini 卡的结构逐字相同（`Column(padding(14.dp))` + emoji + 间隔 + 数值 ExtraBold + 标签），只差四个值：emoji 20sp vs 22sp、间隔 6dp vs 8dp、数值档位 `headlineSmall`/`title2`(= `Hero`) vs `headlineMedium`/`title2`(= `Value`)、**可点 vs 不可点**。前三个是纯参数；第四个是控件语义，所以 `onClick` 收成可空：Miuix 侧 null → 不带 `onClick` 的 `Card`（非 null 才走带 `onClick` 的重载），MD3 侧 null → **`Surface`**（合并前 `MiniStat` 就是 `Surface(shape = shapes.large, color, contentColor)`，没有水波纹也没有按钮语义；换成 `Card` 会多出一层 elevation 与另一档底色）。首页那三处调用全是具名实参，加参数一个都没碰到 —— 这是「默认值 = 首页那一版的值」换来的。
2. **两个色访问器从 internal 放宽成 public，理由与第 2 对的 `appSurfaceColor` 同一套**：统计页要在屏幕里画图表（柱色、比例条色、名次色），这是「屏幕侧取色」唯一被承认的例外。不放宽就得把绘图也塞进组件层，而那正是上一条不搬的理由。放宽的只有 `appPrimaryColor` / `appPrimaryContainerColor`（加新增的 `appHighestContainerColor` / `appChartColors`）；`appMutedColor` / `appErrorColor` / `appFaintColor` 仍 internal —— 弱化色有 `AppMutedText` 这个正规出口，屏幕不该自己去取。
3. **`AppMutedText` 是本对最划算的一个抽象，同时是档位表开始收敛的信号**：同一个「弱化正文」语义此前分散在六处（环图中心小字、柱状图日期、图例右侧百分比、`AppHintText`、私有 `RowTag`、`AppFilterSectionLabel`），抽出来后 `AppHintText` 变成它的 `Hint` 档特例、两个私有小函数各减一行。更重要的是**档位表这次一个都没新增**：本对九个文字位点（环图中心大字 / 小字、图例两列、柱顶数值、日期、名次、名称、×数量、空态）全部命中现成 12 档 —— 第 3 对建表 7 档、第 4 对 +5、第 5 对 +1、第 6 对 0、第 7 对 0，增量在收敛。
4. **一处纠错，写下来免得下次又当 bug 修**：合并前我判断 Miuix 版柱高 `amount.toFloat() / maxDaily` 有 NaN 风险 —— **这个判断是错的**。`StatsState.kt:112` 已经 `.coerceAtLeast(1)`，`maxDaily` 恒 ≥ 1，两版结果完全相同。合并版取 MD3 的 `if (state.maxDaily > 0) … else 0f` 只为口径统一（守卫冗余但无害），**不是修 bug**，真机复测时不必专门盯这一处。
5. **两处非等价改动（都收敛到更好的一边，刻意）**：① 排行榜的点击目标用 `state.findItemIdByName(name)`（MD3 版的写法）—— Miuix 版在 UI 里内联了 `items.firstOrNull { it.name == name }?.id`，属业务查找漏进渲染层，语义相同；② `animateFloatAsState` 的 `label` 统一用 MD3 版的 `"bar"` / `"topRankBar"`（Miuix 版带 `miuix` 前缀只为区分双胞胎文件），与第 5 对空态 `Crossfade` 的 label 同一处理 —— 只是转场调试标识。另外 Miuix 版把状态量 hoist 成局部 `val`（`consumedThisWeek` 等），合并版一律 `state.x`，非行为差异。
6. **`AppSection` 是「两边都看得见之后」才敢写的组件**：合并前两版的节标题一个在卡片里、一个在卡片外，**文案也不同**（「近 7 天消耗趋势」vs「消耗趋势」、「库存分类占比」vs「库存分类」、「消耗排行榜」vs「消耗排行」），所以是两个参数（`cardTitle` / `sectionTitle`）而不是一个 —— 统一任何一边都是改文案，属行为改动。`titleSpacing`（MD3 三节分别 16 / 16 / 12dp）按约束 ⑥ 点名「只在 MD3 侧生效」。卡片选型沿用第 2 对立的规矩「谁合并前用什么就继续用什么」：MD3 分支走 `AppCard`（`Surface(shapes.large, surfaceContainer)`）、Miuix 分支走 `AppPaddedCard`（库 `Card` + 自带 12dp 水平边距）—— 一节之内两个都用上，各走各的主题分支。
7. **列表度量三个值一起给，别让屏幕自己拼**：`appStatsListMetrics()` 返回 `contentPaddingHorizontal` / `itemSpacing` / `itemPaddingHorizontal`。两版的分工方式相反（MD3 把水平边距放在 `LazyColumn` 的 `contentPadding`、item 自己不加；Miuix `contentPadding` 左右 0、非卡片 item 自己加 20dp、卡片那 12dp 在 `AppSection` 内部），漏拼任何一个都是只有真机看得出来的版式改动。卡片最终的水平内缩两版本来就不同（MD3 20dp / Miuix 12dp），非卡片 item 则都是 20dp —— 这是照抄，不是对齐。

**守卫**：`ScreenParityTest.MergedScreens` += `StatsScreen.kt`（第 7 对）。**这一对特殊：它本来就是那条守卫的起因** —— 测试的规则 3 注释里写着「合并前后都要盯着：现在查 `MiuixStatsScreen`，合并后同一条断言自动落到 `StatsScreen` 上」，而它的文件过滤是 `endsWith("StatsScreen.kt")`，删掉 Miuix 文件后断言自动落到合并版，一行都不用改（这次是当初的设计生效，不是运气）。按 §18 立的流程 `grep -rn "MiuixStats" app/src/test/` —— 命中 3 处全在 `ScreenParityTest` 的散文注释里（讲 2026-09-15 那次修复的来龙去脉），属历史记述，保留。`ImeHandlingTest` 三条清单本对无关：统计页页面内没有输入框（`imeScreens` 不含它），`NavChrome.kt` 只删了调用点与一个 import（`navigationBarsPadding` 有、`imePadding()` 无的口径未动）。`CorruptGuardTest` 本对无关。合并版按 `ScreenParityTest` 四条规则自检通过：调 `rememberStatsUiState(viewModel)` ✓、无 `sumOf {` / `groupBy {` / `count {` / `.sortedByDescending` ✓、无内联统计口径 ✓、无 Miuix 双胞胎 ✓。

**顺手校正一处文档漂移（不是本对造成的）**：两份审查报告的「现状」批注（`docs/audits/2026-08-21-project-review.md` B9 与 `docs/audits/chileme-review.md` 表头 P1-10）从第 1 对起就一直写着「渲染层仍是两套平行实现（16 文件约 7,200 行）」/「双主题渲染层仍两套」—— 批注是在全量对账那天写的，之后六对都没回头改。按「不改写原文、只更新现状批注」的口径（§8）把它们改成 7/8 对的进度。**教训**：现状批注是活文档的一部分，逐对推进时应与 `INDEX.md` 一起更新，不能只在结项时补。

**行数账**：屏幕本体 900 → 409（-491，-55%）；组件层 +195（`AppText` +87 / `AppSurface` +109 / `AppControls` -1 / `AppListRow` ±0）；`NavChrome` -8；`ScreenParityTest` +1。**本对净 -303**，七对累计净 **-652**（+106 / -9 / +43 / -142 / -261 / -186 / -303）。8 对屏幕本体 7,205 → **3,960**；已合并七对的本体 5,105 → 1,860（-64%）；渲染层文件 16 → **9**（只剩设置页一对）。

**剩下的账（比 §20 的 5,200 又上调一点，两头相抵后净高一两百行）**：只剩 Settings 958 + 1,142 = 2,100 行。这一对组件层只长了 +195，比 §20 给「统计 + 设置」两对预留的 +300~400 少一半 —— 好事；但统计页屏幕本体的收缩率是 -55% 而不是假设的 -60%（原因见上：图表代码无处可缩）。按设置 -60%~-65%（840~735 行）、组件层再 +200~300 估：全合完约 **5,300–5,500 行（≈ -24%~-26%）**，比 §20 的 5,200（-28%）高一两百行，比路线图写的「4,500 量级」高约 20%。差距的三块来源没变（组件层 KDoc 580 行不削、同构不同参的控件不强行合并、屏幕本体收缩率有天花板），本对新增第四块：**自绘 / 绘图代码不重复但也不缩减** —— 统计页是第一次撞上，设置页预计还有一处（主题预览色卡）。

**需真机复测（并入「全部 8 对迁完后一次性复测」那一批；用户 2026-09-16 明确：不逐对停下来测）**：两套主题各一遍统计页 —— ① 顶栏「统计」+ 消耗记录入口（MD3 `History` / Miuix `Recent`，primary 色）；② 三张 mini 统计卡：**不可点**（无水波纹）、emoji 20sp、数值 `Hero` 档 ExtraBold、标签 `Label` 档、三种容器色（primary / secondary / error Container）、`weight(1f)` 三等分、卡内 14dp 内衬、圆角 MD3 `shapes.large` / Miuix 库默认 16dp；③ 到期日历行（与首页同一个 `ExpiryCalendarCard`，但这里的水平边距走 `itemPaddingHorizontal`：MD3 0 / Miuix 20dp）；④ 近 7 天柱状图：有消耗的日子柱高按 `maxDaily` 比例 + 600ms 减速动画 + 柱顶数字（`Tag` 档 Bold primary）+ 底部「月/日」弱化标签，**无消耗的日子是 8dp 高的最弱色短柱、无数字**，柱宽 62%、圆角 50、整块高 120dp；⑤ 节标题三处：**MD3 在卡片内**（`titleMedium`+Bold，下面间隔 16 / 16 / 12dp）、**Miuix 是卡片外的 `SmallTitle`**（文案更短：消耗趋势 / 库存分类 / 消耗排行），卡片水平内缩 MD3 20dp / Miuix 12dp；⑥ 库存分类环图：140dp、`Stroke(30f)`、每段留 3° 缝隙、800ms 扫出、中心「总件数」（`Hero` 档 ExtraBold + `Tag` 档弱化小字）、下方图例（10dp 圆点 + 「emoji 分类名」`Meta` 档 Medium + 「N 件 · P%」弱化）、无库存时显示「暂无库存数据」（`Meta` 档弱化）；⑦ 环图 8 色板：两主题各一套（**Miuix 侧第 3、4 色是容器色 / 容器前景色，不是 tertiary / inversePrimary**），随主题种子色与动态取色变化、无硬编码 hex；⑧ 消耗排行榜：名次（`Heading` 档 Bold primary，宽 20dp）+ 18sp emoji + 名称（`Meta` 档 Medium，单行省略）+ 比例条（`primaryContainer`，最小 4%、600ms 动画、高 10dp 圆角 50）+「×N」（`Action` 档 Bold primary）；⑨ **点排行榜某一行能进对应食品详情**（`findItemIdByName` 找不到时整行不可点、无水波纹）；⑩ 排行空态 🍽️「还没有消耗记录」；⑪ 列表项间距 MD3 16dp / Miuix 12dp，底部留白 = 导航栏 + 96dp、顶部 + 4dp，末尾滚得出来；⑫ 切主题后重复 ①–⑪。

## ⚠️ 本轮新发现（此前**任何**文档都没登记）

① **`ARCHITECTURE.md` 教人硬编码 FileProvider authority**（已修，见上 §2）。这不是笔误级问题：`namespace = com.agon.app` 与 `applicationId = com.chileme.pantry` 是刻意分离的，按原文写死 `com.agon.app.fileprovider` 会在拍照时抛 `IllegalArgumentException`，而 `CLAUDE.md` §5 的约束恰好是反的 —— 两份文档给出相反指令。

② **换机直传会产生一批孤儿封面**：`data_extraction_rules.xml` 的 `device-transfer` 放行 `covers/` 却排除 `datastore/` → 封面图传到新机、引用它的库存 JSON 没传，新机首次启动 `cleanupOrphanCovers()` 会把这些图当孤儿删掉。不致命（数据本来就没过去），但白传一轮图片。已登记进 `ARCHITECTURE.md`，**未改行为**（要不要让 `device-transfer` 也排除 `covers/`，或反过来让库存数据也能换机直传，属产品决策）。

③ **`filesDir/snapshots/` 未被任何备份规则排除** → 每日全量库存 JSON（最近 3 份）会随 Android 系统备份进入用户自己的 Google 账号。这是 `chileme-review.md` P0-6 里**唯一站得住**的部分（该条主体论点是误判）。已在 `ARCHITECTURE.md` 登记 + README 隐私段落如实说明；**未改规则**，因为「让库存数据随系统备份走」其实可能是用户想要的（与 ② 是同一个决策的两面）。

④ **`REQUIREMENTS.md` §4 与 F20 自相矛盾**（已修，见上 §5）。「❌ 云同步」与「F20 坚果云同步」同时存在于同一份「唯一事实源」里，任何按 §4 判断需求边界的人都会得出错误结论。

⑤ **`devlog/2026-09-15.md` 的门禁分档描述在当天最后一个提交后就过期了**（已修，见上 §9）。有点讽刺：这正是该项目最在意的那类「文档与代码漂移」，而且漂移窗口只有几小时。

⑥ **`INDEX.md` 与 `devlog/2026-09-15.md` 的单测数不一致**（91 vs 116，已修）；`INDEX.md` 的 2026-08-20 行状态与「当前待办总览」里同一事项的状态互相矛盾（已修）。

⑦ **`chileme-review.md` 的 P3-1 建议与需求边界冲突**（已在该文首批注 + 本轮不改范围）。审查报告建议「做 Glance 桌面小组件，可能是性价比最高的新功能」，但 Widget 在「明确不做」清单里 —— 报告本身没读 §4。提醒：外部审查建议进入 backlog 前，应先过一遍 `REQUIREMENTS.md` §4。

⑧ **`MainActivity` 的批量「移动存放位置」弹窗缺 `decorFitsSystemWindows = false`**（拆 `Common.kt` 时顺路发现，**已在同一天的第二个提交修掉，见 §12**）。MD3 分支 `AlertDialog`（`MainActivity.kt:649`）里有 `OutlinedTextField`（`:676`），但全文件 `DialogProperties` / `decorFitsSystemWindows` 各 0 处；对照 `ManageScreens.kt:311`/`:431`、`SettingsScreen.kt:769` 都写了 `properties = DialogProperties(decorFitsSystemWindows = false)`。按 09-15 那轮 IME 修复自己的结论（`ImeHandlingTest` 的 KDoc 第 3 条：MD3 弹窗是独立浮动窗口，不关这个开关 IME inset 传不进内容），MD3 模式下键盘会盖住这个弹窗的确认按钮。**为什么漏网**：`ImeHandlingTest` 第 3 条的清单只列了 `SettingsScreen.kt` 与 `ManageScreens.kt`，`MainActivity.kt` 不在里面。Miuix 分支不受影响（`WindowDialog` 由库处理 IME，见 `MiuixDialog.kt` 的 KDoc）。属一行修复 + 一行测试清单扩充，但**是行为改动**，故不混进纯搬运提交、单独成一次提交（§12）；**真机复测仍未做**（沙箱无设备），需在手势导航与三键导航两种模式下各验一次。


---

## 📋 待办（本轮之后的真实现状）

> 完整清单以 `devlog/INDEX.md`「当前待办总览」为准；下面是本轮复核后**确认仍然成立**的部分。

### 中优先级
| 事项 | 证据 |
|---|---|
| `photoPath` 存绝对路径 + `File.exists()` 在组合期同步调用 | `FoodAvatar.kt:43`、`EditFoodScreen.kt:284`（换机/清数据后封面悬空；列表滚动每帧 syscall） |
| `nutstorePasswordFlow` 仍把明文推进 StateFlow 常驻内存 | 09-15 只修了「加密失败不再静默」，明文常驻未改 |
| MIUIX 缺配色入口（KDoc 已改正，功能仍缺） | `MiuixSettingsScreen` 无 `AppPalette` 选择器 —— **用户指示暂缓** |
| 剩余统计口径（「本周」文案 / TOP5 按单位 / 分类占比只算库存） | `StatsScreen.kt:122`、`StatsState.kt:69` —— **用户指示暂缓** |
| ~~批量「移动存放位置」弹窗（MD3 分支）缺 `decorFitsSystemWindows = false`~~ | ✅ 本轮已修（见 §12）+ ✅ **用户真机复测通过（2026-09-16）**；`ImeHandlingTest` 第 2 条的「数量差」代理指标已换成直接检查导航栏段 |

### 低优先级 / 一行改动
| 事项 | 证据 |
|---|---|
| ~~detekt 由 report 切 block~~ | ✅ 本轮已切（见 §14）—— 但前提是先修掉「detekt 一直在空转」：`--config` 少了 `--build-upon-default-config`，规则一条都没激活；真实清单 73 条，2 条改代码、71 条体量指标显式关闭后归零才切 |
| ktlint 全量规则集 | `.editorconfig` 现只开 6 条；需专门一轮 `ktlint --format` 并单独 review 那份 diff |
| 详情页「吃掉一份」无撤销 | `FoodDetailState.kt:48-50`（列表页减号反而有撤销） |
| 归档上限 `take(200)` 静默丢最老记录 → 需独立计数器 + UI 提示 | `FoodRepository.kt:487`、`:630` |
| 恢复路径（导入/快照/云端）仍无集成测试 | 现为静态断言 + 真机确认 |
| 三键导航下 IME 未单独复测 | 09-15 真机确认只覆盖手势导航 |
| `ObsoleteSdkInt` 3 处 | `SettingsScreen.kt:275`/`:286`、`Theme.kt:99`（minSdk 26 < 31，检查本身必要） |
| README / 应用商店截图缺失 | B6/B7 的遗留部分 |
| CI 里 `actions/*@v4` 的 Node 20 弃用告警 | `upload-artifact@v4` 8 处、`cache@v4` 1 处、`gradle/actions/setup-gradle@v4` 3 处（`checkout`/`setup-java` 已 v5）；**run 35060113017 的 annotations 已逐个点名这三个**（被强制跑在 Node 24 上）；升级前需确认上游已发对应大版本 |

### 结构性（第二批未做 + 第三批全部）
- 错误模型统一（`Result` + `Channel<UiEvent>` + 全局异常处理）—— 实测 `Channel<`/`UiEvent` 全项目 0 处
- ~~`MainActivity` 拆分（1,123 行）~~ ✅ 本轮已拆完（→ 6 个文件，见 §13）；~~`Common.kt`（983 行）~~ ✅ 本轮已拆（见 §11）；**只剩第三批（结构性），方案见「🧭 拆分路线图」**
- 凭据拆库 + 备份规则调整（与上面 ②③ 是同一个决策）
- App 级组件层 + 页面去重（8 对渲染层约 7,200 行）／派生数据下沉 VM + `WhileSubscribed`／Repository 拆分 + `Clock` 注入 + 轻量 DI（仍无 `Application` 类）／字符串资源化（`strings.xml` 只有 `app_name`）+ 无障碍补全（`contentDescription = null` 61 处、`semantics` 7 处）／数据健康检查页 + 诊断包 + 许可清单
- CI 加固：`paths-ignore`、供应链（`verification-metadata.xml` / `dependency-review-action` / `gitleaks` / `zizmor`）、APK 体积基线、`bundleRelease`(AAB)、`versionCode` 不再用 `github.run_number`（`release.yml:112`）

### 需用户决策
- `targetSdk 36 → 37`（`OldTargetApi`；会触发边到边强制等新一档行为变更，需实机回归，未上架 Play 无合规压力）
- Miuix `0.9.4-rc01` → 稳定版（等上游发版；升级步骤见 `docs/MIUIX_UPGRADE.md`）
- 备份规则的两面决策（②③）：让库存数据随系统备份/换机直传走，还是把 `snapshots/`、`covers/` 一并排除
- Splash 图标发糊（`themes.xml` 用 `@mipmap/ic_launcher`，自适应图标会被系统再套一层遮罩）
- 统计图表 semantics（TalkBack 读不出数据）
- MD3 medium/high 对比度档位、日历周视图密度、自动同步「仅 Wi-Fi」、删除分类后孤儿记录批量重新归类

---

## 🧭 拆分路线图（第二批：`MainActivity.kt`；第三批：结构性）

### 第二批：`MainActivity.kt`（1,115 行）→ 6 个文件

**现状（实测行号）**：`READY_TIMEOUT_MS`:167 · `EmphasizedDecelerate`/`EmphasizedAccelerate`:170-171 · `class MainActivity`:173-244 · `TabSpec`:245 · `MainApp`:254-715（462 行）· 4 个批量操作栏:717-883 · `MainTabs`/`MiuixMainTabs`:884-903 · `MainTabsPager`:905-989 · 4 套导航栏:991-1115。

`MainApp` 内部还能再切三段：状态与副作用 254-385（`backStack` / `pagerState` / 多选 / 3 个 Snackbar collector / `nestedScroll` 连接）、`Scaffold` 外壳 387-575（`bottomBar` 390-422、FAB 423-455、`NavDisplay` 462-558 含 8 个 `entry<AppRoute.*>`、Snackbar 覆盖层 559-575）、弹窗 577-715（批量移动存放位置，MD3 `AlertDialog` + `MiuixDialog` 双实现，139 行）。

| 新文件 | 内容 | 约行数 |
|---|---|---|
| `MainActivity.kt` | `READY_TIMEOUT_MS` + `class MainActivity`（深浅色/风格分流、启动放行超时、splash、`CompositionLocalProvider`） | ~120 |
| `MainApp.kt` | `EmphasizedDecelerate`/`Accelerate`（只有 FAB 动画用，**可继续 private**）+ `MainApp`（状态 + Scaffold 外壳 + Snackbar 覆盖层） | ~300 |
| `AppNavGraph.kt` | `NavDisplay` 的 8 个 `entry<AppRoute.*>` 抽成一个 `AppNavHost(backStack, …)` | ~140 |
| `AppDialogs.kt` | 批量移动存放位置弹窗（双实现；⚠️⑧ 的键盘避让已于本轮修复，搬运时把 `ImeHandlingTest` 第 3 条清单里的路径同步改成 `AppDialogs.kt`） | ~175 |
| `BatchBars.kt` | `BatchActionBar` / `BatchCancelButton` / `BatchMoveLocationButton` / `BatchArchiveButton` | ~200 |
| `NavChrome.kt` | `TabSpec` / `MainTabs` / `MiuixMainTabs` / `MainTabsPager` / `MiuixBottomNav` / `MiuixFloatingNav` / `Md3BottomNav` / `FloatingPillNav` | ~285 |

（含各文件的 `package` / import / 文件头注释开销，故合计略大于 1,115。）

**与 `Common.kt` 那一轮的三条关键差别（必须先认账）**：

1. **不再是纯搬运：11 个顶层 `private` 声明必须放宽成 `internal`**（`TabSpec`、`MainTabs`、`MiuixMainTabs`、`MainTabsPager`、4 个批量栏、4 套导航栏）。Kotlin 的顶层 `private` 是**文件级**作用域，`MainApp` 一搬到别的文件就看不见它们。`internal` = 模块内可见（app 模块没有第二个消费方，R8 照常裁剪），**不是**公开 API 变更；但代价是 `detekt` 的 `UnusedPrivateMember` 从此不再覆盖它们（用死代码风险换可读性，值得，需在文件头写明理由）。`Common.kt` 那轮不需要这一步，因为它的 18 个声明本来就是 public。
2. **`ImeHandlingTest` 必须同步改，否则一定红**——这是本轮唯一会「静默破坏测试」的点。`app/src/test/.../ImeHandlingTest.kt:56` 只读 `com/agon/app/MainActivity.kt`，断言 `.imePadding()` ≥3、`.navigationBarsPadding()` ≥3、且前者**严格少于**后者（A 方案：底栏不跟键盘抬升）。实测现状 ime=3（`:566` Snackbar、`:731`/`:754` 两条批量栏）、nav=4（`:563`、`:730`、`:753`、`:1054` 悬浮导航）。拆分后 Snackbar 归 `MainApp.kt`、批量栏归 `BatchBars.kt`、`:1054` 归 `NavChrome.kt`，`MainActivity.kt` 自己变成 0/0 → 断言失败。改法：第 2 条测试从「读一个文件」改成「读一组文件求和」（文件清单 + 每处期望值写进注释），三条不变量原样保留。
3. ~~⚠️⑧ 那处 IME 缺陷顺手修~~ **已提前做掉**（本轮第二个提交，见 §12）：`MainActivity.kt` 已补 `DialogProperties(decorFitsSystemWindows = false)` + `imePadding()`，并已进 `ImeHandlingTest` 第 3 条清单。第二批把弹窗搬去 `AppDialogs.kt` 时，只需把清单里的路径跟着改。

**执行顺序（每步一个提交，便于 CI 二分定位）**：① `BatchBars.kt` + `NavChrome.kt`（叶子，无人依赖它们，风险最低）**+ 同批改 `ImeHandlingTest` 第 2 条**（第一次拆分就会让它红）→ ② `AppDialogs.kt`（纯搬运 + 改 `ImeHandlingTest` 第 3 条的路径）→ ③ `AppNavGraph.kt` → ④ `MainApp.kt`（最后，`MainActivity.kt` 只剩 Activity 本体）。（原第 ③ 步「补 `DialogProperties` + 扩测试清单」已在本轮 §12 完成。）**验收**：`assembleDebug` 绿；116 例单测全绿（含改过的 `ImeHandlingTest`）；ktlint 6 条零违规；`git diff -w` 里除 `private→internal`、文件头注释与那一行 `DialogProperties` 外，没有实现级改动。

> **✅ 执行结果（2026-09-16，详见 §13）**：四个搬运提交 + 一个修 CI 的提交，`MainActivity.kt` 1,123 行 → 6 个文件合计 1,371 行；CI run 35063573632 与 35064714882 三 job 全绿。实际顺序把 `MainApp.kt` 提到第二步（先落定外层，内层块的比对基准就只有一个文件）；可见性放宽实测只需 8 个而非计划的 11 个（3 个批量按钮与 `MiuixMainTabs` 没有跨文件消费方，保持 private）；「`rememberNavBackStack` + `NavDisplay` 只在 `MainApp` 一处」这条硬约定按下面的表述改口：**状态源仍在 `MainApp`，唯一的 `NavDisplay` 移到 `AppNavGraph.kt`、由 `MainApp` 的内容槽位调用一次**，约束实质（一个 nav host、backStack 不下传给屏幕）未变。

**硬约定（拆分时不得破坏）**：`rememberNavBackStack` + `NavDisplay` 只在 `MainApp` 一处；二级页一律走回调（`onTabs` / `navigate`），**不得**把 `backStack` 往下传给屏幕；外层 `Scaffold` 的 `contentPadding` 刻意不消费——`@Suppress("UnusedMaterial3ScaffoldPaddingParameter")`（`:252`）与其下方那段解释注释必须跟着 `MainApp` 一起搬；`nestedScroll` 的 ±8f 阈值（`:274`/`:275`）建议提成具名常量（如 `CHROME_HIDE_THRESHOLD_PX`），但那是**唯一一处「顺手改善」**，必须单独提交或在 PR 描述里点名，不许混进搬运提交。

### 第三批：结构性（不是拆文件，是把边界变成可执行的约束）

排序原则：**先能拦、再去重、后补体验**。

| # | 事项 | 前置 | 证据（本轮实测） | 验收 / 风险 |
|---|---|---|---|---|
| 1 | ~~**detekt 由 report 切 block**~~ ✅ **已完成（§14）** | 无（一行改动） | ~~基线双零~~ **实测：所谓「双零」是 detekt 空转造成的假象** —— 修好后真实清单 73 条 | ✅ `DETEKT_MODE=block`；另加 `detekt_selftest` 防空转 |
| 2 | ~~**开启 detekt 复杂度规则**~~ ✅ **已完成（§14，按实测清单收敛）** | #1 + 第二批拆完 | `detekt.yml` 的 `complexity` 规则集 `active: false`；`chileme-review.md` P2-1 点名 `Common.kt`（已拆）/`MainActivity.kt`/`SettingsScreen.kt`（1,142 行，现为最大文件） | 先 report 跑出清单 → 逐条修或写明豁免 → 再切 block |
| 3 | **双主题去重 + App 级组件层**（最大的一项）🟡 **进行中：第 7/8 对已合并（消耗记录页 §15、归档页 §16、食品详情页 §17、首页 §18、食品列表页 §19、管理页 §20、统计页 §21），渲染层文件 16 → 9，8 对屏幕本体 7,205 → 3,960 行；七对累计净 -652，`AppNavGraph` 已零主题分支，`NavChrome` 只剩设置页一处分支** | #2 的清单 | 16 个渲染层文件 7,205 行 = MD3 侧 3,741 + Miuix 侧 3,464；8 对屏幕逐页两套 | 见下方分步；**会撞红两个静态守卫** |
| 4 | **错误模型统一** | 无 | `Channel<` / `UiEvent` 全项目 0 处；错误靠散落的 `String?` + Snackbar 文案 | `Result` + `Channel<UiEvent>` + 一处全局收集 |
| 5 | **Repository 拆分 + `Clock` 注入 + 轻量 DI** | #4（错误模型会改 Repository 签名） | `FoodRepository.kt` 950 行；`Application` 类 0 个；`LocalDate.now()` 直接散在业务里 | ⚠️ **会撞两个源读测试**：`CorruptGuardTest` 读 `FoodRepository.kt`（4 处）、`AppViewModel.kt`（2 处）、`SecureStore.kt`、`HomeScreen.kt`/`MiuixHomeScreen.kt`；`CompactConsumptionTest` 读 `FoodRepository.kt`、`ConsumptionLogScreen.kt`/`MiuixConsumptionLogScreen.kt`。且 `CorruptGuardTest.functionBody()` 是**按 4 空格缩进截函数体**的——签名一搬家就 `assertTrue("源码里找不到 …")` 直接失败。拆分与测试改动必须同一个提交 |
| 6 | **派生数据下沉 VM + `WhileSubscribed`** | #5 | 8 个 `*State.kt` 已是纯计算容器（好基础），但屏幕仍在组合期筛选/排序 | `stateIn(SharingStarted.WhileSubscribed(5_000))` |
| 7 | **字符串资源化 + 无障碍补全** | #3（去重后再做，否则同样的文案要改两遍） | `strings.xml` 只有 `app_name` 1 条；`contentDescription = null` 61 处、`semantics` 7 处 | 文案入 `strings.xml`；图标按钮补 `contentDescription` |
| 8 | **数据健康检查页 + 诊断包 + 许可清单** | #4/#5 | 损坏留档已按 key 粒度（`filesDir/corrupt/`），但用户出路只有「导入备份 / 放弃」 | 新页面读 `corruptedKeys` + 一键导出诊断包 |
| 9 | **CI 加固**（可与上面并行） | 无 | `paths-ignore` 未配；无 `verification-metadata.xml` / `dependency-review-action` / `gitleaks` / `zizmor`；无 APK 体积基线；`versionCode` 仍用 `github.run_number`（`release.yml:112`）；`upload-artifact@v4`（8 处）/`cache@v4`（1 处）/`gradle/actions/setup-gradle@v4`（3 处）仍是 v4，而 `checkout`/`setup-java` 已在 v5（Node 20 弃用告警） | 逐项独立小 PR；升 actions 前先确认上游已发对应大版本 |

**#3 的分步做法**（唯一「动架构」的一项，不能一把梭）：
1. 先建 **App 级组件层**：`ui/components/app/` 下 `AppScaffold` / `AppTopBar` / `AppCard` / `AppListRow` / `AppTextField` / `AppDialog` / `AppSnackbar` / `AppSegmentedControl` / `AppProgress`，内部用 `LocalThemeStyle` 分流——**直接照抄 `Badges.kt` 里 `StatusBadge` 的现成结构**（本轮把它单独成文件，就是给这一步当模板）。
2. 逐屏替换，**一次一对、一对一个提交**，顺序按体量从小到大：~~`ConsumptionLog`（190）~~ ✅ **已合并（§15，屏幕本体 396→113 行）**→ ~~`Archive`（282）~~ ✅ **已合并（§16，屏幕本体 566→186 行）**→ ~~`FoodDetail`（343）~~ ✅ **已合并（§17，屏幕本体 697→270 行）**→ ~~`Home`（401）~~ ✅ **已合并（§18，824→333 行）**→ ~~`FoodList`（411）~~ ✅ **已合并（§19，830→267 行）**→ ~~`Manage`（434）~~ ✅ **已合并（§20，892→282 行，一个文件三个页面）**→ ~~`Stats`（445）~~ ✅ **已合并（§21，900→409 行；图表自绘代码不重复也无处可缩，故降幅 -55% 低于前六对）**→ `Settings`（958/1,142，最大，留最后）。每步跑 `ScreenParityTest`（原名 `MiuixParityTest`，第 1 对合并时已扩围+改名，见 §15）。
3. ⚠️ **两个静态守卫会被这一步撞红，必须同步迁移**：
   - ✅ **已迁移（§15）**：`MiuixParityTest` 用 `screensDir().listFiles { it.name.startsWith("Miuix") }` 枚举，要求每个 `Miuix*.kt` 都调 `remember*UiState(`、且不含 `sumOf {`/`groupBy {`/`count {`/`.sortedByDescending`。去重后 `MiuixXxxScreen.kt` 被删，测试会因 `assertTrue("未找到任何 Miuix*Screen.kt，路径假设失效")` 直接失败 → 每删一个 Miuix 文件都要同步改这个假设；**最终形态应改成「每个屏幕文件都必须调 `remember*UiState`」，不再按文件名前缀分流**。
   - 🟡 **部分迁移（§16，做法与本条设想不同）**：`ImeHandlingTest` 第 1 条点名 5 个屏幕文件必须含 `imePadding()`。**没有**让 `AppScaffold` 统一吃掉 inset（那对没有输入框的屏幕是行为改动），而是保留「屏幕自己传 `modifier = Modifier.imePadding()`」，守卫清单从 5 项缩到 3 项（归档页 §16、食品列表页 §19 各删一条；删列表页那条时差点写出重复条目，见 §19）。**第 3 条（`decorFitsSystemWindows` 按文件点名）在第 6 对也迁了一次**：管理页那两个输入弹窗搬进组件层，清单里 `ManageScreens.kt` 改成 `ui/components/app/AppFormDialog.kt`（见 §20） —— 合并后 `imePadding()` 只有一份，一个条目覆盖两套主题；每合并一对就删掉对应的 `Miuix*Screen.kt` 条目（否则 `read()` 返回 null 会误报）。归档页那一对已按此办理，列表页那一对同样处理。
4. **验收**：16 个渲染层文件 → 8 个（+ `app/` 组件层若干小文件）；总行数从 7,205 降到 4,500 量级；两个守卫改写后仍全绿；真机两套主题各过一遍 8 屏。

**为什么是这个顺序**：#1/#2 先把「已经为零的基线」变成拦截，之后任何一步的回归都会被 CI 当场抓住（否则 #3 那种 7,205 行的改动没有安全网）；#3 收益最大也最险，必须建立在组件层模板 + 守卫改写之上；#5 排在 #4 之后，是因为错误模型会改 Repository 的返回类型——先拆文件再改签名等于同一批代码搬两次。

---

## 💡 决策记录

- **为什么历史审查报告只加批注、不改写原文**：这三份报告是「当时的判断」的记录，价值在于可追溯（包括判断错的那条）。直接改写会让后来人无法理解后续修复的动因，也会掩盖 P0-6 这类误判——而误判本身是有信息量的（它说明「读文件时凭印象下结论」的风险）。所以一律文首/标题加状态批注，原文保留。
- **为什么把 F22–F43 补进 `REQUIREMENTS.md` 而不是另开一份「已实现功能清单」**：该文件自我定位为「功能范围的唯一事实源」，且规定「新需求必须先在此登记再开发」。已经上线的 22 项功能不在里面，等于这份事实源缺了三分之二，任何人（含 AI）拿它判断需求边界都会错。补登记比另开文件更能恢复它的作用。
- **为什么只改 KDoc 而不补 MIUIX 配色入口**：补入口需要 `MiuixRootTheme` 打通种子色通道（Miuix `ThemeController` 的 `keyColor`），属功能开发且用户已明确暂缓；而错误的「功能对等」声明是**当下就在误导人**的，改注释零风险。
- **为什么 ②③ 只登记不改行为**：两者是同一个产品决策的两面（要不要让数据随系统通道迁移），任一侧单独改都可能与用户意图相反。按 `CLAUDE.md` §4.2「超出范围的需求先与用户沟通确认」处理。
- **为什么本轮不跑构建就交付**：改动是 12 份 Markdown + 1 处 KDoc 注释，无编译语义变化；沙箱既无 JDK/SDK，ktlint 二进制又被网络策略拦截（`curl: (35) SSL_ERROR_SYSCALL`）。按 `CLAUDE.md` §4.4 以 CI 为验收，本轮 CI 需确认三点：`assembleDebug` 仍绿、ktlint 仍零违规（KDoc 改动是唯一被检文件）、116 例单测不受影响（未动任何测试与被测代码）。
- **为什么拆分保持同包、不改包名**：改成 `ui/components/badges/` 之类的子包会让 27 个调用方全部要加 import，把一次「零风险搬运」变成 27 个文件的 diff，review 成本和回归面都翻几倍。同包拆分能让 `git diff` 只剩「删一个文件 + 加八个文件」，也让 CI 成为唯一的验收点（本地无 JDK）。
- **为什么不顺手把新文件的 import 排序**：`.editorconfig` 明确写了「全量格式规则留待专门一轮 `ktlint --format` 一次性刷，单独 review 那份 diff」，`import-ordering` 也在未启用之列。在搬运提交里混入排序，会让「逐字节相同」这条最强证据失效——本轮能宣称纯搬运，正是因为校验脚本可以拿声明块与原文做**逐字节**比对。
- **为什么机械拆分必须人工过一遍操作符 import**：`getValue` / `setValue` 这类委托操作符在源码里不以名字出现，按符号名匹配的脚本必然漏；漏掉的后果不是警告而是编译失败。凡是「按文本匹配搬 import」的拆分，都要额外按「文件内是否有 `by` 委托 / 是否有 `var … by`」补一遍——这条已写进本日志，供第二批（`MainActivity.kt` 里同样有 `by rememberSaveable`）直接复用。
- **为什么本轮只拆 `Common.kt`、不动 `MainActivity.kt`**：`Common.kt` 的 18 个声明全是 public，拆分是纯搬运，可以用逐字节校验证明「没改任何东西」；`MainActivity.kt` 有 11 个顶层 `private`（跨文件必须放宽成 `internal`）且被 `ImeHandlingTest` 按路径读取，一定会产生语义与测试改动。两种风险等级的工作混在一个 PR 里，等于把可证明安全的那一半也拖进「需要人肉复核」的档位。
- **为什么历史 devlog 与审查报告正文里的 `Common.kt` 引用不改**：与本轮文档对账同一条原则——历史文件是「当时的判断」的记录，改了就无法追溯（`chileme-review.md` 的 P0-6 误判正是靠原文才看得清）。只更新「活的」位点：`CLAUDE.md`、`ARCHITECTURE.md`、`DESIGN_SPEC.md`、`MIUIX_UPGRADE.md`、`INDEX.md`、审查报告文首的现状批注表。
- **为什么 ⚠️⑧ 单独成一个提交、不并进拆分提交**：它是**行为改动**（弹窗 inset 处理），而拆分提交的全部价值在于「可以用逐字节比对证明什么都没改」。两者混在一起，可证明的那一半也要靠人肉复核了。分开之后：拆分提交若出问题，回滚不影响修复；修复若真机复测出岔子，回滚不影响拆分。
- **为什么顺手改写了 `ImeHandlingTest` 第 2 条的断言方式**：原断言是「imePadding 数 < navigationBarsPadding 数」，用两个计数的大小关系间接表达「底栏不随键盘抬升」。补上弹窗那一处后两边都是 4，它会**把正确的修复判成违规**——这不是阈值调一下就行，而是指标本身选错了：任何新增浮层都会让它翻脸，而它想约束的其实只有 4 套底栏实现。改成直接检查那段代码后，约束与被约束物一一对应，第二批把底栏搬去 `NavChrome.kt` 时也只是换个读取范围。
- **为什么第 ④ 步的形参名照抄被捕获的局部名（`navigate`/`popRoute`/`selectTab`…）**：这样搬过去的 101 行代码**零改写**，只需要反缩进，`git diff -w` 里这一段是空的 —— 「纯搬运」从一句声明变成一条可执行的断言。代价是 `AppNavHost` 的形参名不够「组件化」（`navigate` 而不是 `onNavigate`），但换名就意味着块内 20 余处调用点全要改，每一处都是一个可能改错的地方。命名口味让位于可验证性。
- **为什么把「CI 红一次」原样记进日志而不是悄悄修掉**：两处根因（推断类型暴露 private 类型、脚本写出的非法转义）都不是本轮特有的手误，而是**「本地无 JDK + 用脚本搬代码」这个工作方式**的必然产物，下一批还会再遇到。把症状、为什么静态门拦不住、以及补上的审计脚本一起写下来，比留一个「已修」的干净历史有用得多。
- **为什么文档同步单独成一个提交、且排在最后**：文件归属与行号在结构定稿前会变（本轮 `MainApp.kt` 就经历了 593 → 443 → 335 行三次），提前改文档等于改两三遍，还会让搬运提交的 diff 里混进 Markdown。等 CI 绿了、结构定了，一次改到位。
- **为什么历史审查报告正文里的 `MainActivity.kt:923` 只在横幅批注里改址**：与 §8 同一条原则 —— 正文是「当时的判断」的快照，改了就无法追溯；但**横幅是活的导航**，读者会照着它去翻代码，指错文件比不指更糟。所以只在横幅里写「现址 `NavChrome.kt:132`（原 `MainActivity.kt:923`）」，新旧都留。
