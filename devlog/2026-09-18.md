# 2026-09-18 开发日志

> 本轮性质：**结构性重构第一轮（路线图 #4a + #4b + #4c）**。上一轮（[2026-09-17](2026-09-17.md) §12）做完文档事实核查，
> 用户看完待办清单后指示「要改。规划一下 4 5 把这两个做了」⇒ 本轮先把 #4/#5 拆成可执行阶段写进
> [`ROADMAP`](../docs/ROADMAP.md)，再落地 #4 的第一阶段 **4a**（一次性 UI 事件的建模方式）。
> 顺带收掉上一轮遗留的两件工具/事实问题：词法预检器入库（§4）、核查第 14 处（§5）。
> 沙箱依旧无 JDK/SDK，验收以 CI 为唯一编译与单测口径（`CLAUDE.md` §4.4）；本轮 CI 跑了 **3** 次，**3 次全绿**
> （4a 那次 4 分 20 秒）。⚠️ 第 3 次（4b）跑完的那一刻 **GitHub 令牌失效**（当晚已恢复：`5178cee` 与 4c 的两个提交都推送成功、CI 正常触发，逐 job 结论也取全了）⇒ 那段时间 job 级结论没取全、提交一度推不出去，
> 详见「验收依据」末段。
> ⚠️ 「行为保持不变」是本轮的验收判据，但撤销/提示这类交互**仍需真机复测**，口径见文末「待真机复测」。

---

## ✅ 已完成

### 1. #4 / #5 的分阶段计划写进 ROADMAP（用户批准的「规划一下」）

- **#4 错误模型统一** 拆成 4a（一次性事件建模）→ 4b（收集端重复逻辑 + 文案落位断言）→ 4c（`(Boolean, String)` 回调改 `Result`/`sealed`）。
- **#5 Repository 拆分 + `Clock` 注入 + 轻量 DI** 拆成 5a（`Application` 子类 + 手写容器）→ 5b（时钟注入 + 定点时钟测试）→ 5c（按域纯搬运拆分，一域一提交）。
- **顺序 #4 先于 #5**：错误模型会改 `FoodRepository` 的返回类型，先拆再改等于同一批代码搬两次。
- 每项都带**证据行**（逐条实测的数字，不是印象）、验收判据、风险与注意事项。其中 3 条证据行是上一轮核查后重写的
  （核查第 11/12/13 处），因为原写法把「机制隐患」说成了「正在发生的 bug」（详见 §6 的日期纠正）。

### 2. #4a：一次性 UI 事件从「可空 StateFlow + 手工 consume」→ `UiEvent` + `Channel`

**改之前是什么样**：4 处「看过一次就该消失」的提示，全都建成 `MutableStateFlow<X?>(null)` + 一个 `consumeXxx()` 手工清空 ——
`_undoRequest`（列表页减号 → 撤销消耗）、`_deletedConsumption`（消耗记录页删除 → 撤销）、
`_restoredArchivedEvent`（搜索里恢复归档 → 撤销）、`_autoSyncMessage`（启动自动同步成功提示）。

`StateFlow` 会向**新订阅者重放最后一个值**，所以「看过就撕」全靠每个收集点记得调 `consume`。
今天 4/4 都记得 ⇒ **并没有正在发生的重放 bug**。但那是纪律不是机制：新加第 5 个事件、或某个收集点漏调一次，
症状是「旋屏后同一条提示又弹一遍」，非常难查。改成 `Channel` 后**接收即出队**，`consume` 函数与可空状态一起消失。

**改动清单**（提交 `f4cd9a4`，7 个文件 +287/−99）：

| 文件 | 改了什么 |
| --- | --- |
| `viewmodel/UiEvent.kt`（新增 63 行） | `sealed interface UiEvent`（4 类，**载荷是数据不是回调**）+ `enum UiSurface`（AppShell / Home / ConsumptionLog）；类注释写清它替代了哪 4 个符号、以及为什么分三条队列 |
| `viewmodel/AppViewModel.kt`（598 → 592 行） | 3 条 `Channel<UiEvent>(Channel.BUFFERED)` + **一个** `private suspend fun emit()` 按 `surface` 分流；对外 3 个 `Flow`（`receiveAsFlow()`）。删掉 3 个嵌套数据类、4 个 `_x` 状态、4 个 `consumeXxx()`、`autoSyncMessage` 整块 |
| `MainApp.kt` | 原先**两条** `LaunchedEffect`（各收一个可空流 + 各自 consume）合成**一条**收 `appShellUiEvents`，`when` 穷尽 sealed |
| `ui/screens/ConsumptionLogScreen.kt` | 改收 `consumptionLogUiEvents`；`LaunchedEffect(snackbar)` 的 key 保持不变（切主题要停旧协程） |
| `ui/screens/HomeScreen.kt` | 改收 `homeUiEvents`；key 从 `state.autoSyncMessage` 换成 `snackbar` |
| `ui/screens/HomeScreenState.kt` | 去掉 `autoSyncMessage` 字段、`consumeAutoSyncMessage()`、`remember` 的 key 与工厂参数（共 5 处） |
| `viewmodel/UiEventTest.kt`（新增 127 行） | 6 例守卫，见 §3 |

**实施时推翻了计划里的两处设想**（都记进 ROADMAP #4「4a 落地结果」，不是照原计划硬做）：

1. **落点是三个不是两个**。规划时只读了 `MainApp` 与消耗记录页，**漏了首页 `AppScaffold` 自己的宿主** ——
   自动同步提示一直落在那里。这是"读得不够就下手"的典型，动手写代码时才撞见。
2. **「一个 `Channel` + 一份收集实现挂多处」不成立**。`Channel` 是**单接收方**语义：多个收集协程会互相抢事件
   （谁先 `receive` 谁拿到，结果不确定）。所以按落点分了**三条队列** —— 发送端仍只有一处（`emit()`），
   每条队列恰好一个接收方，接收方生命周期与该宿主一致。

   也考虑过「干脆统一到主壳一处收集」，被否掉的理由比落位更硬：`showSnackbar` 会**挂起到提示关闭为止**，
   若用户已经离开首页，就往那个**没有渲染**的宿主发提示 ⇒ 收集协程被永久堵住，之后所有撤销条都不再出现。
   队列化正好复现旧行为：事件在队列里等着，用户回到那一页才弹。

**为什么是 `Channel(BUFFERED)` 而不是 `SharedFlow(replay=0)`**：`MainActivity` 的主题分支会让 `MainApp` 整棵重组
（切主题 = 旧组合销毁、新组合建立），`replay=0` 的 SharedFlow 会在这个瞬间**把事件直接丢掉**；
`Channel` 是排队，切换完成后照弹。

**唯一的逻辑删除**：`undoDeleteConsumption` 里那句「若待处理的撤销事件正是这条记录就清空它」。
收集端一直是「先 consume 再弹条」，弹条期间那个状态早已是 `null`，故那句**永远不成立**；
改用 `Channel` 后也没有「待处理的事件」可清 ⇒ 删掉，理由写在代码注释里。
除此之外 4 条提示的**文案、落位、主题分流逻辑逐句未改**（这是"纯搬运"的判据）。

### 3. 新增守卫 `UiEventTest`（单测 121 → **127** 例）

6 例钉住三件事：① 每个事件的 `surface` 分流是否正确（`UndoConsumption`/`UndoRestoreArchived` → AppShell、
`Notice` → Home、`UndoDeleteConsumption` → ConsumptionLog）；② 各事件的载荷字段齐全；
③ **事件里不许出现函数类型**（一次性事件必须带数据，不许带回调 —— 回调会捕获发出时那个界面的宿主，
跨主题或跨页面就失效）。

第 ③ 条只能读源码文本断言：本仓**没有 `kotlin-reflect`**，用不了 `KClass.primaryConstructor` / `sealedSubclasses`。
源码文本断言是本仓既有惯例（`CorruptGuardTest` 就这么做，它钉住 `AppViewModel` 里 3 个函数的存在与函数体片段）。
本轮特意核过：按行剥注释的过滤方式在 `UiEvent.kt` 上安全 —— 该文件没有 `"*/*"` 这类会骗过行级过滤的字面量，
而它唯一提到 `-> Unit` 的地方在 `* ` 开头的 KDoc 行里，会被过滤掉（否则第 ③ 条会自己咬自己）。

### 4. `tools/kt-lexcheck.py` 入库（提交 `dec3a0e`）—— 把"本地能抓的错"从临时脚本变成工具

沙箱没有 JDK，**任何 Kotlin 语法/词法错误都只能等 CI（≈4 分钟）才发现**。上一轮临时写了个预检脚本，本轮修好后入库：

- **三条判据**：① 括号/引号/模板配平（能识别注释与嵌套块注释、字符串模板表达式）；② ktlint 实际启用的那 5 条词法规则
  （`no-wildcard-imports` / `final-newline` / `no-trailing-spaces` / `no-consecutive-blank-lines` / `no-blank-line-before-rbrace`）
  + `string-template`；③ **未使用的 import**（这一条是唯一的防线，因为 §5 说明 CI 上根本没有门禁）。
- **`--selftest`：12 个内建自检对照**。改这个脚本本身必须先跑自检 —— 一个"报告全绿"的检查器如果抓不到已知缺陷，
  比没有检查器更危险。
- **两个假对照被抓出来**：原稿里有两条 selftest 条目是"好文件"的复制品，看起来是 12 个对照，实际**不隔离任何缺陷**。
  换成真变异（模板唯一使用点、`${i}_101`/`${n}天`/`${d/30}个月` 这类必须加花括号的形状）之后做了**反证**：
  故意改坏 `$name` 保留逻辑 ⇒ selftest 变红 `exit 1`；还原 ⇒ `exit 0`。
- **两个自身 bug**：① 遇到 `${` 不切换模式（改成显式栈）；② 字符串模板表达式的内容**必须保留**在 code-only 文本里，
  否则模板里用到的符号会被误判成未使用。
- **两类误报及修法**：① `${d}天` 型 —— 后一个字符是 Unicode 单词字符时 `$d` **不是**冗余花括号（ktlint 同判）；
  ② `"$name"` 型简单模板必须保留标识符，否则真用到的 import（如 `$CLOUD_BACKUP_KEEP`）会被误报未使用。
- 现状：全仓 **81** 份 `.kt` 扫描，有问题 **0** 份；`--selftest` **12/12**。

### 5. 核查第 14 处：detekt 的 `UnusedImports` 默认是**关**的（提交 `fe0f527`）

上一轮把「ktlint 的 `no-unused-imports` 已弃用 ⇒ 本仓改用 detekt 的 `UnusedImports` 兜底」写进了
`detekt.yml` 与 `.editorconfig`。本轮用 `gh api` 取上游 detekt **v1.23.8** 的 `default-detekt-config.yml`（第 744 行，
不是凭印象）核对：**`UnusedImports: active: false`** —— 官方因误报率高而默认关闭，本仓也没显式打开。
⇒ **未使用的 import 在 CI 上没有任何门禁**，那两处"兜底"的说法是错的（与 09-16 那次「detekt 空转」同一类：
以为开着的门禁其实没开）。同一份默认配置里核了另外三条 `Unused*`（`UnusedPrivateMember` / `UnusedPrivateProperty` /
`UnusedPrivateClass`），那三条声称"保留默认开启"是对的 ⇒ 错的只有这一条。

实测确实漏了一个：`AppBatchMoveDialog.kt` 的 `import androidx.compose.foundation.layout.size`
（09-17 把 `AppDialogs.kt` 搬进组件层时删漏的；全文件除 import 行外再无 `size` 字样，手工核过是真死 import），已删。
两处文字各自改正并写明复核依据。

**没有顺手把这条规则打开**：它默认关闭正因为误报率高，开启前要先单独跑一轮看误报面 —— 那属于门禁调整，
不该混进这次清理。本地由 `tools/kt-lexcheck.py` 的第 3 条判据兜（含自检对照）。

### 6. 守卫交接 + 文档同步 + 日期纠正（提交 `8f75184`）

**（a）守卫交接**（ROADMAP #4 里预告过的那次，同轮完成、不留两套）：
临时守卫「一次性事件必须有 `consume` 配对」→ 正式守卫「**禁止再出现可空 StateFlow 型一次性事件**」。
目标 **0**，出现即 ✗，豁免必须登记且**理由为空也算失败**。附带三道自检：

1. **阳性对照**：内嵌一个已知样本，正则抓不到就说明本项的「0」不可信（脚本头部第 3 条硬约定：零结果必须做阳性对照）；
2. **替代机制在场**：`UiEvent` 的 sealed interface、3 条 `Channel<UiEvent>`、3 个 `receiveAsFlow()` ——
   防止「0 个」是因为整套机制被删了；
3. 现值：可空事件状态 **0** 个、事件型 `consume` 函数 **0** 个（`AppViewModel` 里只剩业务方法 `consumeOne`）。

**（b）数字加粗约定**（被守卫逼出来的）：ROADMAP 里**历史基线值不加粗**（不再被断言）、**现值加粗**（被
`tools/doc-metrics.sh` 的「文档写死的关键数 vs 实测」断言）。起因是 4a 一提交，那条守卫立刻 ✗ 报
「ROADMAP:45 `Channel<` 文档写 0、实测 3」—— **绊线按设计响了，响的是我自己上一轮刚写的那行**。
这正是它存在的意义：证据行会过期，过期就当场报错，而不是等下一个人照着做。

**（c）日期纠正 22 处**：今天已是 2026-09-18（本轮 4 个提交的作者日期都是 09-18），
而我给今天新发现的第 11–14 处核查结果打的标签写的是「2026-09-17」—— 上一轮（核查第 1–10 处、本仓 §12）才是 09-17。
**跨了 UTC 日界没察觉**。19 处随本提交改正，3 处代码注释随 `f4cd9a4` 走；INDEX 待办标题上补了
「#4/#5/#8 三条于 09-18 复核修正」，免得以后分不清哪轮的发现。

**（d）文档同步**：ROADMAP 进度总览第 4 行 ⏳ → 🔶 4a ✅；新增「4a 落地结果」小节（现值 + 两处设想错误 +
为什么必须按落点分 + 纯搬运判据）；**4b 行整行重写**（原写法「新增一份 `UiEventHandler` 挂在每个宿主上」不成立，
理由见 §2 的第 2 点）；INDEX 的 #4 条改成"4a 已做 / 4c 才是真正要收的东西"。

### 7. 本轮我犯的 7 处错（都在本轮内发现并改正）

| # | 错在哪 | 怎么被抓到的 |
| --- | --- | --- |
| 1 | 4b 的设想错两处：落点当两个（实为三个）、以为一条 `Channel` 能挂多个收集协程 | 动手写代码时读首页读出来的；已回写 ROADMAP |
| 2 | **凭记忆写守卫的形状**：以为那段是 `printf` 块，实际是 `row '…' "$(python3 - <<'PY' …)" '…'` | `str.index()` 找不到标题串抛错，**文件一个字节都没改** |
| 3 | 嵌套 heredoc 用了同一个 `PY` 分隔符 ⇒ 外层提前收尾，剩下的内容被 bash 当命令解析 | bash 语法错；改成先写临时文件再拼接 |
| 4 | 把今天的发现标成 09-17（跨 UTC 日界没察觉），22 处 | 提交前查 `git log` 的作者日期 |
| 5 | 批量替换的断言计数写错（一句实际 3 处，我按 1 处断言） | 断言在写入前抛错，**5 个文件一个都没改**；先 grep 出真实次数再重来 |
| 6 | 新守卫**第一版有个会撒谎的绿灯**：汇总行自己重算条件，上面打了 ✗、下面仍写「0 个 ✓ 对照通过」 | 跑对照 B/C 时看见 ✗ 与 ✓ 同时出现；改成汇总只看 `bad` 列表 |
| 7 | **对照装置放 `/tmp` 下跑 ⇒ 测了个空**：脚本开头是 `cd "$(dirname "$0")/.."`，根目录变成 `/tmp`，所有 python 项退化成「需 python3」、devlog 变「0 个 / 行」 | 输出明显不对（全是 0）才发现；把副本放进 `tools/` 内重跑才有效，跑完即删 |

共同点：#2/#3/#5 靠**写入前的断言/异常**拦住（所以没有半成品落盘）、#6 靠反证对照、#7 靠"输出好得不像真的"、
#1/#4 靠提交前重读。**#7 最阴** —— 它让两次对照看起来跑过了，实际什么也没测；教训是变异副本必须放在
被测脚本能自己 `cd` 到仓库的位置。

第 6/7 两处修完后的三连对照复验（都在仓库内跑，跑完删除副本）：

| 对照 | 手法 | 期望 | 实测 |
| --- | --- | --- | --- |
| A | 往 `AppViewModel` 注入 `private val _fakeEvent = MutableStateFlow<String?>(null)` | ✗ + 计数 1 | ✗ `_fakeEvent` + 「1 个；另有 1 处 ✗ ⇒ 本项不通过」✓ |
| B | 把探测正则改成永不匹配 | 报「阳性对照失败」 | ✗ 阳性对照失败 + 「0 个；另有 1 处 ✗ ⇒ 本项不通过」✓ |
| C | 把替代机制期望值 3 改成 99 | 报 `Channel` 数不符 | ✗ `Channel<UiEvent>` 实测 3 个 + 不通过 ✓ |

三种情况都不再出现 ✓；还原后恢复「0 个 ✓ 对照通过」。注入的字段已从 VM 删除并复跑词法预检确认无残留。

### 8. #4b：主壳三处「按主题分流」收成一个 helper + 把文案与落位钉进 CI（提交 `265193f`）

**范围被 4a 的实施结果改过**（ROADMAP 的 4b 行已整行重写）：原计划「新增一份 `UiEventHandler` 挂在每个
Snackbar 宿主上」做不到 —— 三个落点的宿主类型不同（主壳是两个裸 host、二级页是 `AppSnackbarHostState`），
且收集协程必须与宿主同生命周期。所以 4b 只做真正重复的那部分：**分流**。

- `ui/components/UndoSnackbar.kt` 新增 `internal suspend fun showUndoSnackbarAcrossThemes(isMiuix, md3Host,
  miuixHost, message): Boolean`，放在两个 `showUndoSnackbar` 扩展旁边；
- `MainApp` 三处形状完全相同的 if/else 各收成一次调用（撤销消耗、恢复归档、**批量归档** —— 第三条不走事件系统，
  是 `archiveSelected()` 里 `scope.launch` 的直接调用；重复的是"分流"，与走不走事件无关）；
- 删掉随之失效的 3 条 import（`SnackbarResult`、`MiuixSnackbarResult` 别名、`showUndoSnackbar`）⇒
  主壳里 `SnackbarResult` 字样实测 **0** 处；净 −4 行（+21/−25）。

**一个被否掉的更"干净"方案**：让主壳直接用二级页那个 `AppSnackbarHostState`（它已经把分流收在容器里了）。
否掉的理由是生命周期而不是审美：那容器是 `remember(isMiuix)` 建的，切主题会换**新**容器与新宿主，
而主壳的收集协程是 `LaunchedEffect(Unit)`（key 一变就取消协程、中断 `showSnackbar` —— 当年 MD3 撤销条不出现的
根因），协程捕获的还是旧容器 ⇒ 提示弹到**没有渲染**的宿主上，而 `showSnackbar` 挂起到关闭为止
⇒ 收集协程永久堵住、之后所有撤销条都不再出现。所以主壳这两个宿主的身份必须跨主题稳定，分流只能收在自由函数里。
这段理由写进了 helper 的 KDoc（下一个想"顺手统一"的人会先读到它）。

**新增守卫 `SnackbarCopyTest`（8 例，单测 127 → 135）**：#4a/#4b 的验收判据是"纯搬运"，而纯搬运意味着
**文案与落位逐字不变** —— 这类改动既不编译失败也不崩，只会让用户看见另一句话或另一个位置的条。钉了：
5 条提示文案在全仓的**分布**（哪个文件、几处）、恢复归档那两句在主壳与归档页**两处一致**
（全仓唯一故意重复的文案：归档页走本地回调 `state.restoreEntry(id) { merged -> … }`、不经事件系统）、
分流只有一处实现、两主题的「撤销」标签与 6 秒自关、主壳覆盖层落位 7 个标记
（底部对齐 / 导航栏避让 / 键盘避让 / 动画偏移 84-8dp / 两主题各自宿主形态）、二级页落位
（首页 `FloatingNav`；消耗记录页与归档页**不指定** ⇒ 走 `AppScaffold` 默认 `SystemBars`）。
首例是**注释剥离的阳性对照**：`UiEvent.kt` 的 KDoc 里也写着这些文案，剥注释后必须查不到，
否则"只出现在 X 文件"这类断言全是假的。

**期望值必须先量、而且我量错了两回**（都记在测试类注释里）：
1. 第一遍测量脚本按 `startswith('UndoSnackbar.kt')` 匹配，而键是**带目录前缀**的相对路径
   ⇒ `UndoSnackbar.kt` / `HomeScreen.kt` / `AppChrome.kt` 三项报出**假零**。按 basename 重测才拿到真值。
   （与 §7 第 7 处同一类错：测量装置本身没被验证。）
2. 两处期望值本来是我**猜的**：以为 `MiuixSnackbarResult.ActionPerformed` 有 2 处（实为 1 处 —— Miuix 那个扩展
   只是返回结果、不做比较）；以为 `SnackbarResult.ActionPerformed` 有 1 处（实为 2 处 —— 它是
   `Miuix**SnackbarResult.ActionPerformed**` 的**子串**）⇒ 断言一律改成**带接收者的整行片段**。

**本地无编译器 ⇒ 用 Python 镜像验证这道守卫**：把 8 例里的 37 条断言按同样逻辑跑一遍（基线全过），
再做三种变异证伪：① 改文案「已减少一件并计入消耗」→「已消耗一件」⇒ 红；② 落位 `84.dp` → `80.dp` ⇒ 红；
③ 把 `SnackbarResult` 的 import 抄回主壳（模拟有人绕过 helper）⇒ 红；还原后恢复全绿。
`tools/kt-lexcheck.py` 全仓 **82/82** 干净（含新测试文件）。

**还修掉一处会编译失败的写法**：`sourceRoot` 是 `by lazy` **委托属性**，Kotlin 对它**不做智能转换**，
在 lambda 里直接写 `it.relativeTo(sourceRoot)` 会因「要 `File`、给的是 `File?`」编译失败 ⇒ 先取局部 `val root`。
（这类错本地抓不到，只能靠读；已写进那个属性的 KDoc。）

**顺带查清一件文档口径**：全仓其实有 **6 处** Snackbar 宿主站点（主壳覆盖层 + 首页 / 消耗记录 / 归档 /
食品详情 / 设置 各自持有 `AppSnackbarHostState`），其中只有 **3 处**收 `UiEvent`。
此前"三个宿主"的说法容易被读成"全仓只有三个宿主"，ROADMAP 与 INDEX 都已按此改写。

### 9. #4c：5 个回调式错误上报 → 带分类的 `UiEvent`（提交 `a6b67d7`，修复 `2fd6089`，CI 绿）

**范围比计划大**：计划写的是 3 个 `(Boolean, String)` 回调，实测 **5** 个 —— 4 个 `(Boolean, String)`
（`syncUpload` / `loadCloudBackups` / `syncDownload` / `restoreLocalSnapshot`）+ 1 个 `(Boolean, Boolean)`
（`importBackupWithSnapshot`）。第 5 个是**改完前 4 个之后靠残留扫描才发现的**（扫 `(Boolean,` 与 `onResult`）：
它的三句提示文案原本写在 `SettingsScreen` 里由界面拼两个布尔得到，与「从快照还原」那套完全同构 ⇒
留下它验收 ①「一次性事件只剩一种建模方式」就不成立，所以一并收（文案搬进 VM，逐字未改）。
⇒ ROADMAP 的证据行已重写，记为**核查第 15 处**（文档的数与代码不符、且没有任何东西会因此变红）。

**改了什么**（设计理由与取舍在 ROADMAP「4c 落地结果」，这里只记实施事实）：
`data/CloudSync.kt` 新增 `sealed interface OpFailure`（`Auth` / `Network` / `Other`）+
`Throwable.toOpFailure(fallback)`，401 那句抽成 `NUTSTORE_AUTH_MESSAGE`（4 个抛出点共用，
因为**分类不能靠"两处字符串恰好一样"**）；`UiEvent` 新增 `OpFailed(op, failure)` / `CloudBackupsEmpty(message)`、
`Notice` 的落点改为可指定（默认仍是 `Home`）、加 `UiSurface.Settings` 与 `DataOp`（5 值）；
`AppViewModel` 加第 4 条 `Channel` 与 `settingsUiEvents`；`SettingsState` 三层 ×5 个函数；
`SettingsScreen` 5 处调用点简化 + 1 个收集器（穷尽 `when`，只有 `op == ListBackups` 时关备份选择器）。
主源码里 `onResult` 与两种回调形状**清零**（残留的 `(Boolean, String)` 字样只在注释里，逐个核过）。

**新增守卫**：`OpFailureTest`（6 例）+ `UiEventTest` +2 例 + `SettingsStateTest` 的转发测试补上这 5 个动作
⇒ 单测 135 → **143**。`OpFailureTest` 是**真行为测试**而不是源码文本断言，理由：分类逻辑的输入是**异常对象**，
真机造不出 DNS 失败 / 401 / 超时三种网络环境，只有 JVM 单测能覆盖；顺带它也让"文案逐字未改"有了可执行的凭据
（改造前三个调用点各自的兜底话被逐个写死，含"消息为空白等同没有消息"）。
`SettingsStateTest` 那 5 个动作**改造前测不到**（带回调参数，测试里没法直接调）⇒ 这次是净增覆盖。

**文案零漂移是机械验证的**：`git diff -U0` 里所有被删行的字符串字面量与新增行比对，**消失 0 条**
（新增的 8 条全是注释里的中文短语）。这比"我读过一遍觉得没改"强得多。

**守卫升级（与代码同提交）**：`doc-metrics.sh` 里那条"替代机制在场"检查原本把队列数**写死成 3**，
加了第 4 个落点它就红 ⇒ 改成**从 `UiSurface` 枚举反推**期望值（落点数 = 队列数 = 对外 Flow 数）。
这样它能抓两种真错误：「有落点没队列」（事件发出去没人收 ⇒ 提示静默消失）与「有队列没落点」。
两个方向都反证过：临时加第 5 个落点 ⇒ 报「实测 4 个、落点 5 个」；临时删掉 `Settings` ⇒ 报「实测 4 个、落点 3 个」；
还原后恢复 ✓。**写死数字那版今天已经咬到我一次** —— 红的是我自己两轮前刚加的守卫。

### 10. 本轮（4c）我犯的 6 处错，与一次「CI 红了但看不到日志」的定位过程

| # | 错 | 怎么发现 | 怎么改 |
| --- | --- | --- | --- |
| 1 | ROADMAP 的 4c 证据行写「3 个回调」，实测 5 个 | 改完 4 个后扫残留 | 证据行重写 + 记为核查第 **15** 处（§9） |
| 2 | 批量改名先换短串 `SyncFailure`，把 `toSyncFailure` 吃掉一半 | 换完断言"旧名残留 = 0"命中 0 ⇒ **写入前中止** | 改名一律**长串先换**，再逐文件断言旧名清零 |
| 3 | 第二个改名脚本的靶子**凭记忆**写（写成了上一次修复前的版本）⇒ 命中 0、抛错、文件未被改 | `assert s.count(old) == 1` | 靶子一律先 `sed`/`grep` 打出来再写（本日同类错第 3 次） |
| 4 | `private const val NO_CREDENTIALS_MESSAGE` 插在**类体内** | 提交前自查：Kotlin 的 `const val` 只能在顶层或 `companion object` | 挪到顶层（与本文件既有的 `TAG` 同一惯例），KDoc 里写明为什么 |
| 5 | `UiEvent` 从 4 类变 6 类后，`MainApp` 的 `when` **不再穷尽** | 提交前全仓扫 `is UiEvent.` | 补两个分支（sealed 的 `when` 不穷尽**直接编译失败** —— 这正是 sealed 想要的效果） |
| 6 | 重写 `Notice` 的 KDoc 时，把 `SnackbarCopyTest` 阳性对照依赖的那句文案**写没了** | **CI 红了**（本地无编译器） | 照原文补回，并把这段隐形耦合写进 KDoc（`2fd6089`） |

**#6 的定位过程值得单独记**（本轮最花时间的一段）：

1. CI 结果：静态门禁 ✅、release+R8 ✅、「debug APK + 单测 + lintDebug」❌。
2. 日志与产物**都取不到**：`gh run view --log` / `--log-failed` / REST 的 logs 端点 / `gh run download -n test-report`
   一律 `EOF`（托管在 `results-receiver…` 与 `blob.core.windows.net`，本环境被墙）。这是**环境事实**，不是配置错。
3. 能取到的只有 job 结论、步骤名、**check-run annotations**。从 annotations 读出两条线索：
   `Process completed with exit code 1`；`No files were found with the provided path:
   app/build/reports/lint-results-debug.*` ⇒ **lint 报告压根没生成** ⇒ Gradle 在 `lintDebug` 之前就停了；
   而 release job 编译同一份主源码是绿的 ⇒ 红在 `testDebugUnitTest`（单测编译或断言），不是主源码。
4. 于是把「会扫到我改过的文件的测试」全量映射出来（用 `grep` 列出每份测试读了哪些主源码路径），
   逐条**镜像成脚本**在本地跑：`CorruptGuardTest` 9 条全过（含 `functionBody` 的截断规则与"先读后写快照"的顺序）、
   `UiEventTest` 源码文本 5 条全过、`ScreenParityTest` 4 例全过（含"8 个已合并屏幕不许再出现 `Miuix<X>` 第二实现、且本体都还在"）、
   `SnackbarCopyTest` **8 例 36 条里恰好 1 处不符** ⇒ 就是那条阳性对照。
   `ImeHandlingTest` / `MiuixDialogContentTest` 用推理排除：我新增的是 `//` 注释与**不含任何字符串字面量**的收集器，
   既不会改动 `codeOnly()` 被 MIME 通配符骗到的吞并区间，也没碰 8 个 `MiuixDialog` 调用点。
5. 修完重跑镜像 9/9 全过 ⇒ 推送 ⇒ run [35297362614](https://github.com/SkyForest233/chileme/actions/runs/35297362614)
   三 job 全绿。

**教训（两条，都不是"下次小心点"这种）**：
- **阳性对照是双向的**：它既证明守卫没空转，也会在"被当样本的那句注释"被无意删掉时立刻变红。
  这次是它救的场 —— 若没有那条对照，我删掉一句 KDoc 文案不会有任何后果，
  而那些「某句话只出现在某文件」的分布断言也就悄悄失去了凭据（正是它当初被写下来的理由）。
- **耦合必须写在被依赖的那一侧**：`SnackbarCopyTest` 的注释里写了"UiEvent.kt 的 KDoc 里也写着这些文案"，
  但 `UiEvent.kt` 那边**一个字都没提** ⇒ 改 KDoc 的人（我）无从知道。现已把这段写进 `Notice` 的 KDoc。

**⚠️ 一件做不成的事（环境限制，需用户或权限）**：本想给 `build.yml` 加一步「失败时把单测 XML 结果与
Kotlin 编译错写成 annotations」—— annotations 端点是通的，本地已做阳性对照（假 XML + 假日志能正确吐出
「类名 > 中文用例名 > 断言消息」与 `e: …:44:9 too many arguments` 这样的行号，`bash -n` 与 `py_compile` 都过、
heredoc 终止符在 YAML 块标量剥缩进后落在第 0 列也核过）。但推送被 GitHub 拒绝：
`refusing to allow a GitHub App to create or update workflow .github/workflows/build.yml without workflows permission`
⇒ **当前令牌改不了 `.github/workflows/*`**。那条提交已从分支上摘掉（`git reset --hard HEAD~1`），补丁留在会话工作区。
若希望以后 CI 失败能自报家门、不必再靠镜像脚本猜，需要给这个 App 加 `workflows` 权限，或由用户本人提交那一步。

---

### 11. 本轮收尾：用户实机复测通过、两个待决策项已决、环境抽风两次

**① 真机复测通过（2026-09-18，用户实机）**：两主题 × 4a/4b 的四处提示 + 4c 的设置页 5 条流程，**无问题**
⇒ #4 全项（4a/4b/4c）收官，验收 ①–⑤ 全部达成（③ 由"机制上成立"升级为实机确认）。

**② 两个待决策项，用户都选了「保持现状」**（内容见上方「待办变化」）：英文技术串**不改**、
`saveLocalSnapshot` 那条死管道**不删**。两个决定都记在此处，是为了以后不必重新论证一遍。

**③ CI 失败自报那一步：我这边做不成，已改成给用户手工提交**（用户选的方式）。
补丁与操作说明做成了交付物：`CI补丁-请你自己提交.md` + 装好的完整 `build.yml.new`
（放在**会话工作区根目录**、不在仓库里 —— 免得给仓库加一个平时用不上的文件）。
本地验过 5 项：装进 `build.yml` 后结构正常（161 → 222 行，`id: build` 与 `if: steps.build.outcome` 对得上）、
`bash -n`、内嵌 Python `py_compile`、heredoc 终止符在 YAML 剥缩进后落在第 0 列、
**阳性对照**（假 XML + 假编译错 ⇒ 正确吐出「类名 > 中文用例名 > 断言消息」与 `OpFailureTest.kt:44:9`
这样的行号，退出码 1）。用户提交后，本仓 CI 失败就不必再靠"把守卫镜像成脚本"来定位（§10 那 20 分钟）。

**④ 环境抽风两次（都不是代码问题，但都影响验证，如实记）**：
- **GitHub 令牌一度失效**（4b 那次 CI 跑完的那一刻）：job 级结论没取全、文档提交推不出去。
  当晚恢复 ⇒ `5178cee` 补推成功、run 35295354068 三 job 全绿，4b 那格"依据较弱"已升级为已足。
- **本地检出被重置回基点 `fbab91a`**（4c 的文档提交推送成功之后）：`git log` 只剩一个提交，
  本轮 8 个提交在工作区里全变成"未提交改动"；`/tmp` 下的草稿与备份（含那份 CI 补丁的 `format-patch` 产物）
  整目录消失；先前被 `reset --hard` 摘掉的那个提交对象也不在对象库里了。
  **但工作区文件一个没丢，远端分支稳稳停在 `65a28e9`。**
  恢复办法：`git fetch` 后用 **`git reset --mixed FETCH_HEAD`**（只拨 HEAD 与索引，**绝不碰工作区文件**）
  ⇒ 恢复后 `git status` **0 行**、`git diff HEAD` **0 行**（与远端逐字节一致），8 个提交全数回到历史里。
  ⚠️ 这里刻意**没有**用 `--hard`：当时工作区是那些内容的唯一副本，`--hard` 一旦判断错就是真丢。
- **一个差点误判的信号**：恢复前先跑 `git diff FETCH_HEAD`，报"47 个文件、12177 行删除"，
  看着像工作区缺了 47 个文件 —— 实际那 47 个正是**未跟踪**文件（`git diff` 不比较未跟踪文件），一个都没少。
  **教训：`git diff <commit>` 报的"删除"必须先与 `git ls-files --others` 对账，才能当"缺失"看。**

---

### 12. #5 全程落地：5a DI 容器 → 5b 时钟注入 → 5c 仓库按领域拆完（965 → 252 行）

§1–§11 记的是 #4。**#5 是用户 09-18 晚答复「我已经真机复测通过了，没有问题。继续按规划推进 5」之后开工的**，
本节把 5a/5b/5c 一起记完（10 个提交，其中 2 个是修 CI 红；run 号见末节台账，全部取自 `gh run list`，非凭记忆）。

#### 12.1 5a：轻量手写 DI（提交 `ceb0cf1`）

`ChiliMeApp`（`Application` 子类 **0 → 1**，`AndroidManifest.xml` 挂 `android:name`）持有 `AppContainer`
（`clock` / `repo`）；`AppViewModel` 从 `application` 取容器，**构造签名 `(Application)` 一字未动** ——
`ViewModelProvider` 的默认工厂用反射找 `(Application)` 构造器，而 Kotlin 的默认参数只生成带
`DefaultConstructorMarker` 的合成构造器 ⇒ 加个带默认值的第二参数就会**运行时崩**（编译器不报）。
仓库的现场构造点归零，现由 `tools/doc-metrics.sh` 的「依赖构造点」守卫看着（含 Manifest 必须挂同名
`android:name`：漏挂只在运行时炸）。**不引入 Hilt/Koin**（用户批准的做法：手写容器）。

#### 12.2 5b：时钟注入（提交 `e054b32`）

`FoodRepository` 的 `internal` 主构造多收 `clock: Clock = Clock.systemDefaultZone()`；替换**数据层 9 处 + VM 5 处**
函数体硬调 ⇒ 数据层与 VM 的硬调**归零**，14 处已注入；UI/主壳 **7 处刻意保留**（界面「现在几点」没有值得测的
跨零点逻辑，把时钟穿进 Compose 要多传好几层），另有默认参数 3 + 便捷属性委托 5，逐条登记在脚本豁免清单里并写明理由。
新增 `FoodRepositoryClockTest` 6 例 + `AutoSyncDueTest` 4 例，单测 143 → **153**。
固定时钟刻意选**离当天半年远**的 2026-03-05：若有人把时钟改回系统时钟，按「固定的今天」摆好的数据会落到
完全不同的相对位置 ⇒ 断言必红；选个等于真实日期的值，这种回归会**静默通过**。
⚠️ 计划里原写「数据层 7 处」是错的：`CloudSync`（云端备份文件名时间戳）与 `LocalSnapshotStore`（快照文件名时间戳）
都在 `data/` 下且只被 VM 调用，被误归给「UI 侧」⇒ 核查第 17 处，本次一并注入（两处都加带默认值的 `clock` 参数，
默认值就是系统时钟 ⇒ 行为逐位不变）。

#### 12.3 5c：仓库按领域拆完（7 个提交，一个领域一个提交）

| 步 | 新文件 | 行数 | `FoodRepository.kt` | 搬走的函数 |
| --- | --- | --- | --- | --- |
| 5c-1 | `RepositoryCore.kt` | 244 | 965 → 772 | 共用底座：`Decoded`/`orElse`/`DecodeCache` + `decodeStrict`/`markCorrupt`/`isCorrupt`/`resilientRead`/`rawFlow`/`lightFlow` + `pruneCorruptDir` |
| 5c-2 | `FoodItems.kt` | 106 | 772 → 686 | `seedIfNeeded` / `upsert` / `updateLocationBatch` |
| 5c-3 | `FoodArchive.kt` | 100 | 686 → 601 | `archiveItems` / `restoreArchived` / `restoreArchivedBatch` / `deleteArchived` / `clearArchive` |
| 5c-4 | `FoodConsumption.kt` | 177 | 601 → 444 | `changeQuantity` / `addConsumption` / `deleteConsumption` / `undoConsumption` / `migrateConsumptionIds` / `compactConsumption` |
| 5c-5 | `FoodBackup.kt` | 124 | 443 → 336 | `buildBackupJson` / `buildCsvExport` / `previewBackup` / `importBackupJson` / `clearAll` |
| 5c-6 | `FoodSettings.kt` | 65 | 336 → 283 | 外观 5 + 同步节奏 3 + 分类与位置 3（共 11 个 setter） |
| 5c-7 | `FoodCredentials.kt` | 49 | 283 → **252** | `setNutstoreCredentials` / `migratePlaintextPassword` |

核心文件收官后只剩 **9 个类级函数**（7 个 `decode*` 包装 + `nutstoreCredentialKeysFlow` + `discardCorrupt`）、
19 个 key、19 条对外读取流；`data/` 下 15 个文件**最大 293 行、无一超 400**（已加进 doc-metrics 当判据，
并做过阳性对照：把判据临时收紧到 100 行，它确实点名超限文件 ⇒ 不是摆设）。

**形状 = 同包 `internal` 扩展函数**（`internal suspend fun FoodRepository.upsert(item: FoodItem)`），
函数体逐字搬、只整体左移 4 空格 ⇒ 同包内调用写法一字不变，只有跨包的 `AppViewModel` 要为搬走的函数
逐个加 import（本仓禁通配导入，共加 **31** 行；另有 `compactConsumption` 只在领域文件内部被调用，**不**导出）。**否决过的三个方案**（都写在 `RepositoryCore.kt` 的文件 KDoc 里）：
① 类里留一行转发（转发成员与扩展函数撞名 ⇒ 成员遮蔽扩展 ⇒ **无限递归且编译期不报**）；
② 把流搬成扩展属性（`get() =` 每次访问**新建 Flow 实例** ⇒ 行为差异）；
③ 包级共享 `internal TAG`（本包已有多个文件级 private TAG，本地无编译器验证不了撞名）⇒ 每个文件一份 TAG 副本。

**纯搬运的判据（每笔都核过）**：`git diff -w --stat` 的**新增行数恰好等于放宽可见性的行数**
（5c-2 = 7、5c-3 = 2、5c-4 = 2、5c-5 = 5、5c-6 = 8、5c-7 = 3），其余全是删除；
搬运脚本自带两道自校验：搬走的每一行都必须在新文件里逐字找到（缩进除外）、声明行按
「`internal` + 原签名去 `private` + 泛型后插入接收者」**反推比对**。

#### 12.4 这一轮我犯的 8 处错（都在本轮内发现并改正）

1. **手写搬 `FoodItems.kt` 时把函数名弄丢了**（产出 `internal suspend fun FoodRepository.) {`），
   而"逐行还原核对"**恰好豁免了声明行** ⇒ 没抓到。改成脚本化 + 反推断言。
2. **一次把两个领域都搬进了工作区** ⇒ 违背"一个领域一个提交"，无法落账 ⇒ 用事先拷贝的备份整体还原重来
   （**绝不用 `git checkout <file>`** 还原未提交改动）。
3. **搬运脚本认声明行的正则容不下开头的 `private `** ⇒ `compactConsumption` 被原样搬成**没有接收者**的
   顶层函数，体内却用着仓库成员 `clock` ⇒ 编译错（CI 红 ①）。放大器：自校验只断言"我改写过的行"，
   没改写的行**两边都看不见** —— 与第 1 条同一个结构。修法：按行号定位改写 + 断言"改写记录数 == 要搬的函数数"
   + 新断言"新文件里每个顶层 `fun` 都必须带接收者，否则体内不得出现任何仓库成员名"。
4. **`CompactConsumptionTest` 没跟着搬**（仍读 `FoodRepository.kt` 找 `if (!record.isDeletable())`）⇒ 单测红（CI 红 ②）。
   根因是我只手工镜像了 `CorruptGuardTest`，没想到别的测试也在读仓库源码 ⇒ 写成工具 `tools/guard-mirror.py` 入库，
   每搬一个领域先扫一遍。
5. **#5c 方案里给"消耗域"编了 7 个不存在的函数名**（`consumeToArchived` / `consumeByCategory` / `restoreConsumed` /
   `setExpiringAutoArchive` / `setDaysBeforeExpiring` 等，连 `autoArchiveKey` 这个 key 也不存在）：master 起点、
   开工前的 965 行原版、当前文件三处都搜不到 ⇒ 是我凭记忆编的，不是拆分弄丢的代码（**没有任何东西丢失**）。
   差点拿它当"死代码"去处理。教训：`grep` 出"零引用"之前，先确认这个标识符**真的存在** ——
   阳性对照同样适用于"找不到"这种结果。
6. **散文写出被追踪的标识符**（第 4 次同款）：新文件的 KDoc 写了损坏 key 集合的后备字段名 ⇒
   计数 32 → 33 被守卫抓到。凡散文要提这类名字一律改用描述式措辞。
7. **两次夸大 / 无效的对照**：① 我在测试注释里写"顶层函数用 4 空格规则截断会只剩几行、断言全部假绿"，
   实测 `upsert` 只差 1 行（30 vs 31）、`changeQuantity` 差 2 行（50 vs 52），断言其实都能过 ⇒
   按实测口径改写（参数仍是原则上必要：截断点会落进函数体内部，内部块靠前的函数会被截掉大半）；
   ② 5c-7 想验证守卫镜像能抓到失配，把 `FoodCredentials.kt` 改回 `FoodRepository.kt` 做对照，
   结果工具报 0 问题 —— 因为那条断言是 `Regex(...).findAll(...).count()` 形状，**正是工具 docstring 里
   写明不覆盖的那一类** ⇒ 这条守卫只能手工核（新文件 2 处 / 旧文件 0 处，恰好等于断言要求的 2）。
   教训：工具报"0 问题"只等于"它覆盖的那一类没问题"，不等于 CI 一定绿。
8. **doc-metrics 新行里 `2>/dev/null || echo '需 python3'` 把我自己的括号错伪装成"环境缺 python3"** ⇒
   两条新行去掉吞错误的重定向。同一轮还踩了两次 canary 放错位置：副本放 `/tmp` 时脚本第 29 行
   `cd "$(dirname "$0")/.."` 会 cd 到 `/` 去扫全盘（**超时 180 秒**）；放仓库根当点文件时 `dirname` 得到 `.`、
   再 `cd ..` 就跑到仓库外（值为空）⇒ **canary 副本必须放 `tools/` 里**才与真脚本同环境。

#### 12.5 环境抽风第二次与第三次：令牌失效 + 沙箱重建（持久性边界）

- **令牌失效**：5c-6 提交完正要推送时 `GH_TOKEN` 失效（git 与 gh 两条通道都 401）⇒ 只能本地提交、
  等用户重连。5c-5 是在失效**之前**推上去的，监视器退出码 0，但当时已无法复查 ⇒ 恢复后补查
  run [35307866156](https://github.com/SkyForest233/chileme/actions/runs/35307866156) = success（**结论要能复查才算记完**）。
- **沙箱重建**：重连后发现本地 `.git` 是**全新克隆**（reflog 只剩 `clone` + `checkout -b` 两条），
  我这条分支的提交对象一个都不在，本地分支被建在 `fbab91a`，而它与远端分支的根 `fbeb5dc`
  是两条**没有共同祖先**的历史 ⇒ `git diff HEAD...FETCH_HEAD` 报 no merge base。
  **工作区文件快照保住了内容**、远端分支也完好（tip = 5c-5）。
- **恢复方式**：`git reset --mixed FETCH_HEAD` 把分支指回远端 tip（**绝不动工作区**），然后核对差集 =
  恰好 3 个文件、行数 283 与 65、VM 里 `import com.agon.app.data.set*` 恰好 11 行 —— 与重建前那一笔一致才重新提交。
- **另一条边界**：放在仓库外（`/home/user`）的三个脚本与 `FR.pre5c*` 备份**全被抹掉** ⇒
  值得留的工具必须进仓库（`guard-mirror.py` 已入库，提交 `0f39d1f`），备份优先靠 git 历史而不是仓库外副本。
- **还有一条读数陷阱**：`git log A..B` 那个"未推送提交"的检查，我写成 `A || B` 形式，A 静默失败后落到 B，
  看起来像验证过其实没有 ⇒ 换成 `git for-each-ref` 才查清"这个克隆根本没有该分支的远端跟踪引用"。

#### 12.6 #5 全项验收对账（收官）

① ✅ 跨零点逻辑有固定时钟单测（5b 的 10 例）；② ✅ 依赖只有一个构造点（doc-metrics 守卫看着）；
③ 🔶 **在 #5 范围内达成**：`data/` 15 个文件最大 293 行、无一超 400；⚠️ 但全仓还有 **6 个文件超 400 行**，
都在 UI/VM 层、不属 #5 范围 ⇒ 别因为 #5 收官就当成全仓达成。**（09-18 晚补正：此处原抄了 6 个行数，
其中 VM 那个是 5b 时的 668、实为 700 ⇒ 核查第 18 处；现改成指针 —— 清单跑 `bash tools/doc-metrics.sh`
看「主代码超 400 行的文件（全清单）」行，处置见 §13 与 ROADMAP #10。）**
④ ✅ **行为不变已由用户真机复测确认**（2026-09-18，见 §12.7）；代码层面每笔 `git diff -w` 也核过：
新增行只有可见性放宽与 import；
⑤ ✅ 单测数只增不减：143 →（5b）**153** →（5c）**153**（拆分不新增测试，但 4 条源码守卫跟着搬了家；
原始 `grep @Test` 会数出 154，多的那处在 `ImeHandlingTest.kt:218` 的 KDoc 里，剥注释后是 153）；
⑥ ✅ ktlint / detekt 0（每轮 CI 的「静态门禁」job 全绿，detekt 仍是 block 模式）。

#### 12.7 真机复测（#5 的最后一道验收）—— 2026-09-18 用户实机**已通过**

- **我给出的复测口径**：8 类操作，且要求 MD3 与 Miuix **两个主题**各过一遍关键页 ——
  ① 冷启动与首页列表；② 新增 / 编辑 / 批量改存放位置；③ 改数量（+ 与 −）、减到 0 自动进归档、撤销；
  ④ 归档页单条恢复 / 批量恢复 / 删除 / 清空；⑤ 消耗记录页删单条（聚合的「月度合计」那条**不该给删**）/ 撤销删除；
  ⑥ 设置页各项开关（动态取色、深色模式、调色板、界面风格、悬浮导航、自动同步天数、分类阈值、分类与位置清单）；
  ⑦ 坚果云凭据保存、备份导出 / 导入、CSV 导出、清空全部数据；⑧ 上述关键页在两主题下各一遍。
  装前提醒过：**先在设置页导出一份 JSON 备份**（签名不同可能要卸载重装，数据会没）。
- **用户答复原文**：「已经真机实测好了，没问题。」追问覆盖范围与包体后确认：
  **按清单全过、两个主题都过、装的是 debug 包**（run [35311403454](https://github.com/SkyForest233/chileme/actions/runs/35311403454) 的 `app-debug` 产物，23MB）。
- ⚠️ **如实记一条缺口**：这次**没有装 release 包** ⇒ release 侧只有 CI 的 R8 混淆 + `lintRelease` 保障，
  没有实机确认。与 #4 那轮「debug 主测 + release 冒烟」的口径相比少了后一半。#5 是纯搬运、
  且 release 的编译与 R8 在 CI 里每轮都过，风险很低，但**这不等于已验证** ⇒ 下次发版前补一次 release 冒烟。
- ⇒ **#5 验收 ①–⑥ 全部达成**（③ 仅在 #5 范围内：`data/` 15 个文件无一超 400 行；
  UI/VM 层那 6 个大文件仍是待办，不因 #5 收官当成全仓达成 —— 清单见 doc-metrics 的「全清单」行，
  09-18 晚已把此处抄的行数改成指针，理由同 §12.6 的补正）。
- **下一步的前置已解除**：#6（派生数据下沉 VM + `WhileSubscribed`）原本卡在"前置 #5"，现在随时可开工。


### 13. 拆分余量评估 + KernelSU manager 结构对照 ⇒ 新开 ROADMAP #10（规划，未开工）

**用户指令原文**：「评估一下仓库里还有多少内容需要拆分。看一下 KernelSU manager 仓库的 app 结构，它也是双主题，
有什么更好的地方。」随后：「按你的规划来。不用放到 audits 里。」⇒ 本节就是那份评估的落账，
执行计划写在 [`ROADMAP`](../docs/ROADMAP.md) **#10**（编号避开已占用的 #6–#9；**不进 `docs/audits/`**，按用户指示）。

#### 13.1 本仓余量（口径：`find` + `wc -l`，作用域 `app/src/main`）

| 层 | 文件 | 行数 |
| --- | --- | --- |
| `ui/screens` | 17 | 5,491 |
| `ui/components/app`（App 级组件层） | 12 | 3,074 |
| `ui/components` | 10 | 1,887 |
| `data` | 15 | 2,127 |
| 壳层（`MainApp` / 导航 / 批量条等） | 6 | 1,217 |
| `viewmodel` | 2 | 819 |
| `ui/theme` | 7 | 384 |
| **合计** | **69** | **14,999** |

超 400 行的 6 个（`doc-metrics.sh` 已加「全清单」行，本节不再重复行数，见 **13.5 核查第 18 处**）。
**结论：真正值得动的只有 3 处** —— 设置页（1,705 行）、`AppViewModel`（700 行）、`EditFoodScreen`（659 行）；
其余 60+ 个文件都已达标，`data/` 是 #5c 刚清完的。

#### 13.2 设置页内部结构（行号边界实测，这决定了 10a 怎么切）

入口 `SettingsScreen` 176–994（819 行）里，**弹窗区 350–994 ≈ 645 行**：`AppOptionDialog` ×2（352/381）、
`AppConfirmDialog` ×3（506/815/973）、以及 **4 对手写的** `MiuixDialog` + `AlertDialog`
（导入预览 416/462、坚果云账号 524/573、云端备份选择 623/745、本地快照列表 832/916）。
其余：`Md3SettingsBody` 996–1380（385）、`MiuixSettingsBody` 1382–1566（185）、
`SettingsNavRow` 1568–1612（45，只被 MD3 body 调用）、`PaletteSwatch` 1614–1705（92，同）。全文 55 处主题分支。
⇒ **入口体的 79% 是弹窗**，这是 10a-1 的落点；两个 body 加起来才 570 行，且 MD3 侧带着 NavRow + Swatch 共 522 行，
所以 10a-2 的 MD3 文件**必然还要再切一刀**才够 400 以下。

#### 13.3 KernelSU manager 对照（`tiann/KernelSU` @ main，2026-09-18 用 `gh api` 逐文件量的）

规模：219 个 `.kt` / 约 1,450 KB / ★18.4k。双主题取值与我们一样是两种（`UiMode.Material` / `UiMode.Miuix`
对应本仓 `ThemeStyle.MATERIAL3` / `MIUIX`），**实现路子相反**：他们每屏写两份完整文件，
根 `MainActivity.kt`（448 行）里另有 8 处 `UiMode.` 分支决定用哪套 `Scaffold`。

| 维度 | KernelSU | 本仓 | 判断 |
| --- | --- | --- | --- |
| 双主题 | 每屏两份实现；同一功能两版体积（字节）Home 23,530 vs 29,222 · Module 43,549 vs 47,506 · Settings 19,383 vs 25,240 · SuperUser 24,467 vs 30,805 | 单文件双主题 + App 级组件层吸收差异 | **每对 Miuix 版都大 4–6 KB ⇒ 已在漂移**；不学 |
| ViewModel | 10 个（一屏一个） | 1 个 700 行（50 函数 / 44 属性 / 20 处 `stateIn(`） | 值得学 ⇒ 但成本高，记 **#10d 延后** |
| 目录 | `ui/screen/<feature>/`（每包 = 分岔 3.5 KB + `XxxUiState` 1.9 KB + `XxxUtils` + 两版实现） | `ui/screens/` 17 个文件平铺 | **不采纳**：实测守卫成本高于收益（见 13.4） |
| 数据层 | `data/repository/` 10 个文件 | #5c 后 `data/` 15 个文件、最大 293 行 | 同方向 ⇒ 外部印证 5c 走对了 |
| 测试 | **0 个测试文件** | 23 个文件 / **153** 例 + 源码守卫 | 我们更好；拆大文件没测试网等于硬拆 |
| 主壳 | `MainActivity.kt` 448 行（导航 + 主题分岔 + 4 套 Scaffold 挤一起） | `MainApp` 357 / `NavChrome` / `AppNavGraph` / `BatchBars` / `MainActivity`，各 < 400 | 我们更好 |
| 最大文件 | 1,170 行（`ModuleMiuix.kt`） | 1,705 行（`SettingsScreen.kt`） | **这一条我们更差** ⇒ #10 要解决的 |

#### 13.4 三处更正：我 09-18 晚口头汇报里说错的

1. **「把设置页的分区抽成两主题共用的 section 组件」不可行**（我当时当成 ① 的主要收益）。
   读了 `SettingsScreen.kt` 文件头 KDoc 才发现：那 4 对手写弹窗 09-16 已评估过并**刻意不并**，
   三条理由写在「照抄而非统一的地方」（坚果云弹窗硬套 `AppFormDialog` 要改字段所有权 + 加密码/说明文槽位；
   导入预览的摘要文本已经只写一份；云端/快照列表是两套行样式与角标口径）。
   两个 body 也各用自己的组件词汇（MD3：29 处 `Text` + 4 处 `SettingsNavRow` + `SegmentedButton`；
   Miuix：11 处 `ArrowPreference` + `RadioButtonPreference` + `OverlayDropdownPreference`）⇒
   **10a 只能承诺搬运，不承诺减重复**；真去重记为 10d（延后）。
2. **「设置页 Miuix 版缺主题切换/云端配置/清空入口」是我的误读**（粗提中文字面量造成的假缺口）。
   逐个核过：主题切换 1401–1410、动态取色 1421、修改账号 1489、上传云端 1516、清空库存 1549 都在，
   只是 Miuix 侧把标题传的是 `ThemeStyle.MATERIAL3.label` 这类变量、不是字面量。**没有功能缺口。**
   （唯一已知的刻意非对等仍是配色方案入口只在 MD3 分支，用户已指示暂缓。）
3. **两个数字口径错了**：① 单测我口头说 154，实为 **153**（粗 `grep @Test` 把 KDoc 里那处也算进去了，
   `doc-metrics.sh` 的词法计数早就注明这个陷阱）；② 我说 10b 要动「31 个引用文件」，那是
   `grep -l AppViewModel` 的粗口径 —— 按**「以 VM 实例为接收者」**重量一次是 **10 个文件 / 56 个站点**
   （第一次量还混进了 `data/` 里同名的仓库方法 `upsert` / `archive…` 造成假阳性，也一并修正）。
   10b 的真实成本 = 10 个文件约 **51** 行 import（禁通配导入）。

**搬包的守卫成本（实测，这是判定"不按功能分包"的依据）**：`ImeHandlingTest.dialogFiles` 按完整路径点名 +
清单里文件不存在直接红；`ScreenParityTest.screensDir()` 只遍历 `ui/screens/` 一层、且反向断言
`SettingsScreen.kt` 必须还在（搬包 = 这一屏静默失去覆盖）；`MiuixDialogContentTest` 递归扫全树（搬包无害）；
`doc-metrics.sh` 有 3 处按路径引用 VM。⇒ 7 个测试文件 + 3 处工具路径 + 一处静默失覆盖，换目录形状，不值。

#### 13.5 顺手抓到两处文档腐烂（核查第 18 / 19 处）

- **第 18 处**：#5 收官记账（提交 `a4efc45`）里写「`AppViewModel` 668」行，实测 **700** 行。
  查证：`git show` 各提交的该文件行数 —— `ceb0cf1`(5a) 656 → `e054b32`(5b) **668** → `a0a44ea`(5c-7) **700**
  （`git diff e054b32 a0a44ea` 显示 +32 行，其中 **31** 行是 5c 给 VM 加的 import）。
  ⇒ 收官记账抄的是 **5b 时**的值，而那时 5c 已经搬完了。同一份旧数在 ROADMAP 与本日志各出现 1 次与 2 次，
  **本次全部改成指针**（指向 `doc-metrics.sh` 新增的「主代码超 400 行的文件（全清单）」行），不再抄行数。
  这类数没有任何守卫看着（不是 `doc-metrics` 的比对格式），所以腐烂了 6 天没人发现。
- **第 19 处**：ROADMAP #7 与 INDEX 写「中文字面量 **583** 处」，今日复跑 **576** 处（口径未变：剥注释后提取字面量）。
  差 7 处，**未逐条定位差异来源** ⇒ 只改数、不编原因；同时把这一项**加进 `doc-metrics.sh` 的比对清单**
  （新增第 21 条守卫，正则同时吃 ROADMAP 的「含中文的字符串字面量 **N** 处」与 INDEX 的「源码中文字面量 **N** 处」两种写法），
  以后它再腐烂会当场报 ✗。

- **做阳性对照时又抓到一个失效模式（顺手修掉）**：把三行新判据的阈值临时收紧到 100 行，它们**输出空值**而不是点名超限文件。
  原因是临时副本放在仓库根，而 `doc-metrics.sh` 开头会 `cd "$(dirname "$0")/.."` ⇒ 副本在根目录时它 cd 到了**仓库上一级**，
  glob 全空、`sizes[0]` 抛 IndexError，被 `$( )` 命令替换吞掉 ⇒ 行内一片空白。这与 detekt 当年「配置没生效、报告恒为 0 条」
  同一类：**空值看着像"没问题"**。已给四行判据（含既有的 `data/` 那行）加空集兜底，现在会明说
  「X 找不到源码（glob 空集）—— 本脚本会 cd 到自己所在目录的上一级，请在仓库根目录跑」。
  重做对照（副本放进 `tools/`）：阈值 100 行时四行都点名（`data/` 9 个、屏幕本体 9 个、`viewmodel/` 2 个、全清单 **53** 个）
  ⇒ 判据不是摆设；把 glob 换成不存在的目录则报兜底文案。**教训：临时副本要放在与被测脚本同一目录**，
  否则它自己的 `cd` 会把作用域带走 —— 而"作用域被带走"的表现恰好是"零结果"，最难察觉。

#### 13.6 这一节的产出与下一步

- 落账：ROADMAP **#10**（证据 + 10a-1/10a-2/10b/10c/10d/10e + 全项验收 + 风险）、INDEX 待办新增一条、
  `doc-metrics.sh` 三行新判据 + 第 21 条数字比对、上面两处腐烂改指针。
- **顺序**：10a-1 → 10a-2 →（10b 按领域 7 个提交）→ 10c（= #6，一屏一提交）→ **一轮真机复测**（覆盖 10c 的行为改动
  + 10a-2 的 `remember` 搬家）。10a/10b 是结构搬运 ⇒ 不单独占用用户的复测时间。
- **开工前的第一步**：跑 `python3 tools/guard-mirror.py`，再**人工核**它查不到的两类断言
  （`CorruptGuardTest` 按 4 空格缩进签名抽函数体、`SnackbarCopyTest` 按全仓递归统计文案分布）。
  ⇒ 10a-1 实做时核过：`SnackbarCopyTest` **不钉设置页文案**（它的路径常量里没有 `SettingsScreen`），
  `CorruptGuardTest` 也不读设置页 ⇒ 本次这两个都不用改；会红的是 `ImeHandlingTest`（见 13.7）。

#### 13.7 10a-1 落地：弹窗区（352–986，跨 635 行）搬到同包 3 个文件（本次提交）

| 新文件 | 行数 | 原行号 | 装了什么 | 参数（= 实测引用面） |
| --- | --- | --- | --- | --- |
| `SettingsBackupDialogs.kt` | 234 | 352–520 | 导出格式选择 / 恢复来源选择 / 导入前预览与二次确认 / 清空库存确认 | 7 个（含 3 个 SAF 启动器 + `pendingImportState`） |
| `SettingsCloudDialogs.kt` | 372 | 522–828 | 坚果云账号（账号 + 密码两个输入框）/ 云端备份选择 / 恢复二次确认 | 4 个 |
| `SettingsSnapshotDialogs.kt` | 211 | 830–986 | 本地历史快照列表（含还原确认） | 3 个 |

`SettingsScreen.kt`：1,705 → **1,079** 行。账按 `git diff --numstat` 记（−658 / +32）：删的 658 = 弹窗区内 632 行
+ 失效 import 24 行 + 被改写的 2 行（`var pendingImport by remember { … }`、头 KDoc 里核查第 20 处那句）；
加的 32 = 3 处调用 17 行 + `pendingImportState` 声明 2 行 + 行内注释 3 行 + 头 KDoc 10 行
（#10a-1 落点 6 + 核查第 20 处勘误 4）。⚠️ 三块跨度是 169 / 307 / 157（合计 633），比 git 记的 632 多 1 ——
区域里 3 行单独的 `)`（旧行 409 / 828 / 986）被最小 diff 与文件里同样的 `)` 配成了「未变上下文」，
不是留在原地的代码；所以**搬走的量按跨度说、删掉的量按 git 说**，两个数别互相套用。
⚠️ 我一度按「1,070」记账 —— 那是 KDoc 批注**之前**的数，`doc-metrics.sh` 现报 1,079；**又一次抄旧数**，
同类事故见核查第 18 处（AppViewModel 668 → 700）。教训照旧：行数只从 `bash tools/doc-metrics.sh` 取。
**仍超 400** —— 两套 body（385 + 185）与 `SettingsNavRow`(45) / `PaletteSwatch`(92) 还在里面，那是 10a-2。

**搬运纪律（照 #5c 的形状，但这次是「抽子 Composable」而非扩展函数）**：脚本化 + 三道自校验，任一不过就不落盘 ——
① 搬走的每一行都必须在新文件里逐字找到（只允许整体左移 4 空格）：三块跨度 169 / 307 / 157 行、**0 行找不到**；
② 参数表与「以入口局部为接收者的实测引用面」逐个比对（多一个少一个都不落盘）；
③ 落盘前查 ktlint 开着的 6 条（连续空行、右花括号前空行、行尾空白、末尾恰好一个换行）。
`kt-lexcheck` 复检 96 份 **0 问题**：三个新文件的 import 一个不多一个不少（⚠️ **核查第 22 处：这半句不成立** ——
「原文件 import ∩ 块内出现的符号」这个算法对**别名 import** 是瞎的，当轮 CI 就红了 24 处 `Unresolved reference`，
见 §13.8；「0 问题」只说明当时的判据 1–3 没意见，不说明 import 齐。另外补了代码里不出现名字但必须有的
`@Composable`、`by` 委托的 `getValue`/`setValue`、
以及原文件从未写出名字的 `MutableState` 与 `ActivityResultLauncher` —— 后两个原来靠类型推断）。

**本次我犯的 2 处错（都在本轮内发现并改正）**：
1. 删失效 import 时按**无别名形式**精确匹配（`import top.yukonga.miuix.kmp.basic.Text`），
   而文件里是带别名的 `… as MiuixText` ⇒ 断言「清单里的 import 找不到」当场拦住、没写盘（脚本先断言后写，
   所以没有半改状态）；改成按**路径前缀**匹配才对。教训：搬家的收尾清理也要按"路径"而不是"字面全等"匹配。
2. 规划里写「`SettingsActions` 已覆盖 19 个动作，可直接当参数包」—— **实测不成立**：弹窗区用的是入口的
   局部回调（`saveNutstore` / `confirmImport` / `confirmRestore` / `confirmSnapshotRestore`）与 3 个 SAF 启动器，
   一次都没经 `SettingsActions`。已按实测的 10 个参数落地，并把 ROADMAP 那句改掉（**方案里的接口名是想出来的、
   不是量出来的** —— 与 #5c 那次「方案里的函数名是编造的」同一类错）。

**唯一一处非逐字改动（2 行，已在 ROADMAP 记账）**：`pendingImport` 在弹窗区里被**赋值** 4 次
（导入预览的关闭/取消），所以入口那行 `var pendingImport by remember { mutableStateOf<PendingImport?>(null) }`
拆成「显式 `val pendingImportState = remember { … }` + 同一行 `var pendingImport by pendingImportState`」，
把 State 对象传给弹窗。读写的是同一个 `MutableState`，语义不变；变化只有"读它的重组作用域从入口挪到弹窗文件"
（该状态本来只有弹窗在读，入口只在导入启动器的回调里写）。

**守卫同批改了 6 处**：前 5 处是规划里点名过的（清单 + KDoc），第 6 处**规划时没想到、是真断言改动**。
`ImeHandlingTest.dialogFiles`
（`SettingsScreen.kt` → `SettingsCloudDialogs.kt`；第 3 条是**逐文件要求含** `decorFitsSystemWindows = false` + 避让，
不改就红 —— 搬走后入口文件里这两个串都没了）+ 它两处 KDoc 的 MIME 行号（`SettingsScreen.kt:379` →
`SettingsBackupDialogs.kt:106`，并注明拆分后 MIME 字面量与避让代码已不同文件、`codeOnly()` 吞不到一起，
但谓词仍按赋值形态匹配、不改）；`MiuixDialogContentTest` 的调用点分布（**总数仍 8**：设置页那 4 个变成
1 + 2 + 1 分布在三个新文件；它递归扫全树，所以不会红，只有 KDoc 的分布说明要改）与「覆盖导入」那处引用；
`ScreenParityTest` 第 8 对的注释补一句（`MergedScreens` 里的 `SettingsScreen.kt` **不动** ——
屏幕入口还在原地、`rememberSettingsUiState(` 也还在里面，规则 1 与反向断言都靠它）。

**第 6 处（真断言改动）：`ScreenParityTest` 规则 2 的扫描面跟着加宽。** 它的 `screenFiles()` 只收
`*Screen.kt` / `*Screens.kt` ⇒ 我这三个 `*Dialogs.kt` **不进清单**（规则 1 因此不受影响：弹窗文件的 `state`
是传参进来的，本就不该自己去 remember）。但规则 2「禁止内联聚合」（`sumOf {` / `groupBy {` / `count {` /
`.sortedByDescending`）原来也只扫屏幕文件 —— 搬完之后那 800 多行 UI 代码就**静默逃出**了这条守卫：
不响 ≠ 没问题，正是这个测试自己在 KDoc 里写的失效方式。所以加了 `uiFiles()`（目录内除 `*State.kt`
的所有 `.kt`）只给规则 2 用。**加宽前先量过判定结果会不会变**：那 4 个禁用模式在 `ui/screens/` 的命中
**全在 `*State.kt`**（状态层的纯函数，本就该在那儿算），非 State 文件 0 命中 ⇒ 加宽只补覆盖、不改判定。
这类「搬家把守卫的覆盖面搬窄了」比「搬家把守卫搬红了」危险得多 —— 红的会逼你去看，窄的什么都不说。

**本地检查（三项，全绿）**：`kt-lexcheck` 96 份 0 问题 · `guard-mirror` 3 个测试文件 0 问题（作用域受限跳过
18 处由 CI 覆盖）· `doc-metrics` 各行照跑。**编译与单测仍只能靠 CI**（沙箱无 JDK）。
另外把两个守卫的断言按源码文本模拟核过一遍（本地没 JVM 跑不了 JUnit）：`ImeHandlingTest` 第 3 条点名的
3 个文件都同时含 `decorFitsSystemWindows = false` 与避让赋值形态 ✓；`MiuixDialogContentTest` 的
`MiuixDialog(` 调用点剥注释后仍是 8 个（App 级 4 + Backup 1 + Cloud 2 + Snapshot 1；入口头 KDoc 里那处
`MiuixDialog(show = true)` 在注释里，词法状态机会跳过）✓。

**本次我犯的错还有 2 处（都在本轮内发现）**：
3. **又抄了旧数**：搬运刚结束时量到入口 1,070 行，就先按这个数写进了 ROADMAP / INDEX / devlog；
   但同一批的守卫脚本又给文件头加了 9 行 KDoc（#10a-1 落点 + 核查第 20 处勘误）⇒ 真值是 **1,079**。
   是 `bash tools/doc-metrics.sh` 报出 1,079 才发现的。改法：一律按 `git diff --numstat` 的 −658 / +32 记账，
   现值只从 doc-metrics 取。与核查第 18 处（`AppViewModel` 668 → 700）同一形态 —— **收官记账抄旧数是复发事故**。
4. **重复造轮子 + 假阳性**：想在本地跑 ktlint（CI 的静态门禁之一），发现官方 release 资产端点
   `release-assets.githubusercontent.com` 在本沙箱 SSL 握手失败（curl 35）⇒ 拿不到原生二进制，
   于是写了个临时脚本模拟 `.editorconfig` 里实开的 6 条规则。写完才想起 **`tools/kt-lexcheck.py` 早就查这 5 条**
   （它文件头第 2 条写明就是 ktlint 那 6 条里的 5 条，还带 12 个 canary 自检）。而且我那版口径有两处假阳性：
   ① 把 `${a.b}` 这种属性链也当冗余花括号（真 ktlint 不报：`MainApp.kt:210` 的 `${ids.size}` 在 CI 上是绿的）；
   ② 少了 kt-lexcheck 那个「`}` 后紧跟标识符字符 ⇒ 花括号必需」的判断
   （`RepositoryCore.kt:192` 的 `${READ_RETRY_DELAY_MS}ms` 就属这种）—— 结果在未改动文件上报了 70 条。
   临时脚本已删（不进仓库）。教训两条：**写临时检查器前先读现有工具的文件头**；任何「模拟门禁」的结论，
   都要先在未改动文件上跑出 0（基线对照）+ 在合成坏例上跑出命中（canary 对照），否则那个「0 条」毫无意义
   —— 本次两路对照都做了，才有资格说改动文件干净。

**核查第 20 处（本次顺手抓到）**：`SettingsScreen.kt` 文件头 KDoc「三处非等价改动」的第③条写
`onUpload` / `onCloudRestore`「供 body 与「恢复来源」弹窗共用」—— 弹窗区 352–986 里这两个名字 **0 次出现**
（恢复来源弹窗直接调 `state.setShowRestoreSourceDialog` / `state.loadLocalSnapshots()`），
只有两版 body 的「上传云端」「云端恢复」两行在用。已改成「供两版 body 的备份节共用」并就地标注。

**文档同步**：ROADMAP #10（标题状态、证据行现值、10a-1 行改实况、新增「10a-1 落地结果」表）、
INDEX #10 条与 MIME 待办行、`DESIGN_SPEC` 两处（粘性避让的例子文件名 + 口径行加「这三个数是 09-17 快照」批注）、
`MIUIX_UPGRADE` §2 第 8 条的例子文件名、`doc-metrics.sh` 两行标签改「屏幕目录 ui/screens」
（#10a 起该目录含弹窗/body 文件，**文件数增加不等于新增屏幕**）。

#### 13.8 10a-1 修红：**别名 import 是搬运脚本的盲区**（CI 红一轮 + `kt-lexcheck` 新增判据 4）

**CI 报什么**（run [35322713982](https://github.com/SkyForest233/chileme/actions/runs/35322713982)，head `c20458a`）：
静态门禁 ✅，`debug` 与 `release` 两个构建 job 都红在 Kotlin 编译（`BUILD FAILED in 38s`）。
错误 28 条 = **24 条 `Unresolved reference`**（`MiuixText` / `MiuixTextButton` / `MiuixSurface` / `MiuixIcon`）
+ **4 条连带的**「`@Composable` invocations can only happen from the context of a `@Composable` function」
（都在未解析的 `MiuixSurface {}` 尾随 lambda 里 —— 编译器认不出那是个 Composable，就把它内部的调用一并判违规）。
分布：`SettingsBackupDialogs.kt` 2 · `SettingsCloudDialogs.kt` 15 · `SettingsSnapshotDialogs.kt` 7。
两个 upload 步骤都 warn「No files were found」（没有测试报告、没有 lint 报告）⇒ 红在编译阶段、单测根本没起来，
与 38 秒的耗时自洽。

**怎么拿到日志的**：`gh run view --log-failed` 与 `gh api …/jobs/<id>/logs` 依旧指向
`results-receiver.actions.githubusercontent.com`（EOF），沙箱 `curl` 直连 Azure blob 也被墙（`SSL_ERROR_SYSCALL`）——
**但网页抓取工具能读**：用户从 Actions 页面复制的 job 日志签名 URL
（`productionresultssa7.blob.core.windows.net/…/job-logs.txt?sig=…`，约 10 分钟时效）可以直接取到全文。
⇒ 已写进 `WORKFLOW.md` §3：**CI 红了先请用户给日志链接，别先按排除法猜**。
本轮拿到日志前我猜了 5 个方向（被删的 24 条 import 是否仍在用 / 同包重复声明 / 入口的 `private` 顶层 /
`@OptIn` 是否漏带 / 3 个调用点的参数名·顺序·类型），**5 个全不中**；日志到手 30 秒定位。
（那次 `@OptIn` 排查还有个自坑：正则 `@OptIn\([A-Za-z:,\. ]*\)` 的字符类漏了数字，`ExperimentalMaterial3Api`
里的 `3` 匹不上 ⇒ 整张表把有注解的文件全显示成「无 @OptIn」。当场自查发现，没让它进任何结论。）

**根因**：入口里这些符号是**带 `as` 别名**的 —— `import top.yukonga.miuix.kmp.basic.Text as MiuixText`
（同类还有 `Surface as MiuixSurface`、`TextButton as MiuixTextButton`；`Icon as MiuixIcon` 入口自己仍在用，没被删）。
搬运脚本算依赖用的是「简单名 = import 路径最后一段」，别名那一层不在表里 ⇒ 给新文件补 import 时，
代码里写的是 `MiuixText`、表里只有 `Text`，一条也补不上；而入口侧「删失效 import」按名字是否还出现判断，
`MiuixText` 在入口已 0 次出现 ⇒ 删得对。**两侧各自都对，合起来就是漏。**
全仓这类别名 import 共 **25** 条（全在 `top.yukonga.miuix.kmp.basic.*`，是双主题组件层的产物）。

⚠️ 同一个盲区本轮**出现两次**：§13.7「我犯的 2 处错」第 1 条正是删除侧按无别名形式精确匹配、被脚本断言当场拦住
（那次有断言兜着）；添加侧没有对应断言 ⇒ 一路漏到 CI。**教训：别名是"第三个名字"** ——
一个符号在本仓可以有路径名（`basic.Text`）、别名（`MiuixText`）、还可能是项目自己的同名声明
（`MiuixDialog` 就是我们自己的函数、不是别名），任何按名字算依赖的脚本必须三者一起收。

**修法（只加 9 行 import，别的没动）**：`git diff --stat` = 3 文件 / **9 insertions(+) / 0 deletions(-)**。
`SettingsBackupDialogs.kt` +1（`TextButton as MiuixTextButton`）· `SettingsCloudDialogs.kt` +4 ·
`SettingsSnapshotDialogs.kt` +4。落点按既有惯例放在 `basic.*` 组内字典序位置
（本仓 import 顺序**无门禁**：`.editorconfig` 里 `ktlint_standard = disabled`、未开 `import-ordering`）。
脚本自带 4 条断言：别名在代码里用量 > 0（否则补进去就成了未用 import，会撞判据 3）、路径不重复、
插入后 `basic.*` 组仍字典序、文件末尾仍恰好一个换行。9 条覆盖 **24** 处引用 ⇒ 与 CI 的 24 条 Unresolved 逐条对上。

**新守卫：`tools/kt-lexcheck.py` 判据 4「用了别名却没 import」**（判据 3「未使用的 import」的镜像）：

- 别名表由 `build_alias_corpus()` **扫全仓**建（现 25 个），刻意不受命令行 targets 影响 ——
  否则 `python3 tools/kt-lexcheck.py 某个文件.kt` 这种最常用法下表是空的、检查会静默失效；
  运行时第一行打印「别名表：25 个」，表空则打印 ⚠️（零结果必须能自证探测在工作，本仓规矩）。
- 用量按**词法**数（`code` 已剥注释与字符串）⇒ KDoc 里的 `[MiuixText]`、测试里的字面量 `"MiuixText("` 都不算；
  本文件自己声明的同名顶层与已 import 的都放行。
- 自检对照 **12 → 15**（新增 1 报 + 2 不报），`--selftest` 15/15 ✓。
- **端到端阴性对照**：把修复前（`HEAD`）的三个文件导出来喂给新判据 ⇒ 正好点名 9 处、次数 2/15/7 与 CI 一致。
  这条对照做过，才有资格说「现在全仓 96 份 0 问题」这个零是真的。
- ⚠️ 已知盲区写进工具文件头：某个别名若同时又是项目自己的顶层声明名，会漏报（本仓 25 个别名与项目顶层声明零交集）。

**为什么不做成 CI 里的 Kotlin 测试**：本地判据 4 在 push 前几毫秒就能抓到这类错，而 CI 侧本来就抓得到
（这次就是 CI 抓的，代价是一轮 4.5 分钟 + 请用户取日志）；再写一个本地跑不了（无 JDK）、
自己可能写错的源码文本测试去防一个已有两道防线的错 ⇒ 判定不做。

**核查第 22 处（本次自查，改的是本轮自己写下的话）**：§13.7 里「`kt-lexcheck` 复检 96 份 0 问题」后面那句
「（说明「原文件 import ∩ 块内出现的符号」这个算法够用）」—— **不成立**：那个算法对别名是瞎的，
「0 问题」只说明判据 1–3 没意见，不说明 import 齐。已在原句就地标注。
`detekt.yml:167` 的「含 12 个内建自检对照」也一并改成 15（那是**现状**陈述，不是历史记录；
台账里 `dec3a0e` 那行的「12 个」是当时的实况，按"日志不改写"的规矩**不动**）。

**文档同步**：ROADMAP #10（标题状态、10a-1 落地结果补修红条、风险汇总补这条已兑现的风险）、
INDEX #10 行的指针、`WORKFLOW.md` §3 拿日志的办法 + §4 的 `Unresolved reference` 行、`detekt.yml` 的对照数、
本节与台账两行。

---

## 🔍 本轮的验收依据（本地无编译器，逐条列清楚）

| 判据 | 结果 | 依据 |
| --- | --- | --- |
| 编译 + 单测（4a 及之前 4 个提交） | ✅ | CI run [35293564571](https://github.com/SkyForest233/chileme/actions/runs/35293564571)，3 个 job 全 success，4 分 20 秒。`build.yml:45` 是 `assembleDebug testDebugUnitTest lintDebug` **同一步** ⇒ job 绿即单测全过 |
| 编译 + 单测（devlog 提交 `f6428fa`） | ✅ | CI run [35294192604](https://github.com/SkyForest233/chileme/actions/runs/35294192604)，`gh run view` 取到 conclusion=success |
| 编译 + 单测（**4b** `265193f`） | ✅（依据较弱，见下） | CI run [35294881512](https://github.com/SkyForest233/chileme/actions/runs/35294881512)：`gh run watch --exit-status` **退出 0**（该 flag 只在 run 成功时返回 0），日志里有「✓ 静态门禁 in 15s」与门禁阳性对照「命中 2 条已知违规（exit=2）」；**但紧接着令牌失效，job 级 conclusion 没能取到**。09-18 晚令牌恢复后补取：run [35295354068](https://github.com/SkyForest233/chileme/actions/runs/35295354068)（head `5178cee`，含 4b 全部代码）三 job 全 success ⇒ **这一格的依据已足，不再是"较弱"** |
| ktlint / detekt | ✅ 0 发现 | 三次 run 的「静态门禁」job；`ci-gates.sh` 自带阳性对照（故意命中 2 条已知违规，`exit=2` 证明规则真在跑） |
| 词法预检 | ✅ 82/82 | `python3 tools/kt-lexcheck.py`（4b 后含新测试文件）；`--selftest` 12/12 |
| 文档数字 | ✅ 16 处比对、不符 0 | `bash tools/doc-metrics.sh`（含新交接的守卫） |
| 单测数只增不减 | ✅ 121 → 127 → **135** | 同上脚本的"单测数（剥注释后计数）" |
| 4b 守卫自身有效性 | ✅ 37 条断言镜像全过 + 3 种变异全红 | 见 §8 末段（本地无编译器，只能镜像 + 变异证伪） |
| 编译 + 单测（**4c** `a6b67d7`） | ❌ **红过一次** | run [35296284948](https://github.com/SkyForest233/chileme/actions/runs/35296284948)：静态门禁 ✅ / release+R8 ✅ / 「debug APK + 单测 + lintDebug」❌。根因与定位过程见 §10 |
| 编译 + 单测（**4c 修复** `2fd6089`） | ✅ | run [35297362614](https://github.com/SkyForest233/chileme/actions/runs/35297362614)：**三 job 全 success**（含 `lintDebug` —— 那次红的时候 lint 报告压根没生成，这次生成了且无 error） |
| 词法预检（4c 后） | ✅ **83**/83 | `python3 tools/kt-lexcheck.py`（新增 `OpFailureTest.kt`）；`--selftest` 12/12 |
| 文档数字（4c 后） | ✅ 16 处比对、不符 **0** | `bash tools/doc-metrics.sh`。改 ROADMAP **之前**是 1 处不符（文档写 `Channel<` 3、实测 4）⇒ 那正是守卫在起作用，不是误报 |
| 单测数只增不减 | ✅ 121 → 127 → 135 → **143** | 同上脚本（剥注释后计数） |
| 4c 守卫自身有效性 | ✅ 4 份守卫镜像全过 + 队列守卫两方向反证 | `CorruptGuardTest` 9 条、`UiEventTest` 源码文本 5 条、`SnackbarCopyTest` 8 例 36 条、`ScreenParityTest` 4 例；`doc-metrics` 那条改成从 `UiSurface` 反推后，加/删落点各反证一次（见 §9） |

**没能验证的**：CI 的日志与 artifact 端点（`results-receiver` / `blob.core.windows.net`）在本沙箱被网络掐断
（`EOF`），所以**拿不到测试报告里的实际用例数**（⚠️ 09-18 晚补充：**job 全文日志有别的路子** ——
请用户从 Actions 页面复制签名 URL、再用网页抓取工具读，见 §13.8 与 `WORKFLOW.md` §3；测试报告 artifact 仍拿不到），127 是本地按同一套源码词法计数的结果；
"单测全过"的依据是 `build.yml:45` 的步骤构成 + job 结论，不是报告本身。4c 之后同理：**143** 也是本地词法计数的结果（新增 `OpFailureTest` 6 例 + `UiEventTest` 2 例）。⚠️ 4c 那次红还多证明了一件事：产物端点同样被墙（`gh run download -n test-report` 也是 `EOF`），所以**连测试报告 XML 都拿不到** —— 这才有了 §10 那套"把守卫镜像成脚本"的定位办法。

**⚠️ 本轮结束时 GitHub 令牌失效（环境事实，不是代码问题）**：4b 那次 CI（run 35294881512）
`gh run watch --exit-status` 已经返回 0（= run 成功），**紧接着**同一条命令里的 `gh run view` 就报
`HTTP 401: Bad credentials`，`gh auth status` 显示「The github.com token in GH_TOKEN is no longer valid」，
`git push --dry-run` 报 `could not read Username for 'https://github.com'`。影响与处置：

- 4b 的**逐 job 结论没能取到**，只有 watch 的退出码 + 门禁 job 的 ✓ 与阳性对照注解 ⇒ 上表把这一格标成"依据较弱"，
  不冒充成与前两次同等的证据；
- ~~4b 之后的文档提交（ROADMAP/INDEX/本节）**推不出去**，只在本地提交里；等 GitHub 重连后一次推送~~ **已解决**：重连后以 `5178cee` 推送成功，run 35295354068 三 job 全绿；
- **不再继续写 4c/#5 的代码**：本仓的验收口径是"CI 是唯一编译裁决"，用户此前也明确拒绝过"本地写、不过 CI"
  的做法 ⇒ 在没有 CI 的情况下堆改动，等于把风险攒到下一次一起爆。

**另一个诚实的口径**：4a 那批 4 个提交是**一次推送**，CI 只对 head `8f75184` 跑了一次 ⇒
中间三个提交（`dec3a0e` / `fe0f527` / `f4cd9a4`）**没有各自单独过 CI**。它们的全部编译内容都包含在 head 里
（`dec3a0e` 只是 python 工具，不参与编译），所以"能编译、单测全过"这个结论覆盖到它们的代码；
但**逐个提交可构建（bisect 友好）没有被验证**。上一轮用户明确要求过"每对屏幕各自过 CI"，
本轮是单个逻辑改动 + 工具 + 文档，故按一次推送处理，在此记明差异。

---

## ✅ 真机复测（本轮唯一的行为风险面）—— 2026-09-18 用户实机**已通过**

> **结果（2026-09-18，用户实机复测）**：两主题 × 下面两张表（4a/4b 的四处提示 + 4c 的设置页 5 条流程）
> **全部通过、无问题** ⇒ #4 的验收 ③「提示的出现位置与文案逐条不变」从"机制上成立"升级为**实机确认**。
> 下面两张表保留原样，作为下一次碰这块代码时的复测清单。

代码层面是纯搬运，但"提示条"这类东西只有装上才知道。**两主题 × 四处提示**，每处确认 4 件事：

| 触发路径 | 提示文案（应逐字不变） | 落点 |
| --- | --- | --- |
| 列表页步进器减号 | 已减少一件并计入消耗 | 主壳覆盖层 |
| 列表页搜索里恢复归档 | 已恢复「××」到零食柜 / 库存中已有同批次「××」，已合并数量 | 主壳覆盖层 |
| 消耗记录页删除一条记录 | 已删除「××」的消耗记录 | 该页 `AppScaffold` |
| 启动时自动同步成功 | 已自动同步到坚果云 ☁️ | 首页 `AppScaffold` |

每处要看的：① 提示出现在**原来那个位置**（主壳是"动画偏移 + `imePadding`"的自定义落位，二级页在 scaffold 内）；
② 点「撤销」**真的回滚**；③ **旋屏一次不重复弹**；④ 停在别的 Tab 时触发的事件，**回到该页才弹**
（队列语义，与旧的可空 StateFlow 行为一致）。
**4c 追加的复测面（设置页，两主题 × 5 条流程）**：

| 触发路径 | 提示文案（应逐字不变） | 落点 |
| --- | --- | --- |
| 设置页「立即上传」成功 | 已上传到坚果云 ☁️ | 设置页 `AppScaffold` |
| 设置页「拉取云端备份」但云端是空的 | 云端暂无备份，请先上传 | 设置页（**并关掉选择器**） |
| 从云端备份恢复成功 | 已从坚果云恢复数据 ✅（已自动留存恢复前快照） | 设置页 |
| 从本地快照还原成功 | 已从本地快照还原数据 ✅（已自动留存还原前快照） | 设置页 |
| 导入 JSON 备份成功 | 导入成功，数据已恢复 ✅（已自动留存导入前快照） | 设置页 |

每条要看的：① 提示落在**设置页自己的宿主**（不是首页、也不是主壳覆盖层）；② 文案与上表一致；
③ **只有「拉列表失败 / 列表为空」会关掉备份选择器**，上传 / 下载 / 还原失败时选择器不动；
④ 未填凭据就点上传 / 拉列表 / 下载 ⇒ 提示「请先填写并保存坚果云账号和应用密码」；
⑤ 前置快照那半句会随实际结果变化（存下了 / 未能保存），两种都属正常。
⚠️ 网络类失败仍会显示 OkHttp 的**英文技术串**（如 `Unable to resolve host …`）—— 改造前就这样、本轮刻意未改，
真机看到它**不算回归**；要不要换成中文见「📋 本轮之后的待办变化」里的待决策项。

安装注意：签名不同可能要先卸载 ⇒ **先导出 JSON 备份**（既有约定）。

---

## 📋 本轮之后的待办变化

- **#4**：✅ **全项收官** —— 4a ✅ · 4b ✅（§8）· **4c ✅（§9）**。
  4c 的实际范围也与原计划不同：计划 3 个回调、实测 **5** 个（核查第 15 处）；4b 的原计划"合并收集端"做不到（§8）。
  验收 ①②④⑤ 已达成，③（提示位置与文案逐条不变）机制上成立但**仍需真机过一轮**。
- **#5**：未动。5a/5b/5c 的计划与证据行已在 ROADMAP（跨零点逻辑**早已可注入且已被测**，只剩 12 处硬调 `now()`）。
- **新增待评估**：detekt 的 `UnusedImports` 要不要开（需单独一轮误报面调查，见 §5）。
- ~~**待用户动作**：① 文末两张复测表；② 两个待决策项~~ ⇒ **两项都已完成**（2026-09-18）：
  复测**通过**、两个决策都选了"保持现状"，详见 §11。
- **4c 查出的两件事 ⇒ 用户已决（2026-09-18，都选"保持现状"，见 §11）**：
  - **网络类失败的英文技术串**（`Unable to resolve host …` 这类）：**不改**。
    `OpFailureTest` 里钉住现状的那条断言因此长期有效 —— 它的作用从"等着被改"变成"防止有人无意改掉"。
  - `saveLocalSnapshot(onDone: ((Boolean) -> Unit)?)`：接口 / 包装 / 实现 / VM 四层管道齐全，
    但**全仓没有任何调用点**（`grep` 实证）⇒ 疑似死 API（**核查第 16 处**：不是文档错，是代码里的死管道）。
    **留着不删** ⇒ 从此它是一处「已知的死代码」，登记在此，免得以后有人（包括我）当成遗漏再查一遍。

---

## 💡 决策记录

1. **`Channel(BUFFERED)` 而非 `SharedFlow(replay=0)`**：切主题会销毁并重建 `MainApp` 整棵组合，
   `replay=0` 会在这个瞬间丢事件；`Channel` 排队，切换完照弹。
2. **三条队列而非一条**：`Channel` 是单接收方语义，多收集协程会互相抢（详见 §2）。
3. **发送端一处、接收端三处**：`emit()` 按 `UiEvent.surface` 分流是唯一的发送点（可断言）；
   接收端必须与宿主同生命周期，所以是三处。
4. **事件载荷是数据不是回调**：回调会捕获发出时那个界面的宿主对象，跨主题/跨页面即失效；
   而且"跨主题宿主对象不得漏回屏幕层"是本仓既有架构约束（`docs/ARCHITECTURE.md:157`）。
5. **不启用 detekt `UnusedImports`**：门禁调整要单独一轮，本地检查器已覆盖。
6. **守卫随重构交接，不留两套**：临时守卫在同一次提交里换成正式守卫（§6a）。
7. **历史数字不加粗、现值加粗**：让"会被断言的数"与"只是历史记录的数"在排版上就能区分（§6b）。
8. **4b 用自由函数而不是让主壳复用 `AppSnackbarHostState`**（§8）：容器带 `remember(isMiuix)`，
   切主题换宿主，会把主壳那条 `LaunchedEffect(Unit)` 协程堵死在没渲染的宿主上。**宿主身份稳定性 > 抽象统一性**。
9. **helper 用 `internal` 而不是 `public`**：只有主壳需要它，`internal` = 只给本模块，不进公开 API 面；
   二级页继续走 `AppSnackbarHostState`，两条路各有各的道理，不强行合并。
10. **失败分三档而不是四档**（§9）：不给 HTTP 状态码单列一档 —— 状态码本来就在文案里、用户看得见，
    而 UI 目前对 507 与 500 没有任何不同处理。为一档没人区分的情况加类型，只是让 `when` 多一个分支。
11. **`CloudBackupsEmpty` 必须单独一档**：改造前 `onResult(false, "云端暂无备份…")` 把"成功但没东西"
    塞进了失败那一档；天真地换成 `Result` 会让界面把"云端是空的"当错误处理。**语义被压平的地方要拆开，
    不能只换载体**。
12. **`syncUpload` 的本地打包失败显式用 `OpFailure.Other`，不走 `toOpFailure()`**：结果虽同，
    但显式写才表达得出"这是本地数据问题、不是网络问题"；把本地错误分类成网络错会误导用户去重试。
13. **改名趁早**（§10 #2）：`SyncFailure`/`SyncOp` → `OpFailure`/`DataOp` 在动手当天就改了 ——
    "导入备份"与"快照损坏"都不是同步操作，装不进那个名字；等到引用铺开再改，成本与风险都大得多。

---

## 🔗 本日提交与 CI 台账

> 数据取自 `git log` 与 `gh run list/view`，非凭记忆。本轮 4 个提交一次推送 ⇒ 只有 1 个 CI run（针对 head）。

| 提交 | 内容 | CI run | 结果 |
| --- | --- | --- | --- |
| `dec3a0e` | §4 词法预检器入库 `tools/kt-lexcheck.py`（12 个内建自检对照；含两个假对照的改正与反证） | [35293564571](https://github.com/SkyForest233/chileme/actions/runs/35293564571)（对 head） | ✅ |
| `fe0f527` | §5 删死 import + 改正「detekt 兜底未使用 import」这个错 claim（核查第 14 处） | 同上 | ✅ |
| `f4cd9a4` | §2 **#4a**：4 个「可空 StateFlow + 手工 consume」→ `UiEvent` + `Channel`（3 条队列）；§3 新增 `UiEventTest`（121 → 127） | 同上 | ✅ |
| `8f75184` | §6 文档同步 + 守卫交接 + 日期纠正 22 处 | [35293564571](https://github.com/SkyForest233/chileme/actions/runs/35293564571)（head） | ✅ 3 job 全绿，4 分 20 秒 |
| `f6428fa` | 本节日志第一版（§1–§7）+ INDEX 行 | [35294192604](https://github.com/SkyForest233/chileme/actions/runs/35294192604) | ✅ conclusion=success |
| `265193f` | §8 **#4b**：三处分流收成 `showUndoSnackbarAcrossThemes()` + 新增 `SnackbarCopyTest`（127 → 135） | [35294881512](https://github.com/SkyForest233/chileme/actions/runs/35294881512) | ✅ watch `--exit-status` 退出 0（job 级结论因令牌失效未取到） |
| `5178cee` | 4b 的文档同步（ROADMAP 4b 行与「4b 落地结果」、INDEX #4 条、本节日志补 §8）—— 令牌恢复后补推 | [35295354068](https://github.com/SkyForest233/chileme/actions/runs/35295354068) | ✅ **三 job 全 success**（逐 job 结论已补取） |
| `a6b67d7` | §9 **#4c**：5 个回调 → `OpFailure` + `UiEvent`（第 4 条队列、`OpFailed`/`CloudBackupsEmpty`/`DataOp`）；`OpFailureTest` 6 例（135 → 143）；`doc-metrics` 队列守卫改成从 `UiSurface` 反推 | [35296284948](https://github.com/SkyForest233/chileme/actions/runs/35296284948) | ❌ 门禁 ✅ / release ✅ / **单测 ❌**（根因见 §10 #6） |
| `2fd6089` | §10 #6 的修复：补回被 KDoc 重写弄丢的阳性对照样本（纯注释改动） | [35297362614](https://github.com/SkyForest233/chileme/actions/runs/35297362614) | ✅ **三 job 全 success** |
| 本次补记（§9 + §10 + 验收依据 + 复测 + 台账） | ROADMAP 8 处（含 4c 行、证据行修正、`Channel<` 3→4）+ 本节 | [35298314128](https://github.com/SkyForest233/chileme/actions/runs/35298314128)（head `65a28e9`） | ✅ |
| `d76c8d0` | §11 #4 收官：用户实机复测通过、两个待决策项已决（都保持现状）、记下环境抽风两次 | [35300225688](https://github.com/SkyForest233/chileme/actions/runs/35300225688) | ✅ |
| `ceb0cf1` | §12.1 **#5a**：`ChiliMeApp` + `AppContainer`（`Application` 子类 0 → 1，仓库现场构造归零） | [35300747824](https://github.com/SkyForest233/chileme/actions/runs/35300747824) | ✅ |
| `e054b32` | §12.2 **#5b**：时钟注入（数据层 9 + VM 5 处硬调归零、14 处已注入；+10 例固定时钟单测，143 → **153**） | [35302372909](https://github.com/SkyForest233/chileme/actions/runs/35302372909) | ✅ |
| `d784a33` | §12.3 **#5c-1**：共用底座 → `RepositoryCore.kt`（965 → 772） | [35304943106](https://github.com/SkyForest233/chileme/actions/runs/35304943106) | ✅ |
| `1f5effc` | **#5c-2**：库存 → `FoodItems.kt`（772 → 686）；`CorruptGuardTest` 的 `upsert` 条跟着搬 | [35305533342](https://github.com/SkyForest233/chileme/actions/runs/35305533342) | ✅ |
| `a80ea11` | **#5c-3**：归档 → `FoodArchive.kt`（686 → 601） | [35305914530](https://github.com/SkyForest233/chileme/actions/runs/35305914530) | ✅ |
| `3df805a` | **#5c-4**：消耗 → `FoodConsumption.kt`（601 → 444）；顺带修边界脚本"啃掉下一个函数"的 bug | [35306543127](https://github.com/SkyForest233/chileme/actions/runs/35306543127) | ❌ 编译失败：`compactConsumption` 搬成没有接收者的顶层函数 |
| `daca520` | 修 ①：补上接收者 + 脚本改成按行号改写 + 两条新断言 | [35306818189](https://github.com/SkyForest233/chileme/actions/runs/35306818189) | ❌ 单测失败：守卫仍指旧文件 |
| `b83c22e` | 修 ②：`CompactConsumptionTest` 改读 `FoodConsumption.kt` | [35307347145](https://github.com/SkyForest233/chileme/actions/runs/35307347145) | ✅ |
| `205d046` | **#5c-5**：备份/导出/清空 → `FoodBackup.kt`（443 → 336）；清 2 条孤儿段标题 + 4 个孤儿 import | [35307866156](https://github.com/SkyForest233/chileme/actions/runs/35307866156) | ✅（令牌随后失效，结论于恢复后补查） |
| `202f12a` | **#5c-6**：设置写入 → `FoodSettings.kt`（336 → 283）—— 沙箱重建后按差集核对重新提交 | [35309378448](https://github.com/SkyForest233/chileme/actions/runs/35309378448) | ✅ |
| `0f39d1f` | `tools/guard-mirror.py` 入库（源码文本守卫镜像；含分极性、按 @Test 分块、作用域受限跳过三条规则） | 与 `a0a44ea` 同推，无独立 run | — |
| `a0a44ea` | **#5c-7**：凭据 → `FoodCredentials.kt`（283 → **252**）；`if (enc != null)` 计数断言跟着搬 | [35310028492](https://github.com/SkyForest233/chileme/actions/runs/35310028492) | ✅ |
| 本次补记（§12 + ROADMAP/INDEX 收官同步 + doc-metrics 三行与 `data/` < 400 行判据） | ROADMAP 5 处（节标题、证据行现值 + 文件表、风险段改"已兑现"、5c 表格行、全项验收逐条对账）+ INDEX 2 处 + doc-metrics 3 行 | [35311403454](https://github.com/SkyForest233/chileme/actions/runs/35311403454)（head `a4efc45`） | ✅ |
| 本次补记（§12.7 真机复测通过 + ROADMAP 5 处 / INDEX 2 处收官同步） | #5 验收 ①–⑥ 全达成、④ 由 🔶 改 ✅；**release 包未实机确认**这一条如实记进 §12.7 | 见下一次 run | — |

| 本次（§13 评估与规划 + ROADMAP #10 + INDEX 待办 + doc-metrics 三行判据与第 21 条数字比对 + 核查第 18/19 处改指针） | **纯文档与工具**，不动主代码；#10 未开工 | [35316605782](https://github.com/SkyForest233/chileme/actions/runs/35316605782)（head `3f681b2`） | ✅ 3 job 全绿 |
| 本次（§13.7 **#10a-1**：设置页弹窗区（352–986）搬到同包 3 个文件；6 处守卫同批改（含 `ScreenParityTest` 规则 2 扫描面加宽）；核查第 20/21 处） | 行为零改动（逐字校验 0 缺失）⇒ 不占用真机复测；`SettingsScreen.kt` 1,705 → 1,079 | [35322713982](https://github.com/SkyForest233/chileme/actions/runs/35322713982)（head `c20458a`） | ❌ 门禁 ✅ / **两个构建 job 都红**：`:app:compileDebugKotlin` 24 处 `Unresolved reference` + 4 处连带 `@Composable` 违规（别名 import 漏带，根因与修法见 §13.8） |
| 本次（§13.8 **10a-1 修红**：补 9 条别名 import + `kt-lexcheck` 判据 4「用了别名却没 import」与 3 个自检对照（12 → 15）+ 核查第 22 处 + `WORKFLOW`/`detekt.yml`/INDEX 同步） | 3 文件 / **9 insertions(+) / 0 deletions(-)**；守卫端到端阴性对照通过（修复前的文件正好点名 9 处、次数 2/15/7 与 CI 一致）；本地三项检查全绿 | 本提交触发的 run（下一笔补记） | — |

**上一轮遗留的 CI 状态**：run [35291801827](https://github.com/SkyForest233/chileme/actions/runs/35291801827)
（head `1d78694`，即 §1 的 ROADMAP/INDEX 重写 + 两条守卫）本轮开始前已 ✅，故本轮的 4 个提交是在绿的基础上叠的。
