# 2026-09-15 开发日志

> 本轮主题：**修复两轮独立审查（本仓库自查 + 第三方 `docs/audits/chileme-review.md`）交叉验证过的 5 个高置信度问题**。
> 全部改动均为「小改、可独立验证」性质，未触及需求边界与 UI 视觉规范。
> ✅ 沙箱无 JDK / Android SDK，本轮**未本地构建**，改由 CI 验证：PR #7 → run [34917992215](https://github.com/SkyForest233/chileme/actions/runs/34917992215) **通过**（`assembleDebug` / `testDebugUnitTest` / `lintDebug` 全绿，2m54s）。

---

## ✅ 已完成

### 1. 读流兜底：读异常不再杀进程（P0）

- **文件**：`data/FoodRepository.kt`
- **问题**：`dataStore.data` 在 IO 抖动 / 文件被占用时抛异常；19 个 `stateIn(SharingStarted.Eagerly)` 共享协程一旦收到异常，因 `viewModelScope` 未挂 `CoroutineExceptionHandler`，异常会交给线程默认处理器 → **进程被杀**；若表现为读阻塞（永不发射），首页 `ready` 门控又会让启动画面永久停留。
- **改动**：新增 `resilientRead(keyName, fallback, transform)`：`IOException` 先退避重试 2 次（300ms），仍失败则 `Log.e` + 回落 fallback。`rawFlow()` / `lightFlow()` 均改为建立在它之上（`lightFlow` 新增 `keyName`/`fallback` 两个参数，9 处调用点同步更新）；凭据两个 flow 合并为 `nutstoreCredentialKeysFlow()` 复用。
- **影响面**：读写语义不变（写仍受 `isCorrupt` 守卫保护），仅把「崩溃」降级为「本次读到默认值 + 日志」。

### 2. 启动放行超时：不可能再卡在启动页（P0）

- **文件**：`MainActivity.kt`
- **改动**：`contentReady` 由普通 `var` 改为 `mutableStateOf`（超时翻转需触发重组），新增 `READY_TIMEOUT_MS = 3_000L`：`ready` 3 秒内未达成也强制渲染首帧。
- **取舍**：极端情况下会多显示一帧默认主题，但杜绝「永久停在启动画面、只能杀进程」。

### 3. 损坏态下不再删除封面图（P0，最严重的一处）

- **文件**：`viewmodel/AppViewModel.kt`
- **问题**：启动时的 `cleanupOrphanCovers()` 用 `itemsFlow.first()` / `archiveFlow.first()` 组装「被引用图片」集合；而解码失败时这两个流会按设计回落**空集**，于是 `covers/` 下所有文件被当成孤儿**全部删除**——而图片无法从 `corrupt/` 的 JSON 留档恢复。守卫本想救数据，清理逻辑却在同一时刻销毁数据。
- **改动**：`corruptedKeys` 非空时跳过清理并 `Log.w` 提示。

### 4. 过期浪费统计口径改为按件数

- **文件**：`ui/screens/StatsState.kt`
- **问题**：`wastedTotal = archived.count { reason == EXPIRED }` 数的是**归档条数**，而归档保留原 `quantity` —— 「6 瓶冰红茶过期」被统计成 1；同屏「本周消耗」却按件求和，一个屏幕两种口径。该指标是 App「减少浪费」价值主张的唯一量化出口。
- **改动**：抽成纯函数 `calculateWastedTotal(archived) = sumOf { it.item.quantity }`（可 JVM 单测），并在 KDoc 注明它仍受 `take(200)` 归档上限影响。

### 5. `MiuixStatsScreen` 接回状态层（消除静默分叉）

- **文件**：`ui/screens/MiuixStatsScreen.kt`（-67 行）
- **问题**：全项目 16 个屏幕文件里，**只有它没有调用 `remember*UiState`**，而是手抄了一份统计计算。于是 `StatsStateTest` 测的是「MIUIX 主题下根本不会执行」的代码，改统计口径只改一处就会两套主题不一致。
- **改动**：改为 `val state = rememberStatsUiState(viewModel)`，删除内联的周/月消耗、7 日趋势、分类占比、TOP5、浪费统计；本页只保留外壳（Miuix Scaffold/Card/SmallTitle）与图表自绘。

### 6. 导入备份：二次确认 + 导入前自动快照 + 体积上限

- **文件**：新增 `data/BackupFile.kt`；`data/FoodRepository.kt`（`previewBackup`）；`viewmodel/AppViewModel.kt`（`previewBackup` / `importBackupWithSnapshot`）；`ui/screens/SettingsState.kt`（`PendingImport` + 两个转发）；`ui/screens/SettingsScreen.kt`、`MiuixSettingsScreen.kt`
- **问题**：原先选中文件即 `readBytes()` 全量读入（无上限）并**直接整体替换**，既无二次确认也无回滚手段；且 `{}` 这类合法 JSON 会因字段默认值解码「成功」，变成一个能清空用户数据的「合法空备份」。
- **改动**：
  1. `readBackupText(context, uri)`：IO 线程读取，先查 `AssetFileDescriptor.length` 在分配内存前拒绝 > 20 MB 的文件，读后再校验一次；
  2. `previewBackup(raw)`：必须含 `items` 键才认（拦「合法空备份」），返回摘要；
  3. 两套设置页各加确认弹窗：展示**导出日期 / 库存·归档·消耗·历史条数 / 版本过新警告**，红色按钮「覆盖导入」；
  4. `importBackupWithSnapshot(raw)`：先 `buildBackupJson()` + `LocalSnapshotStore.saveSnapshot()` 留一份「导入前状态」（可在设置页一键回退），再执行替换；快照失败不阻断导入但会在提示里说明。

### 7. IME 键盘避让（A 方案 + 弹窗）

- **文件**：`EditFoodScreen.kt`、`FoodListScreen.kt`、`ArchiveScreen.kt`、`MiuixFoodListScreen.kt`、`MiuixArchiveScreen.kt`、`MainActivity.kt`、`SettingsScreen.kt`、`ManageScreens.kt`、`ui/components/Common.kt`（KDoc）、新增 `test/.../ImeHandlingTest.kt`
- **问题**：`AndroidManifest.xml:23` 的 `adjustResize` 在 Android 15+ 强制 edge-to-edge 后**不再缩窗口**，键盘只是叠在窗口上；而全仓此前**只有 `navigationBarsPadding()`、没有任何 IME 处理**。Compose 里只有自带 `windowInsets` 的组件（`TopAppBar`/`BottomAppBar`/`NavigationBar`）会让位，本项目的底栏是自定义 `Surface`，所以「改 `contentWindowInsets`」那类建议无效——`contentWindowInsets` 只作用于 content slot。
- **改动（A 方案，产品决定：底部导航栏不随键盘抬升）**：
  1. **编辑页（P0）**：`Scaffold(modifier = Modifier.imePadding())` → 底部「保存/添加到零食柜」贴着键盘顶边可点（此前必须先按返回键收起键盘才能保存）；该屏双主题共用，改一次两套都好；
  2. **4 个搜索列表页**：同上一行 → 列表可视区止于键盘顶边，末尾条目滚得出来（此前底部留白只算导航栏 + 96dp）；
  3. **3 处 App 级浮层**（Snackbar 撤销条 + 悬浮/常驻批量操作栏）：`navigationBarsPadding()` 后追加 `imePadding()` → 提示条与撤销按钮浮到键盘上方（此前 6 秒撤销窗口在键盘后面白白流逝）；
  4. **弹窗**：Miuix 侧 3 处**不改代码**——核实库源码 pinned 快照 `v0.9.4-rc01`：`DialogContentLayout.kt` 的 `DialogContent` 根节点在 `defaultWindowInsetsPadding = true`（默认）时自带 `.imePadding()`，窗口属性来自 `platformDialogProperties()`（`decorFitsSystemWindows = false`、`usePlatformDefaultWidth = false`），IME inset 本来就能传到弹窗内容；只在 `MiuixDialog` 的 KDoc 里写清依据并警告**不要**传 `defaultWindowInsetsPadding = false`。MD3 侧 3 处（坚果云账号、分类编辑、添加存放位置）补 `DialogProperties(decorFitsSystemWindows = false)` + `Modifier.imePadding()`。
- **口径**：`adjustResize` 保留在 manifest（对旧系统仍是兜底）；页面内 Snackbar（如归档页）由所在 Scaffold 的 `imePadding()` 一起抬，不单独加，避免双份 padding。
- **CI 过程（记录一下，避免后人踩同一个坑）**：首轮 run `34922600736` 在 `:app:compileDebugKotlin` 失败 —— `SettingsScreen.kt:767`、`ManageScreens.kt:311/431` 报 `Unresolved reference 'imePadding'`：这两个文件只补了 `DialogProperties` 的 import，漏了 `androidx.compose.foundation.layout.imePadding`。补 import 后 run `34922807620` **全绿**（3m4s，含 lint 与单测）。**只靠读代码看不出来，编译期才发现** —— 沙箱无 JDK 时，这类错误只能靠 CI 拦。
- **测试**：新增 `ImeHandlingTest`（3 例）静态守卫——含输入框的屏幕必须有 `imePadding()`；MainActivity 的 App 级浮层至少 3 处、且 `imePadding()` 数严格少于 `navigationBarsPadding()` 数（把「底栏不抬」这个产品决定固化下来）；带输入的 MD3 弹窗必须 `decorFitsSystemWindows = false`。

### 8. 测试与文档

- `StatsStateTest`：新增 2 例（按件求和、无过期归档为 0），共 7 例。
- 新增 `MiuixParityTest`（3 例）：静态守卫——每个 `Miuix*Screen.kt` 必须调用 `remember*UiState`、不得出现 `sumOf {` / `groupBy {` / `count {` / `sortedByDescending`，`MiuixStatsScreen` 必须复用 `rememberStatsUiState`。防止本轮修掉的「双实现分叉」复发。
- `docs/ARCHITECTURE.md` §5：新增「读流兜底」「启动放行超时」「封面清理守卫」「导入备份加固」「统计口径」「Miuix 屏幕只换外壳」6 条约定；§3 `BackupData` 行补充 `BACKUP_VERSION` 与导入前校验。
- `docs/REQUIREMENTS.md` F14：要点补「导入前二次确认 + 自动留存导入前快照」。
- 另产出两份审查报告（本轮一并入库）：
  - `docs/audits/2026-09-15-code-review.md`（本仓库全量自查，含 §7 结论可靠性自评）
  - `docs/audits/2026-09-15-third-party-review-verification.md`（对第三方 `chileme-review.md` 的逐条复核：30 条成立 / 6 条需修正 / 1 条不成立）

---

## 💡 决策记录

- **为什么把读兜底放在仓库层而不是 VM**：19 个 flow 的收集点分散在 repository 与 VM 两处，逐个加 `catch` 必然漏；`rawFlow`/`lightFlow` 是所有读的唯一入口，收口一处即可全覆盖。
- **为什么不给 `viewModelScope` 挂 `CoroutineExceptionHandler` 就算完**：那只能阻止崩溃，flow 一旦失败该数据流就永久静默了（UI 停在旧值/空值且无提示）。仓库层兜底能保证「数据流继续活着」。
- **为什么 `previewBackup` 要求必须含 `items` 键**：`BackupData` 全字段带默认值，`{}` 能解码成功——不加这道判断，「格式错误友好提示」在**最危险的输入**（合法但空）上反而失效。
- **为什么快照失败仍允许导入**：导入的主要用途就是「当前数据已损坏时用备份覆盖」。因序列化失败而拒绝导入，会让用户彻底没有恢复手段。
- **为什么加静态守卫测试而不是自定义 lint**：源码就在仓库里，一个 JVM 单测读取 `Miuix*.kt` 断言即可，成本远低于写 lint 规则，且 CI 已经在跑单测。

---

## 🔬 真机验证清单（本轮改动）

> 装 CI 产出的 `app-debug.apk`（run 34917992215 的 artifacts）。

### A. 必测：4 项，约 10 分钟

**A1. 导入备份的「预览 → 确认 → 自动快照 → 回退」全链路**（改动最大，最值得测）

1. 设置页 →「导出备份」→ 存到「下载」（顺带确认导出仍正常）；
2. 随便改几条库存 / 删一条归档，让当前数据与备份**不同**；
3. 设置页 →「从 JSON 文件导入」→ 选刚导出的文件；
4. **预期**：弹出「导入备份」弹窗 —— 首行「备份导出日期：2026年9月15日」（应为**导出当天**，不是今天乱变），次行「库存 N 件 · 归档 N 条 · 消耗 N 条 · 历史 N 条」，且条数与导出前一致；底部红字「覆盖导入」；
5. 点「覆盖导入」→ **预期**：提示「导入成功，数据已恢复 ✅（已自动留存导入前快照）」，数据回到导出版本；
6. **回退测试**：设置页 →「从本地历史快照恢复」→ 列表里应出现**刚导入前**那一份（时间 = 导入前一分钟）、以及当天首次启动的那份（系统每日自动快照）→ 选它 → 确认 → 数据应回到第 2 步改过的状态。
   - 若提示「导入前快照未能保存」→ 说明写快照失败，需查 `snapshots/` 目录权限。

**反向用例**（各 30 秒）：
- 选一个**非备份**的 JSON / CSV → 预期「导入失败：这不是本应用的备份文件」，且当前数据**不动**；
- 用文本编辑器把备份里的 `"items"` 改名为 `"itemz"` 再导入 → 预期同样被拒（防「合法空备份」清空数据，这是本次新增的关键判断）；
- （可选）往设备塞一个 >20 MB 的 `.json` 再选 → 预期「读取文件失败，或文件超过 20 MB」，App 不卡死、不 OOM。

**A2. 过期浪费改为按件数**

1. 新增食品，**生产日期选 3 天前、保质期填 1 天**（保证已过期），**数量填 3**；
2. 首页出现「一键清理 N 件过期食品」→ 点它 → 归档；
3. 进统计页 → **预期**「过期浪费」显示 **3**（修复前显示 1）；
4. 对照「本周消耗 / 本月消耗」也应是件数口径，三者一致。

**A3. MIUIX 与 Material You 数字一致性**（本轮新增守卫要保的东西）

1. 设置页 →「主题风格」切到 **MIUIX**（HyperOS 组件）→ 进统计页，记下三个数字 + 分类占比 + TOP5；
2. 切回 Material You → 再看统计页；
3. **预期**：两套主题**完全一致**（修复前 `MiuixStatsScreen` 是另一套手写实现）；顺带确认图表不空白、不崩。

**A4. 冷启动放行**（超时兜底无法自然复现，测正常路径 + 反复压）

1. 冷启动 → 启动页一闪而过，**首帧就是真实主题色**，不应「先绿后变」；
2. `adb shell am force-stop com.chileme.pantry` 后连续启动 10 次 → 不应出现卡在启动页 / 白屏；
3. （进阶，可选）模拟读失败：`adb shell run-as com.chileme.pantry chmod 000 files/datastore/pantry_store.preferences_pb` → 启动 App → **预期**不崩、不卡启动页，进主页且数据为空（= 读兜底生效）；
   ⚠️ 测完必须恢复：`adb shell run-as com.chileme.pantry chmod 600 files/datastore/pantry_store.preferences_pb`，且在权限异常期间**不要**写入数据（写路径的兜底不在本轮范围）。

### B. 回归：本轮动了 9 个设置流与清理逻辑，顺手确认没伤到

- **封面清理**（改动 #3 的正常路径）：给食品加图 → 删除该食品 → `adb shell run-as com.chileme.pantry ls files/covers` 应少一个文件；其他食品的封面不受影响；
- **9 个 lightFlow 迁移**：动态取色 / 深色 / 配色 / 主题风格 / 悬浮导航 / 坚果云账号 / 上次同步时间 / 自动同步间隔 / 上次自动同步日 —— 各改一次 → **杀进程重开**仍保持新值；
- **坚果云凭据**（两个 flow 合并过）：填账号+应用密码 → 保存 → 杀进程重开 → 仍显示已保存；
- **每日自动快照**：当天首次启动后，「本地历史快照」应新增一份；
- **导出**：JSON 与 CSV 各导出一次仍能成功打开。

### C. 不需要真机验证的部分

- `calculateWastedTotal` 求和逻辑、`previewBackup` 拒收逻辑、`MiuixParityTest` 静态守卫、备份编解码兼容 —— 已由 JVM 单测覆盖（共 78 例，CI 全绿）；
- 读兜底的重试次数与退避时长属实现细节，靠代码审查 + A4 第 3 步间接确认。

---

## 📋 待办（本轮未做，按审查报告优先级）

> 下表是**按优先级挑出来的近期可做项**；更完整的分批路线图（第一批 10 项 / 第二批 8 项 / 第三批结构性 7 项）见 `docs/audits/2026-09-15-code-review.md` §5，双方交叉验证后的合并清单见 `docs/audits/2026-09-15-third-party-review-verification.md` §4。

| 优先级 | 事项 | 来源 |
|---|---|---|
| — | ~~IME 未处理~~ → 已于本轮修复（A 方案 + 弹窗，见上「7. IME 键盘避让」）；仍留一项**真机确认**：MD3 弹窗是浮动小窗，`decorFits` 关掉后弹窗会「整体上移约半个键盘高度」，视觉上是否正确需真机看（若觉得位置偏高，下一步可改成铺满窗口 + 内容居中的自绘弹窗） | 第三方 P0-1 |
| 高 | 守卫粒度：`upsert`/`changeQuantity` 因 `history`/`consumption` 损坏而整体拒绝写入，把「部分不可用」放大成「完全不可用」；需要按 key 粒度 + 提供「放弃损坏数据」入口 | 自查 §1.2 |
| 中 | 备份加固未覆盖另外两条恢复路径：`syncDownload`（坚果云整版本恢复）与 `restoreLocalSnapshot` **已有二次确认**（弹窗含恢复时间 + 「整体替换」提示，见 `SettingsScreen.kt:884` / `:971`），缺的是 ① 导入前**自动前置快照**（现在恢复错了无法回退）② 未走 `previewBackup` 的 `items` 键校验。建议把「先存快照 + 校验」抽成公共前置步骤接入两处 | 自查 §1.10（本轮复核后订正） |
| 中 | 消耗记录区分「月度聚合」：加 `aggregated` 标记并禁用单条删除（现在一条删除会抹掉整月历史） | 第三方 P0-5 |
| 中 | MIUIX 风格缺配色入口，而 KDoc 声称功能对等（15 套配色在 Miuix 下不可选、MD3 下选的配色在切换后无提示地失效） | 第三方 P3-6 |
| 中 | `LocalSnapshotStore` 主线程 IO + `itemCount` 用正则数 `"id":`（口径偏大） | 双方 |
| 中 | 剩余统计口径：「本周」实为近 7 天、TOP5 未按单位分组、分类占比只用当前库存 | 双方 |
| 中 | 凭据：`SecureStore.encrypt` 失败时**静默返回空串**（`catch (e: Exception) { "" }`，无日志、无提示），`setNutstoreCredentials` 随即把明文写回 `nutstore_password` 键（代码注释自称"不应发生"），且该明文键会一直留在 DataStore；`nutstorePasswordFlow` 把明文推进 StateFlow 常驻内存（坚果云同步需要，可改为按需取用）。修法：失败时返回可区分的错误 + 记日志 + UI 明确告知"未能加密存储" | 双方（本轮复核后订正） |
| 中 | `File.exists()` 在组合期同步调用（`Common.kt:262`、`EditFoodScreen.kt:263`），且 `photoPath` 存绝对路径（换设备/清数据后失效）；应只存文件名 + 在 VM/IO 侧判断存在性 | 第三方 P1-8 |
| 中 | 同一个冷流被 3 处收集（`AppViewModel.kt:42/59/120` 各收一份 `itemsFlow`）→ 同一份 JSON 解码 3 遍；启动期又 `first()` 两次 | 第三方 P1-3 |
| 中 | `SettingsState` 把 28 个 key 打包进一个 data class → 重组粒度比逐个 `collectAsStateWithLifecycle` 更粗（任一设置变化都会重组整屏） | 第三方 P1-11 |
| 中 | 仓储集成测试缺失：`dataStore.edit` 有 28 处、12 个写方法都带守卫，但没有任何测试覆盖「守卫是否真的拦住写入」；建议 Robolectric + 临时 DataStore | 自查 §2.9 / 第三方 P2-6 |
| 低 | CI（`build.yml`）：无 `concurrency`（同分支连推会并发跑完旧的）、三次独立 `./gradlew --no-daemon` 调用（可合并为一次）、**release 构建/R8 未在 PR 阶段验证**（`release.yml` 只在打 tag 时跑，混淆问题会在发版那一刻才暴露） | 自查 §4.2 / 第三方 P2-2 |
| 低 | 口径措辞：首页「一键清理 N 件过期食品」的 N 是**记录条数**（`items.count{...}`）却写「件」，与刚统一的按件口径不一致；归档页筛选 chip 同理 | 本轮真机清单 A2 引出 |
| 低 | 文档漂移 10 处（`ARCHITECTURE.md:114` 仍在描述已删除的两段式滑动归档；`README.md` 写 OkHttp 5 而 catalog 是 4.12.0 等）；另 `docs/audits/ci/*.yml`、`*.patch` 是漂移的历史文件，应删 | 双方 |
| 低 | 相机临时文件 `cacheDir/camera/*.jpg` 从不清理（每拍一张留一份） | 第三方 P3-3 |
| 低 | 归档单条「彻底删除」无确认/撤销；详情页「吃掉一份」无撤销；CSV 公式注入；`corrupt/` 目录无上限；导出文件名撞名；`material-icons-extended` 已弃用 | 第三方 |

---

## ⚠️ 已知问题 / 未验证项

- 沙箱无 JDK/SDK，未本地构建；**CI 已通过**（见文首 run 链接），编译与单测有自动化保障。未做的是**真机/模拟器上的行为复测**（下方 IME 与导入流程）。
- IME 遮挡问题（上表第一项）需要**真机**确认：打开「添加食品」→ 点名称输入框 → 观察「添加到零食柜」是否被键盘盖住。
- 归档上限导致的统计失真（`take(200)`）本轮只做了口径修正，未引入独立计数器。
- **IME 修复的真机确认点**：① 编辑页保存按钮是否贴键盘顶边、键盘升降是否跟手无跳变；② 搜索列表末尾能否滚到键盘上方；③ 撤销条是否浮在键盘上方且可点；④ **MD3 弹窗（坚果云/分类/位置）**：`decorFitsSystemWindows = false` 后弹窗窗口会整体上移（窗口居中，故约上移半个键盘高度），请确认位置观感是否可接受——若偏高，可改为铺满窗口 + 内容居中于键盘之上的自绘弹窗；⑤ **切到三键导航**（设置 → 系统导航方式）再测一遍，导航栏更高最易暴露双重 padding。Miuix 侧弹窗无需测试（库内已处理）。
