# 开发日志 — 2026-08-20

## 主题风格切换（阶段 0 + 阶段 1）：引入 Miuix，设置页双实现

### ✅ 已完成

1. **安装 Miuix Skill**（`.claude/skills/miuix/`，limczhh/miuix-skill，证据基线 v0.9.4-rc01 @ 4a6b750b）
   - 评估报告：`docs/audits/2026-08-20-miuix-eval.md`（现状梳理 / 三条路径对比 / 建议）

2. **工具链升级**（为消费 Miuix 0.9.4-rc01，对齐其上游基线）
   - Kotlin 2.2.21 → **2.4.10**；AGP 8.10.1 → **9.3.1**；Gradle 8.11.1 → **9.6.1**（wrapper）
   - kotlinx-serialization-json 1.9.0 → **1.11.0**
   - AGP 9 迁移：`kotlinOptions { jvmTarget }` → `kotlin { compilerOptions { jvmTarget.set(JvmTarget.JVM_17) } }`

3. **引入 Miuix 依赖**（app/build.gradle.kts）
   - `top.yukonga.miuix.kmp:miuix-ui-android:0.9.4-rc01`
   - `top.yukonga.miuix.kmp:miuix-preference-android:0.9.4-rc01`

4. **主题风格状态**（ThemeStyle 枚举 + 持久化 + 下发）
   - 新增 `ui/theme/ThemeStyle.kt`：`ThemeStyle` 枚举（MATERIAL3 / MIUIX）+ `LocalThemeStyle` CompositionLocal
   - DataStore 新增 key `theme_style`（默认 "MATERIAL3"），Repository/ViewModel 暴露 `themeStyle` Flow 与 `setThemeStyle(name)`

5. **MainActivity 接线**
   - 收集 `themeStyle` → 通过 `CompositionLocalProvider(LocalThemeStyle provides ...)` 下发
   - 「settings」路由按 `LocalThemeStyle.current` 分流到 Miuix / MD3 两套实现

6. **MD3 设置页新增「主题风格」切换**（SettingsScreen「外观」分组）
   - SegmentedButton 双选项（Material 3 / MIUIX）+ 说明文案

7. **设置页 Miuix 实现（阶段一核心）**：新增 `ui/screens/MiuixSettingsScreen.kt`
   - 局部 `MiuixTheme` + `ThemeController`（语义对齐 MD3：动态取色→Monet 跟随壁纸，否则 darkMode→System/Light/Dark）
   - 用 Miuix `Scaffold/TopAppBar/SmallTitle/Card` + `RadioButtonPreference/SwitchPreference/OverlayDropdownPreference/ArrowPreference` 组织「外观 / 物品管理 / 关于」三组
   - 组件 API 全部对照 pinned source（v0.9.4-rc01 @ 4a6b750b）核对，未臆造

8. **文档同步**：CLAUDE.md（skill 索引 + 关键约束）、ARCHITECTURE.md（版本/依赖/新文件/theme_style key）、DESIGN_SPEC.md（新增 §7 主题风格切换）

### ⚠️ 构建验证缺口（重要）

本沙箱**仅有 GitHub 网络可达**（Gradle / Maven Central / Google 均被阻断），且无 JDK/Android SDK/Gradle，
**无法执行 `./gradlew assembleDebug`**。代码按 pinned source 逐项核对过 API 签名，但以下事项需在本地构建确认：

1. 工具链升级（Kotlin 2.4.10 / AGP 9.3.1 / Gradle 9.6.1）的完整兼容性——这是本次最大风险点
2. Miuix `-android` 产物与本项目 Compose BOM 2026.01.01 的传递依赖是否冲突
3. AGP 9 其余 DSL 差异（若 `compileOptions`/`buildToolsVersion` 等有额外变动）
4. Miuix 组件的实际渲染效果（Android 12+ 动态取色、深色模式、OverlayDropdown 弹窗宿主）

### 💡 决策

- **选「双实现 + 抽象层」路线（参考 Hyper-pick-up-code）但先渐进**：阶段一只迁移设置页，避免一次性重写 5400 行；`LocalThemeStyle` 即未来的抽象层种子
- **Miuix 版本用 0.9.4-rc01**（用户明确要求 skill 内最新版）：候选版，上游尚无 v0.9.4 稳定 tag，已在 ARCHITECTURE 标注
- **Monet 不传 keyColor**：ThemeController 源码确认 `keyColor=null` 时 Monet 走 `platformDynamicColors`（跟随系统壁纸），与 MD3 侧「动态取色=Material You 壁纸」语义一致，而非套用 AppPalette 种子色
- **阶段一未迁移页面保持 MD3**：Miuix 页面局部包 `MiuixTheme`，外层仍是 `AgonAppTheme`(MaterialTheme)，未迁移屏幕不受影响；底部悬浮导航（MD3）暂保留，属预期中间态

### 📋 待办

- **（阻断）本地构建验证**：`./gradlew assembleDebug`，按上文「构建验证缺口」逐项排查；失败先看工具链/传递依赖
- （阶段二）列表页 / 详情页 / 首页逐屏抽象并迁移到 Miuix
- （阶段三）导航、弹窗、图标、深浅色/取色全局收尾
- （低）Miuix 稳定版 v0.9.4 发布后回迁 RC → 稳定

## 第 2 轮：CI 验证接入

### ✅ 已完成
- 用户手动新增 `.github/workflows/build.yml`（PR/push 触发 `assembleDebug`，JDK 17 + gradle/actions/setup-gradle + 上传 debug APK artifact）并提交到 master
- 本 commit 用于触发 `pull_request` 构建：master 已有 build.yml，推送到 PR 分支会触发对 PR 合并代码（含 Miuix 改动）的构建验证

### 📋 待办
- 等待 CI 结果；若工具链升级（Kotlin 2.4.10 / AGP 9.3.1 / Gradle 9.6.1）或 Miuix 传递依赖报错，按日志修复

## 第 3 轮：CI 首轮失败 → AGP 9 内置 Kotlin 迁移

### ⚠️ 问题
CI `assembleDebug` 在配置/解析阶段快速失败（约 35s，未进入编译）。原因：**AGP 9.0+ 内置 Kotlin，`org.jetbrains.kotlin.android` 插件已不兼容**（与内置 Kotlin 冲突，配置期报错）。

### ✅ 修复
- 移除 `org.jetbrains.kotlin.android`（根 build.gradle.kts 的 `apply false` 声明 + app 模块的 plugins）
- 保留 `org.jetbrains.kotlin.plugin.compose` / `org.jetbrains.kotlin.plugin.serialization`（2.4.10，二者携带 KGP，供内置 Kotlin 使用）
- `kotlin { compilerOptions { jvmTarget = JvmTarget.JVM_17 } }` 采用官方迁移形态（赋值而非 `.set()`）
- Miuix 依赖坐标改用 common（`miuix-ui` / `miuix-preference`，与上游 README / release notes 一致，Gradle Module Metadata 自动解析 android 变体）

### 💡 决策
- 参照 Hyper-pick-up-code（AGP 9.x + 仅 compose/serialization 插件、无 kotlin.android 且构建通过）验证迁移形态
- `android.nonTransitiveRClass=true` 现为 AGP 9 默认值，保留无害；gradle.properties 无 AGP 9 禁用项（enableJetifier / enableDexingArtifactTransform 等均未使用）

### 📋 待办
- 等待 CI 复跑结果；若仍有工具链/传递依赖报错，继续按日志修复

## 第 4 轮：CI 失败 → compileSdk 需升到 37

### ⚠️ 问题
`checkDebugAarMetadata` 失败：Miuix 0.9.4-rc01 及传递依赖（Compose 1.12.0-rc01、material3 1.5.0-alpha22、material-color-utilities 5.0.0）要求 compileSdk ≥ 37，项目为 36。

### ✅ 修复
- `compileSdk = 37`（仅此一项；targetSdk 保持 36、minSdk 保持 24，报错明确三者相互独立）
- 移除显式 `buildToolsVersion = "36.0.0"`，交由 AGP 9.3.1 选用默认 build-tools
- 同步 ARCHITECTURE.md compileSdk 说明

### 📋 待办
- 等待 CI 复跑；若 runner 缺 android-37 平台，补 sdkmanager 安装步骤

## 第 5 轮：CI 失败 → Maven Central 间歇 403（网络，非代码）

### ⚠️ 问题
`Could not GET 'https://repo.maven.apache.org/...' → 403 Forbidden`：CI runner 拉取依赖时被 Maven Central 限流（guava / commons-compress / kotlin-stdlib / AGP 自身依赖等大量解析失败）。上一轮同环境可正常解析依赖，属间歇性网络故障（GitHub Actions 共享 IP 被 CDN 限流的已知问题）。

### ✅ 修复
- gradle.properties 增加依赖下载重试：`org.gradle.internal.repository.max.retries=8` + `initial.backoff=1000`，提升对瞬时 403 的容错

### 📋 待办
- 重新触发构建；若仍 403，考虑 workflow 层面重试 / 镜像仓库 / 分时段重跑

## 第 6 轮：CI 构建通过 ✅

### ✅ 已完成
- 上一轮 403 确认为 Maven Central 间歇限流；加重试后重新触发，`build-debug` **4m1s 通过**
- 验证结论：工具链升级（Kotlin 2.4.10 / AGP 9.3.1 / Gradle 9.6.1）、Miuix 0.9.4-rc01 依赖（compileSdk 37）、ThemeStyle/LocalThemeStyle/MiuixSettingsScreen 代码全部编译通过
- PR #1 check `build-debug` pass，产物 `app-debug` artifact 上传成功
- 阶段 0 + 阶段 1 交付完成，可合并

## 第 7 轮：阶段二 —— 根级主题切换 + MaterialTheme 桥接 + 首页迁移

### ✅ 已完成
1. **根级主题切换 + MaterialTheme 桥接**（新增 `ui/theme/MiuixRootTheme.kt`）
   - `rememberMiuixController(darkMode, dynamicColor)`：从 MiuixSettingsScreen 抽出，统一推导 ThemeController
   - `miuixColorsToMd3ColorScheme(Colors)`：把 Miuix Colors 桥接为 MD3 ColorScheme（缺失角色用最接近角色近似）
   - `MiuixRootTheme`：`MiuixTheme` + 桥接 `MaterialTheme`，让未迁移页面 / Common.kt 组件在 MIUIX 模式下取到 Miuix 配色
2. **MainActivity 根级切换**：MIUIX → `MiuixRootTheme`，MD3 → `AgonAppTheme`；home 路由按 `LocalThemeStyle` 分流
3. **简化 MiuixSettingsScreen**：去掉局部 MiuixTheme（改由根级提供）
4. **迁移首页**（新增 `ui/screens/MiuixHomeScreen.kt`）
   - 用 Miuix `Scaffold/TopAppBar/Card/Button/SnackbarHost` + Miuix `Text`（fontSize/fontWeight 控制）
   - 复用 Common.kt 的 EmptyState/FoodAvatar/StatusBadge/rememberStatusUi（桥接 MaterialTheme 提供 Miuix 配色）
   - 图标沿用 material-icons（MD3 Icon + 显式 tint）
5. 文档同步：ARCHITECTURE（新文件）、DESIGN_SPEC §7（根级桥接 + 首页）

### 💡 决策
- **选「根级 MiuixTheme + 桥接 MaterialTheme」而非逐页包 MiuixTheme**：一次性让全局配色统一 Miuix，未迁移页面自动跟随，避免每个 Miuix 页面重复 ThemeController 逻辑
- 桥接用近似映射（tertiary=tertiaryContainer、onSurfaceVariant=onSurfaceVariantSummary、outlineVariant=dividerLine、scrim=windowDimming 等），属过渡层，缺失角色不影响核心可读性
- 首页文本用 Miuix Text 保证小米字体层级；图标沿用 material-icons 的 ImageVector（MD3 Icon + 显式 tint，因 Miuix 与 MD3 的 LocalContentColor 是两套 CompositionLocal）

### 📋 待办
- 等待 CI 复跑；若桥接映射 / Miuix 组件签名有编译问题按日志修复
- （阶段二后续）列表页 / 详情页 / 归档 / 统计 / 管理页逐屏迁移

## 第 8 轮：阶段二构建通过 ✅

### ✅ 已完成
- 修复 `MiuixRootTheme.kt` 注释中 `inverse*/` 的 `*/` 被误解析为块注释结束符（编译错误）
- 复跑 CI：`build-debug` **3m1s 通过**
- 验证结论：MiuixRootTheme 桥接、MiuixHomeScreen（Miuix Scaffold/TopAppBar/Card/Button/SnackbarHost + Common.kt 复用组件）全部编译通过
- 阶段二交付完成

### 📋 待办
- （阶段二后续）列表页 / 详情页 / 归档 / 统计 / 管理页逐屏迁移
- 建议真机/模拟器验证 MIUIX 模式下的实际渲染（桥接配色、深浅色、动态取色）

## 第 9 轮：阶段二 P0 —— 食品列表页迁移

### ✅ 已完成
- 新增 `ui/screens/MiuixFoodListScreen.kt`：列表页 Miuix 实现
  - 用 Miuix `Scaffold/TopAppBar/InputField(搜索)/Button/TextButton/SnackbarHost/IconButton`
  - FoodCard / FilterChip 复用现有实现（桥接 MaterialTheme 取 Miuix 配色）
  - 业务逻辑（筛选/过滤/多选批量归档/撤销 Snackbar）与 MD3 版对等
- MainActivity list 路由按 LocalThemeStyle 分流
- 关键决策：
  - 筛选 Chip 保留 MD3 FilterChip（Miuix 无等价 chip 组件），属渐进迁移预期
  - Miuix Icon 读 Miuix LocalContentColor，MD3 容器语境下显式 tint（避免取错色）
  - 文本在 MD3 容器内用 MD3 Text，Miuix 组件内显式 color

### 📋 待办
- 等待 CI；P0 剩余：详情页、归档页

## 第 10 轮：P0 —— 食品详情页迁移

### ✅ 已完成
- 新增 `ui/screens/MiuixFoodDetailScreen.kt`：详情页 Miuix 实现
  - 用 Miuix `Scaffold/TopAppBar/Button/TextButton/OverlayDialog(删除确认)/SnackbarHost`
  - 状态卡/详情卡/进度条/分隔线复用现有实现（桥接 MaterialTheme 取 Miuix 配色）
  - 吃掉一份动画 / 数量步进器 / 归档逻辑与 MD3 版对等
- MainActivity detail 路由按 LocalThemeStyle 分流

### 📋 待办
- 等待 CI；P0 剩余：归档页

## 第 11 轮：P0 —— 归档页迁移（P0 完成）

### ✅ 已完成
- 新增 `ui/screens/MiuixArchiveScreen.kt`：归档页 Miuix 实现
  - 用 Miuix `Scaffold/TopAppBar/InputField(搜索)/OverlayDialog(清空确认)/SnackbarHost/IconButton`
  - 归档行 Surface / 筛选 Chip 复用现有实现（桥接取色）
- MainActivity archive 路由按 LocalThemeStyle 分流
- **P0 三页（列表/详情/归档）全部迁移完成**，日常操作路径（浏览→详情→吃掉/编辑→归档→恢复）全程真 Miuix 组件

### 📋 待办
- 等待 CI；P1：底部导航 + FAB 换 Miuix + Common.kt 复用组件抽象

## 第 12 轮：P1 —— 底部导航双形态 + FAB 换 Miuix

### ✅ 已完成
- 新增 DataStore 设置 `floating_nav`（默认 true）+ ViewModel `floatingNav`/`setFloatingNav` + ready 门控
- MainActivity MainApp 按「主题风格 × 悬浮开关」分支底栏与 FAB：
  - MIUIX 悬浮 → Miuix `FloatingNavigationBar`（仅图标）
  - MIUIX 非悬浮 → Miuix `NavigationBar`（全宽图标+文字）
  - MD3 悬浮 → 现有 `FloatingPillNav`（自绘胶囊）
  - MD3 非悬浮 → 新增 MD3 `NavigationBar`（全宽图标+文字）
  - FAB：MIUIX 用 Miuix `FloatingActionButton`，MD3 保持原样
- 抽取共享 `MainTabs` + `navigateToTab()` 复用导航逻辑
- 两个设置页「外观」分组新增「悬浮导航」开关（CheckSwitch / SwitchPreference）

### 💡 决策
- 悬浮开关做成跨主题通用（MD3/MIUIX 都支持），默认 true 保持现状，不破坏既有体验
- MIUIX 悬浮导航为 Miuix 官方形态（仅图标，label 仅作无障碍描述），与自绘胶囊（图标+标签）不同属预期差异
- 全宽底栏时 snackbar 的 84dp 上移偏移仍在各屏 SnackbarHost 处，MIUIX 全宽底栏下可能需要微调（真机确认）

### 📋 待办
- 等待 CI；P1 剩余：Common.kt 复用组件抽象、编辑/管理/统计页迁移

## 第 13 轮：P1 —— Common.kt 复用组件双实现

### ✅ 已完成
- Common.kt 复用组件按 `LocalThemeStyle` 双实现（MD3 / MIUIX）：
  - `StatusBadge`：Miuix Surface + Icon + Text（fontSize 显式）
  - `LocationTag`：Miuix Surface + Icon + Text
  - `QuantityStepper`：Miuix Surface + IconButton + Text
  - `EmptyState`：Miuix Text
  - `FoodCard`：Miuix Card（squircle 圆角 + PressFeedbackType.Sink 按压下沉）+ Miuix Text + 选中描边 Modifier.border
- 保持共用（自绘/无 MD3 结构性组件，或项目特色）：
  - `rememberStatusUi`/`urgencyDotColor`/`SelectIndicator`/`CheckSwitch`/`FoodAvatar`/`EmojiAvatar`（颜色经桥接 MaterialTheme 自动统一）
  - FoodCard 内 `LinearProgressIndicator`（颜色显式传语义色，进度条 Miuix 化收益低）
- 颜色策略：MIUIX 分支取色仍用 `MaterialTheme.colorScheme`（桥接后自动映射 Miuix token），只切换结构性组件类型

### 💡 决策
- 颜色统一走桥接 MaterialTheme.colorScheme，避免每个 Miuix 分支重复映射 Miuix token
- CheckSwitch 保留自绘（项目特色打勾/打叉，禁止 material3 Switch 的规范），不换成 Miuix Switch
- Miuix 分支字号用 fontSize 显式对应 MD3 语义字号，避免依赖 Miuix textStyles 字段名

### 📋 待办
- 等待 CI；P1 剩余：编辑页、管理页（统计页保留 MD3+桥接）

## 第 14 轮：P1 —— 管理三页迁移

### ✅ 已完成
- 新增 `ui/screens/MiuixManageScreens.kt`：阈值/分类/位置三个管理页 Miuix 实现
  - 用 Miuix `Scaffold/TopAppBar/IconButton/Surface/Text/TextButton/TextField/OverlayDialog`
  - 输入框改用 Miuix `TextField`（`TextFieldState`），弹窗改用 `OverlayDialog`
- MainActivity 三个管理路由按 LocalThemeStyle 分流
- **P1 实质完成**：导航/FAB/Common.kt 复用组件/管理页均已 Miuix 化

### 💡 决策
- Miuix TextField 用 `TextFieldState`（非 String value/onValueChange），取值 `.text.toString()`
- 编辑页/统计页保留 MD3+桥接（编辑页 DatePicker 为 MD3 特有、统计页图表自绘，Miuix 化收益低），属「刻意不迁移」

### 📋 待办
- 等待 CI；P2：主题切换渐变、Miuix 0.9.4 稳定版回迁、真机视觉走查

## 第 15 轮：P2 —— 文档最终同步

### ✅ 已完成
- ARCHITECTURE.md：screens/ 分层列出全部 Miuix 页面；数据 key 表补 `floating_nav`
- DESIGN_SPEC.md §7：更新迁移进度（已 Miuix 化页面 / 刻意保留 MD3 页面 / 导航双形态）
- CLAUDE.md：关键约束更新（迁移进度指向 DESIGN_SPEC §7，标注勿擅自迁移的页面）

### 📋 待办（P2 剩余，需用户/上游参与）
- 主题切换渐变：边际收益低（Miuix 组件读 MiuixTheme.colorScheme 仍瞬切，桥接层渐变不完整），建议真机确认后再决定
- Miuix 0.9.4 稳定版回迁：等上游发版
- 真机视觉走查：MIUIX 深浅色/动态取色/悬浮全宽导航/弹窗/Snackbar/squircle 圆角

## 第 16 轮：修复 MIUIX 反馈 —— chip 文字可读性 + 设置页备份功能

### ⚠️ 问题（用户反馈）
1. MIUIX 设置页缺「备份与数据」整组（导出/导入、坚果云同步、自动同步、清空库存）
2. 列表筛选 / 编辑页分类 chip 文字过灰难读

### ✅ 修复
1. **chip 文字根因**：桥接里把 MD3 `onSurfaceVariant` 错映射为 Miuix `onSurfaceVariantSummary`（0x99000000，60% 透明黑，仅作摘要文字）；MD3 FilterChip 未选中 label 正是用 onSurfaceVariant。改为 `onSurfaceSecondary`（0xCC000000，80% 黑），一处改动全局修复列表筛选 + 编辑页分类 chip 可读性
2. **设置页备份功能补全**（MiuixSettingsScreen）：
   - 备份与数据组：导出/导入 JSON（SAF）、坚果云同步（配置/上传/恢复）、自动同步下拉、清空库存
   - 弹窗：清空确认、坚果云账号配置、云端备份选择、恢复二次确认（均 Miuix OverlayDialog）
   - 坚果云应用密码框保留 MD3 OutlinedTextField（Miuix TextField 无 visualTransformation 无法遮蔽密码，必要妥协）

### 💡 决策
- chip 修复走全局桥接而非逐页 labelColor：一处改动覆盖所有 MD3 FilterChip/AssistChip/辅助文字
- Miuix TextField 无密码遮蔽 → 密码框 MD3 化是安全必要，非可选

### 📋 待办
- 等待 CI

## 第 17 轮：优化 MIUIX「备份与数据」排版

### ⚠️ 问题（用户反馈）
备份与数据组用横排实心 Button + 堆砌说明文字，视觉粗糙、有"老人味"。

### ✅ 修复
- 改为 Miuix 设置页标准设计语言：统一「图标 + 标题 + 摘要 + 箭头」的 Preference 行（ArrowPreference + startAction 图标）
- 结构：
  - 导出备份（FileUpload 图标）/ 导入备份（FileDownload 图标）
  - 坚果云同步（Cloud 图标，摘要动态显示未配置/已配置/lastSync）
  - 上传到云端 / 从云端恢复（仅已配置时显示，enabled 控制同步中置灰）
  - 自动同步（OverlayDropdownPreference 下拉，4 档间隔）
  - 清空库存记录（error 红色标题，强调破坏性）
- 移除所有横排 Button 与堆砌 Text；清理 unused imports

### 💡 决策
- 用 ArrowPreference 表达"点击执行动作"（导出/导入/上传/恢复/清空），箭头作"可点"视觉暗示，符合设置页"行的集合"节奏
- 坚果云未配置时只显示"坚果云同步"一行，配置后展开上传/恢复，保持清爽
- 破坏性操作用 BasicComponentDefaults.titleColor(error) 红色标题，与 HyperOS 设置页一致

### 📋 待办
- 等待 CI

## 第 18 轮：修复 chip 选中变灰 + 多选批量栏替换导航

### ⚠️ 问题（用户反馈）
1. chip 选中后文字变灰难读（列表筛选、添加食品分类等）
2. 多选时长按选中，批量操作栏被底部导航挡住，不优雅

### ✅ 修复
1. **chip 选中变灰**（根因：MD3 FilterChip 选中 label 默认 onSecondaryContainer，桥接成 Miuix 灰 #A9A9A9）：
   - 桥接 onSecondaryContainer 改为 onTertiaryContainer（蓝），浅灰底蓝字可读
   - primaryContainer 蓝底的 chip（状态/分类/归档原因/自动同步）显式 selectedLabelColor=onPrimaryContainer（白）
   - 覆盖 FoodListScreen / MiuixFoodListScreen / EditFoodScreen / ArchiveScreen / MiuixArchiveScreen / SettingsScreen
2. **多选批量栏替换导航**（选中状态提升到 ViewModel）：
   - ViewModel 新增 selectedIds + toggleSelection/setSelection/clearSelection
   - FoodListScreen / MiuixFoodListScreen 改用 viewModel.selectedIds，移除局部批量栏/归档/snackbar 逻辑
   - MainActivity bottomBar 多选时显示 BatchActionBar（取消/归档 N 项，MD3+Miuix 两套按钮）替换导航；FAB 多选时隐藏；返回键退出多选；撤销 Snackbar 移到 MainActivity

### 💡 决策
- 选中状态提升到 VM：MainActivity 与列表页共享，多选时导航→批量栏切换才可行
- 撤销 Snackbar 移 MainActivity：因批量栏在 MainActivity，归档+撤销生命周期跟随
- 批量栏不受下滑隐藏影响（selectionMode 分支不包 AnimatedVisibility(showChrome)），多选时常显

### 📋 待办
- 等待 CI

## 第 19 轮：修复多选残留 + 批量栏跟随悬浮 + tab 平移动画

### ⚠️ 问题（用户反馈）
1. 返回/切页后批量操作栏仍残留
2. 批量栏不跟随悬浮/非悬浮状态
3. tab 切换要平移动画（两主题一致）

### ✅ 修复
1. **多选残留**：MainApp 加 `LaunchedEffect(currentRoute)`，离开列表页（currentRoute != "list?filter={filter}"）时 `clearSelection()`
2. **批量栏跟随悬浮**：BatchActionBar 加 `floating` 参数——悬浮时居中悬浮胶囊（Box+Surface 圆角），非悬浮时全宽常驻；抽 BatchCancelButton/BatchArchiveButton 复用
3. **tab 平移动画**：NavHost 转场改纯水平平移（去掉 fade）：进入从右全宽滑入/退出向左全宽滑出，返回反向，300ms，MD3/MIUIX 两主题一致（NavHost 转场在 MainApp 层，天然两主题生效）

### 📋 待办
- 等待 CI

## 第 20 轮：返回键仅退多选 + 悬浮批量栏去双层胶囊 + 转场复刻 MiuixDefault

### ⚠️ 问题（用户反馈）
1. 有批量栏时按返回会同时切到主页+关批量栏，应只关批量栏
2. 悬浮态批量栏双层胶囊很丑
3. tab 平移效果不好，参考 Hyper-pick-up-code

### ✅ 修复
1. **返回键仅退多选**：BackHandler 从 MainActivity 移回列表页内部（FoodListScreen/MiuixFoodListScreen，位于 NavHost 内部，组合顺序在导航返回之后，优先级更高），selectionMode 时拦截返回仅 clearSelection
2. **悬浮批量栏去双层胶囊**：「取消」悬浮态改用无边框 TextButton（MD3 TextButton / Miuix TextButton），「归档」保持唯一实心胶囊；非悬浮态仍 OutlinedButton+Button
3. **转场复刻 MiuixDefault**（Hyper-pick-up-code 同款）：进入页从右全宽滑入覆盖；被覆盖页视差左移 1/4 宽 + fadeOut 至 90%；返回反向（popEnter 从 -1/4 滑回 + fadeIn 从 0.9）

### 💡 决策
- MiuixDefault 转场源码（NavTransitions.kt）确认：新页 full-width slide，旧页 parallax -0.25width + alpha 0.9；用 androidx Navigation 的 slideIn/Out + fadeIn/Out(initialAlpha/targetAlpha) 复刻
- corner clip / scrim（NavDisplayEffects 层）暂不复刻，先做核心 slide+parallax+alpha

### 📋 待办
- 等待 CI

## 第 21 轮：批量栏去外层胶囊背景 + 按钮加大

### ⚠️ 问题（用户反馈）
1. 动画"没生效"→ 确认为 debug 构建卡顿（debug 无 R8/编译优化/recomposition 追踪，动画帧率低；release 正常），转场代码本身正确
2. 双层胶囊 = 选项之外还有一层 Surface 背景胶囊，要求去掉
3. 批量栏按钮稍大一点

### ✅ 修复
- 悬浮态 BatchActionBar 去掉外层 Surface 胶囊容器，「取消」文字 + 「归档」实心胶囊独立悬浮（各自带阴影，无背景胶囊）
- 按钮加大：取消/归档 minHeight 40→48dp；Miuix 归档按钮 cornerRadius 16→50（胶囊）；MD3 用 defaultMinSize(minHeight=48)

### 📋 待办
- 等待 CI；动画效果建议 release 构建验证（debug 卡顿属预期）

## 第 22 轮：批量栏按钮回归各主题标准组件

### ⚠️ 问题（用户反馈）
批量栏被改得不统一：MIUIX 归档按钮被强行胶囊化（cornerRadius=50）、MD3 取消按钮胶囊背景丢失（被改成无边框 TextButton）。

### ✅ 修复
- Miuix 归档按钮：去掉 cornerRadius=50，恢复 Miuix 标准按钮（默认 16dp 圆角）
- MD3 取消按钮：悬浮/非悬浮统一用 OutlinedButton（胶囊边框背景），移除无边框 TextButton 分支
- 清理 unused import（material3.TextButton）

### 💡 决策
- 各主题按钮用各自标准组件：MD3 = OutlinedButton(取消) + Button(归档，胶囊)；Miuix = TextButton(取消) + Button(归档，16dp 圆角)
- 悬浮态无外层胶囊背景（上轮已去），仅两个按钮独立悬浮

### 📋 待办
- 等待 CI

## 第 23 轮：MD3 批量栏取消按钮改实心胶囊

### ✅ 修复
- MD3「取消」按钮从 OutlinedButton（描边）改为 Button（实心胶囊，surfaceContainerHigh 中性色 + onSurface 文字），与「归档」error 实心胶囊视觉统一
- 移除 unused OutlinedButton import

### 📋 待办
- 等待 CI

## 第 24 轮：减少消耗撤销 + 统计页 Miuix 外壳 + 统计页编辑功能

### ✅ 已完成
1. **列表页减号撤销（方案2）**：
   - ConsumptionRecord 加 `id: String?`（兼容旧数据）
   - Repository.changeQuantity 返回 QuantityChangeResult(autoArchived, consumptionId)，减少时写 UUID
   - 新增 Repository.undoConsumption(itemId, consumptionId)：删记录 + 数量+1（含减到0归档恢复）
   - ViewModel.changeQuantity 加 withUndo 参数 + undoRequest StateFlow + undoConsumption
   - MainActivity 观察 undoRequest 弹撤销 Snackbar（MD3/MIUIX 两套样式）
   - 列表页减号传 withUndo = delta<0（详情页吃掉一份走 consumeOne 不触发）
2. **统计页 Miuix 外壳**：新增 MiuixStatsScreen（Miuix Scaffold/TopAppBar/SmallTitle/Card 分组），图表 Canvas 自绘复用桥接配色
3. **统计页编辑功能**：消耗排行榜项点击 → 进对应食品详情（可编辑），MD3 与 Miuix 两版一致

### 💡 决策
- 撤销定位用 ConsumptionRecord.id（UUID），比「删最后一条」更精确，避免并发/多步操作误删
- undoConsumption 处理减到0自动归档的边界（从归档恢复数量1）
- 排行榜点击进详情按 name 匹配 items 第一个 id（同名多批次取第一个）

### 📋 待办
- 等待 CI

## 第 25 轮：消耗记录管理页（可直接删除误触记录修正统计）

### ✅ 已完成
- Repository：新增 deleteConsumption(id)（仅删记录不回滚库存）+ addConsumption(record)（撤销删除用）
- ViewModel：deleteConsumption + undoDeleteConsumption + deletedConsumption StateFlow
- 新增 ConsumptionLogScreen（MD3）+ MiuixConsumptionLogScreen（Miuix）：
  - 列出全部消耗记录（按日期倒序），每条显示 emoji/名称/日期/×数量单位，右侧删除按钮
  - 删除弹撤销 Snackbar（MD3/MIUIX 两套样式），撤销恢复记录
  - 顶部提示「删除记录仅修正统计，不会回滚库存数量」
- 统计页（MD3 + Miuix）TopAppBar 加 History 图标入口 → consumption 路由
- MainActivity 加 consumption 路由（二级页，自动隐藏底栏/FAB）

### 💡 决策
- 删除消耗记录只删统计、不回滚库存：符合「修正统计」语义，库存数量可自行在列表/详情调整（回滚库存易出错且需匹配同名食品，收益低风险高）
- 入口放统计页 TopAppBar action（History 图标），比塞进设置页更贴合使用场景

### 📋 待办
- 等待 CI

## 第 26 轮：修复消耗记录页闪退（LazyColumn key 冲突）

### ⚠️ 问题（用户反馈）
点统计页时钟图标进入消耗记录页闪退（两主题模式都崩，有消耗数据时）。

### 🔍 根因
LazyColumn 的 key 冲突：`key = { it.id ?: "${name}-${epochDay}-${amount}" }`。
ConsumptionRecord 新增 id 字段后，旧数据 id=null；若存在「同天 + 同名 + 同数量」的多条记录（如同一天连续吃两份），兜底 key 完全相同 → LazyColumn 抛 `IllegalArgumentException: Key was already used` → 闪退。

### ✅ 修复
- 两个消耗记录页（MD3/Miuix）改用 `itemsIndexed(sorted, key = { index, record -> record.id ?: "idx-$index" })`，index 兜底保证 key 唯一

### 💡 决策
- key 用 index 兜底而非更复杂的组合：简单可靠，列表是静态展示（增删走重组，position key 足够）

### 📋 待办
- 等待 CI；若仍有闪退需用户提供 logcat

## 第 27 轮：修复消耗记录删除无反应（旧数据 id=null）

### ⚠️ 问题（用户反馈）
消耗记录页能打开，但删除按钮无反应。

### 🔍 根因
删除按钮 `record.id?.let { deleteConsumption(it) }`：旧数据 id=null，`?.let` 直接跳过，删除无效。

### ✅ 修复
- Repository 新增 `migrateConsumptionIds()`：启动时给无 id 的旧消耗记录补 UUID
- ViewModel init 里调用该迁移

### 📋 待办
- 等待 CI

## 第 28 轮：修复撤销 Snackbar（MD3 缺失 + Miuix 样式 + 位置瞬移）

### ⚠️ 问题（用户反馈）
1. MD3 模式撤销 Snackbar 没出现
2. Miuix 撤销 Snackbar 太丑
3. 位置奇怪：有底栏在屏幕中间，没底栏在底栏上方，滑动隐藏底栏时瞬移

### 🔍 根因（两个）
1. **位置/样式**：MainApp 的 snackbarHost 手动加 `padding(bottom = 84.dp)`。material3 Scaffold 本来就会自动把 Snackbar 定位在 bottomBar 之上（跟随底栏状态），手动 84dp 破坏自动定位 → 屏幕中间/瞬移/样式错乱
2. **MD3 撤销缺失**：`LaunchedEffect(undoRequest)` 里 `consumeUndoRequest()` 把 key 置 null → key 变化 → 协程被取消 → `showSnackbar` 中断

### ✅ 修复
1. MainApp snackbarHost 去掉 84dp（Scaffold 自动定位）
2. undoRequest / deletedConsumption 改用 `LaunchedEffect(Unit) + filterNotNull().collect` 模式，consume 不再取消协程
3. MiuixConsumptionLogScreen snackbarHost 去 84dp
4. 清理 unused import/变量

### 💡 决策
- Snackbar 一律依赖 Scaffold 自动定位，不手动加偏移（Miuix 文档/源码均确认 Scaffold 自动放在 bottomBar 之上）
- 一次性事件 StateFlow 的消费用 collect 模式，避免 LaunchedEffect key 竞态

### 📋 待办
- 等待 CI

## 第 29 轮：修复撤销 Snackbar 瞬移 + 位置（自定义平滑定位）

### ⚠️ 问题（用户反馈）
食品列表的撤销通知仍瞬移：有底栏在屏幕中间，没底栏在底部，不自然。

### 🔍 根因
1. 瞬移：bottomBar 的 AnimatedVisibility 在滑动隐藏后高度突变 0，Scaffold 把 Snackbar 从「底栏上方」瞬间跳到底部
2. 屏幕中间：悬浮胶囊 bottomBar 高度（navigationBarsPadding+padding+胶囊≈104dp）过高，Snackbar 被顶到该高度之上

### ✅ 修复
- Snackbar 移出 Scaffold，改为外层 Box 覆盖层 + `animateDpAsState` 平滑定位：
  - 底栏可见（showChrome=true）→ snackbarOffset=84dp（悬浮胶囊上方 8dp）
  - 底栏隐藏 → 8dp（贴导航条上方）
  - 250ms tween 平滑过渡，不再瞬移
- 清理 unused import

### 💡 决策
- 84dp = 悬浮胶囊(64) + 胶囊底 padding(12) + 间距(8)，悬浮态精准；全宽底栏略偏高但可接受
- Snackbar 用 navigationBarsPadding + 固定 offset，不依赖 bottomBar 测量高度，避免瞬移

### 📋 待办
- 等待 CI；全宽底栏时 Snackbar 位置可能需要微调

## 第 30 轮：Miuix 设计审查 + 第一部分改进（字体层级/主按钮/摘要色）

### 审查
- 新增 `docs/audits/2026-08-20-miuix-review.md`：按 skill ui-review-workflow 全面审查，结论=结构对、视觉语义（颜色/字体/图标）仍停留 MD3 用法

### 第一部分改进（只改 Miuix，不动 MD3 版页面）
1. **字体层级**：Miuix Text 的显式 fontSize → MiuixTheme.textStyles
   - MiuixHomeScreen：9 处（title4/body2/footnote2/title2）
   - MiuixSettingsScreen：5 处
   - MiuixManageScreens：7 处（emoji 保留 fontSize）
2. **主操作按钮 primary**：
   - MiuixHomeScreen「一键清理」→ ButtonDefaults.buttonColorsPrimary()
   - MiuixFoodDetailScreen「吃掉一份」→ buttonColorsPrimary()
3. **摘要色语义**：MaterialTheme.colorScheme.onSurfaceVariant → MiuixTheme.colorScheme.onSurfaceVariantSummary（设置页说明、管理页"使用中"等摘要文字）

### 第二部分（待做，另开）
- MD3 Text/Surface/LinearProgressIndicator/FilterChip 残留替换（FoodDetail/FoodList/Archive/Stats/Consumption 5 文件）
- 图标换 MiuixIcons
- squircle 描边、Snackbar 分档细节

### 📋 待办
- 等待 CI

## 第 31 轮：修复 Miuix 弹窗不显示（OverlayDialog 无条件调用）

### ⚠️ 问题（用户反馈）
坚果云账号修改界面缺失、从云端恢复无作用、没有弹窗选择。

### 🔍 根因
所有 Miuix 页面的 OverlayDialog 用 `if (show) { OverlayDialog(show=show) }` / `?.let { OverlayDialog(show=true) }` 条件渲染。
Miuix 官方推荐用法是**无条件调用 + show 参数**（靠 Scaffold popup host + LaunchedEffect(show) 驱动动画）；
条件渲染破坏了 popup 的注册/动画机制，导致弹窗不显示。

### ✅ 修复（4 个 Miuix 页面共 9 个弹窗改无条件调用）
1. MiuixSettingsScreen：清空库存/坚果云账号/云端备份选择/恢复二次确认 4 个弹窗；账号输入状态提升到顶层 + LaunchedEffect 同步
2. MiuixArchiveScreen：清空归档弹窗
3. MiuixFoodDetailScreen：移入归档弹窗
4. MiuixManageScreens：分类编辑（加 show 参数 + remember(show,initial) 重建输入状态）、删除分类、添加位置 3 个弹窗

### 💡 决策
- MiuixCategoryEditDialog 加 show 参数，TextFieldState 用 remember(show, initialLabel) 在打开/切换分类时重建（打开清空、编辑同步当前值）
- 弹窗一律遵循 usage-patterns.md 的 OverlayDialog 官方模式：无条件调用 + show 驱动 + onDismissRequest 复位

### 📋 待办
- 等待 CI；继续第二部分（MD3 组件残留替换）

## 第 32 轮：第二部分 —— MD3 Text → Miuix Text（字体层级全量对齐）

### ✅ 已完成（5 个 Miuix 文件的 MD3 Text 全部换 Miuix Text）
- MiuixConsumptionLogScreen：5 处（fontSize → textStyles footnote2/body1/body2/title3）
- MiuixFoodDetailScreen：9 处 MaterialTheme.typography → MiuixTheme.textStyles（headlineSmall→title2、titleMedium→body1 等）
- MiuixStatsScreen：12 处 typography → textStyles
- MiuixFoodListScreen：5 处 typography → textStyles + 3 处 FilterChip label 显式 body2
- MiuixArchiveScreen：2 处 typography → textStyles + 1 处 FilterChip label 显式 body2
- 同步把 onSurfaceVariant 摘要色 → onSurfaceVariantSummary（ConsumptionLog 等）

### 💡 决策
- MD3 typography → Miuix textStyles 映射：bodyMedium→body2、titleSmall→subtitle、labelSmall/labelMedium/bodySmall→footnote2、headlineSmall→title2、titleMedium/bodyLarge→body1、labelLarge→body2
- FilterChip（MD3 妥协组件）label 的 Text 显式 style=body2，避免换 Miuix Text 后默认 main(17sp) 变大
- 颜色 token（MaterialTheme.colorScheme）暂保留桥接，作为第三部分「颜色语义彻底切换」
- MD3 Surface/LinearProgressIndicator 暂保留，后续再换

### 📋 待办
- 等待 CI；后续：MD3 Surface/ProgressIndicator 替换、颜色 token 彻底切换、图标 MiuixIcons

## 第 33 轮：修复 Miuix 弹窗不显示的根本原因（弹窗在 Scaffold 外）

### 🔍 真根因
OverlayDialog 默认 renderInRootScaffold=true，通过 LocalRootDialogStates/LocalDialogStates 找 **Miuix Scaffold 的 popup host**。
但所有 9 个弹窗都放在 Miuix Scaffold 的 content lambda **之外**，拿不到 LocalDialogStates → 弹窗注册失败、永不显示。

### ✅ 修复（4 文件 9 弹窗全部移入 Scaffold content 内）
- MiuixSettingsScreen：4 弹窗（清空库存/坚果云账号/备份选择/恢复确认）移入 Scaffold content
- MiuixArchiveScreen：1 弹窗（清空归档）
- MiuixFoodDetailScreen：1 弹窗（移入归档）
- MiuixManageScreens：3 弹窗（分类编辑×2、删除分类、添加位置）移入各 MiuixManageScaffold content

### 💡 决策
- 弹窗必须与 LazyColumn 平级放在 Scaffold content lambda 内（bodyContent 会被 Box 包裹，弹窗经 DialogLayout 注册到 LocalDialogStates，由 MiuixPopupHost 渲染）
- 之前「无条件调用」只是必要条件，位置（必须在 Scaffold 内）才是充分条件

### 📋 待办
- 等待 CI

## 第 34 轮：坚果云弹窗排版优化 + 第三部分（进度条/分隔线 Miuix 化）

### 坚果云账号弹窗排版
- Column 改 verticalArrangement.spacedBy(12.dp)，按钮区与密码框间加额外 Spacer(4.dp)，解决按钮紧贴密码框逼仄问题

### 第三部分 —— MD3 组件残留替换（进度条 + 分隔线）
- MiuixFoodDetailScreen：LinearProgressIndicator（MD3 lambda → Miuix Float + ProgressIndicatorDefaults 自定义前景/背景色）、HorizontalDivider → Miuix 版
- Common.kt FoodCard MIUIX 分支：LinearProgressIndicator → MiuixLinearProgressIndicator（MD3 分支保留）

### 💡 决策
- Miuix LinearProgressIndicator 用 progress: Float + ProgressIndicatorDefaults.progressIndicatorColors(foreground/background)，非 MD3 的 lambda + color/trackColor
- Miuix 进度条自带圆角（cornerRadius=height/2），去掉手动 clip
- Common.kt 双实现需 alias（MiuixLinearProgressIndicator vs MD3 LinearProgressIndicator）

### 📋 待办
- 等待 CI；继续第三部分：Surface → Miuix Surface/Card（详情页 3 处 + 列表/归档），图标 MiuixIcons

## 第 35 轮：第三部分 —— Surface → Miuix Card/Surface + 引入 miuix-icons

### ✅ 已完成
1. **引入 miuix-icons 依赖**（0.9.4-rc01，仅 MIUIX 主题用，MD3 继续 material-icons-extended）
2. **Surface → Miuix Card/Surface**：
   - MiuixFoodDetailScreen：状态 hero 卡（Miuix Surface，RoundedCornerShape(28)+ui.container）、详情卡/库存卡 → Miuix Card
   - MiuixFoodListScreen：归档搜索结果行 → Miuix Card；FilterToggle 的 Surface(onClick) 换 Miuix Surface（签名兼容，仅换 import）
   - MiuixArchiveScreen：归档行 → Miuix Card

### 📋 待办
- 图标替换（MiuixIcons.Regular.*，仅 Miuix 页面 + Common.kt 分流），映射表已梳理
- 等待 CI

## 第 36 轮：第三部分 —— 图标 MiuixIcons 化（仅 MIUIX，MD3 保持 material）

### ✅ 已完成
1. **8 个 Miuix 页面**的 material 图标 → MiuixIcons.Regular.*（脚本批量 + 手动 import）：
   - 映射：Add→Add、Remove→Remove、Check→Ok、Close→Close、Delete/DeleteForever→Delete、Edit→Edit、History→Recent、Place→Location、Schedule→Timer、RestartAlt→Refresh、SelectAll→SelectAll、CheckCircle→Ok、ErrorOutline→Report、Cloud→CloudFill、CloudUpload→UploadCloud、CloudDownload→Download、FileUpload→UploadCloud、FileDownload→FileDownloads、FilterList→Filter、ExpandMore→ExpandMore、ArrowBack→Back、ArrowForward→Forward
   - 保留 material：CleaningServices、Inventory2（Miuix 无对应图标）
2. **Common.kt 共用组件分流**（按 LocalThemeStyle）：
   - rememberStatusUi：CheckCircle/Schedule/ErrorOutline → MiuixIcons.Ok/Timer/Report（MIUIX 分支）
   - LocationTag MIUIX 分支：Place → MiuixIcons.Location
   - QuantityStepper MIUIX 分支：Remove/Add → MiuixIcons
   - SelectIndicator：Check → MiuixIcons.Ok（MIUIX 分支）
   - MD3 分支全部保留 material 图标

### 💡 决策
- 图标只替换 MIUIX 侧，MD3 侧保持 material（符合用户要求）
- 近似映射：DeleteForever→Delete、History→Recent、Schedule→Timer、CheckCircle→Ok、ErrorOutline→Report、Cloud→CloudFill（Miuix 无精确对应，取语义最接近）
- CleaningServices/Inventory2 无 Miuix 对应，保留 material（用户接受"不完全替换"）

### 📋 待办
- 导航 tab 图标（Home/ListAlt/PieChart/Settings）分流：MainTabs 是顶层 val，需重构为 composable；PieChart 无 Miuix 对应，需用户决策
- 等待 CI

## 第 37 轮：减号图标回退 + 导航图标候选预览页

### 减号图标问题
- Miuix 图标库**无「减号」图标**：MiuixIcons.Remove 是「移除/退出」形状（带箭头），非减号（−）
- 修复：QuantityStepper 的减号回退用 material `Icons.Rounded.Remove`（真减号），加号保留 MiuixIcons.Add

### 导航图标候选预览页
- 新增 docs/audits/miuix-icons-preview.html：从 Miuix 源码提取 Regular 权重 pathData 转 SVG 渲染 9 个候选图标
- 候选：Home/Settings（精确对应）、ListView（替代 ListAlt）、GridView/Tasks/Recent/Store/Layers/Scan（统计 PieChart 替代候选）

### 📋 待办
- 等待用户选定导航图标映射后，做 MainTabs 分流（Home/ListAlt/PieChart/Settings → MiuixIcons，MD3 保持 material）

## 第 38 轮：底部导航图标分流（MIUIX → MiuixIcons，MD3 保持 material）

### ✅ 已完成
- 新增 MiuixMainTabs（MiuixIcons.Regular）：Home→Home、ListAlt→ListView、PieChart→GridView（用户选定）、Settings→Settings
- MiuixBottomNav / MiuixFloatingNav 改用 MiuixMainTabs；MainTabs 保留给 MD3（FloatingPillNav/Md3BottomNav 不变）

### 💡 决策
- MainTabs 保留 material 图标，Miuix 导航另建 MiuixMainTabs，双列表分流（图标是顶层 val，无法在 composable 内按主题切，故用两份列表）
- 统计 PieChart 无 Miuix 对应，按用户选定用 GridView（四宫格）

### 📋 待办
- 等待 CI；图标替换至此完成（仅剩 CleaningServices/Inventory2 保留 material）

## 第 39 轮：第三部分收尾 —— 颜色 token 彻底切换（MaterialTheme → MiuixTheme）

### ✅ 已完成（P0 核心项）
1. **颜色 token**：8 个 Miuix 页面 40 处 `MaterialTheme.colorScheme.X` → `MiuixTheme.colorScheme.Y`（Miuix 语义 token）
   - 同名直映：primary/onPrimary/onPrimaryContainer/primaryContainer/secondaryContainer/tertiaryContainer/error/errorContainer/onErrorContainer/onSecondaryContainer/surface/surfaceContainer/surfaceContainerHigh/surfaceContainerHighest
   - 特殊映射：onSurfaceVariant → onSurfaceVariantSummary（摘要语义）、outlineVariant → dividerLine
2. **图表调色板**：rememberChartColorsMiuix 去掉 MD3 特有的 tertiary/inversePrimary，改用 Miuix 容器色/前景色（tertiaryContainer/onPrimaryContainer/onSecondaryContainer/onTertiaryContainer）
3. **shape token**：ManageScreens 3 处 + SettingsScreen 2 处 `MaterialTheme.shapes.large/medium` → 等价 `RoundedCornerShape(24/16.dp)`
4. 清理 6 个文件的 unused MaterialTheme import

### 保留的 material3（已声明妥协）
- FilterChip/FilterChipDefaults（Miuix 无 chip）
- material3 Icon（CleaningServices/Inventory2 图标）
- OutlinedTextField（坚果云密码遮蔽）

### 💡 决策
- 桥接层 MiuixRootTheme 仍保留（供未迁移的 MD3 页面 + Common.kt MD3 分支用），但 Miuix 页面自身不再依赖桥接取色，直接读 MiuixTheme.colorScheme
- 至此审查报告 P0/P1 全部完成，仅剩 Common.kt 内部 MD3 分支颜色（属 MD3 主题，不动）

### 📋 待办
- 等待 CI；第三部分完成

## 第 40 轮：详情页按钮圆角回归 Miuix 标准

### ⚠️ 问题（用户反馈）
「吃掉一份」「编辑食品信息」按钮是胶囊圆角，问是否故意。

### 🔍 根因
遗漏，非故意。从 MD3 版迁移时照搬了 MD3 的胶囊圆角（cornerRadius=50.dp），Miuix 标准按钮圆角是 16dp（ButtonDefaults.CornerRadius）。

### ✅ 修复
- 移除 3 处 cornerRadius=50.dp（空态返回按钮、吃掉一份、编辑食品信息），恢复 Miuix 默认 16dp 圆角
- 首页「一键清理」此前已用默认，无此问题

### 📋 待办
- 等待 CI
