# 开发日志 — 2026-08-22

## 第 1 轮：修复跨零点 LocalToday 残留点 (B-01) 与封面图片压缩 (B-04)

### ✅ 已完成

1. **B-01：修复跨零点 LocalToday 残留点**
   - `HomeScreen.kt` / `MiuixHomeScreen.kt`：顶部栏副标题日期由 `LocalDate.now().cnDay()` 改为读 `today.cnDay()`（`today = LocalToday.current`）。
   - `StatsScreen.kt` / `MiuixStatsScreen.kt`：引入 `LocalToday`，`today`、`weekAgo`、`monthStart` 改为基于 `LocalToday.current` 计算，`dailyTrend` 增加 `today` 作为 remember key，保证午夜后统计图表与周/月指标自动刷新。
2. **B-04：封面图片落盘前下采样与质量压缩**
   - `ImageStore.kt` 的 `copyImageToCovers` 升级：
     - 读取 EXIF 旋转信息（`ExifInterface.TAG_ORIENTATION`），避免相机拍照方向错乱。
     - 解码图片边界计算 2 的幂次采样率（`inSampleSize`），限制长边不超过 1200px。
     - 矩阵校正旋转并等比缩放后，以 85% 质量保存为 JPEG。
     - 保留原始流直接拷贝作为解码失败时的安全降级兜底。

### 💡 决策

- 封面图最大尺寸定为 1200px：即便在 1440p / xxxhdpi 设备上全屏/卡片显示也能保持高清晰度，单张图片体积从 10MB+ 下降至 100~300KB，避免应用沙盒目录无限膨胀与列表滑动卡顿。
- `StatsScreen` 的 `dailyTrend` 增加 `today` 依赖，解决跨零点后 7 天消耗柱状图日期列不滑动的问题。

## 第 2 轮：引入 Version Catalog (B-02) 与平滑升级过期依赖 (B-03)

### ✅ 已完成

1. **B-02：引入 `gradle/libs.versions.toml`（Version Catalog）**
   - 新建 `gradle/libs.versions.toml`，将构建插件（AGP, Kotlin Compose, Kotlin Serialization, Foojay）、AndroidX/Compose 库、Miuix 组件库、第三方库（Coil, OkHttp, MaterialKolor, Kotlinx Serialization）及测试库统一版本化编排。
   - `build.gradle.kts`（根目录）改用 `alias(libs.plugins.*)`。
   - `app/build.gradle.kts` 插件与依赖全面迁移为 `libs.*` 类型安全访问器引用。
2. **B-03：平滑升级过期依赖**
   - `compose-bom`: `2026.01.01` $\rightarrow$ `2026.08.00`（跨 7 个月重大基线升级）
   - `androidx.core:core-ktx`: `1.15.0` $\rightarrow$ `1.19.0`
   - `androidx.core:core-splashscreen`: `1.0.1` $\rightarrow$ `1.2.0`
   - `androidx.activity:activity-compose`: `1.12.2` $\rightarrow$ `1.13.0`
   - `androidx.lifecycle:lifecycle-viewmodel-compose`: `2.10.0` $\rightarrow$ `2.11.0`
   - `androidx.lifecycle:lifecycle-runtime-compose`: `2.10.0` $\rightarrow$ `2.11.0`
   - Gradle wrapper: `9.6.1` $\rightarrow$ `9.7.1`
3. **文档同步**
   - 同步更新 `docs/ARCHITECTURE.md` 与 `CLAUDE.md`，记录 Version Catalog 落地与新版本基线。

### 💡 决策

- Miuix 库（`miuix-nav-android`, `miuix-ui`, `miuix-preference`, `miuix-icons`）统一使用 `miuix` 单一版本变量控制，防止未来单独升级某一个坐标导致版本不一致。
- 依赖升级与 Version Catalog 组合落地，构建更稳健。
