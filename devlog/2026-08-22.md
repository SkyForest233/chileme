# 开发日志 — 2026-08-22

## 第 1 轮：修复跨零点 LocalToday 残留点 (B-01) 与封面图片压缩 (B-04)

### ✅ 已完成

1. **B-01：修复跨零点 LocalToday 残留点**
   - `HomeScreen.kt` / `MiuixHomeScreen.kt`：顶部栏副标题日期由 `LocalDate.now().cnDay()` 改为读 `today.cnDay()`（`today = LocalToday.current`）。
   - `StatsScreen.kt` / `MiuixStatsScreen.kt`：引入 `LocalToday`，`today`、`weekAgo`、`monthStart` 改为基于 `LocalToday.current` 计算，`dailyTrend` 增加 `today` 作为 remember key，保证午夜后统计图表与周/月指标自动刷新。
2. **B-04：封面图片落盘前下采样与质量压缩**
   - `ImageStore.kt` 的 `copyImageToCovers` 升级：
     - 读取 EXIF 旋转信息（`ExifInterface.TAG_ORIENTATION`），避免相机拍照方向错乱。
     - 解码图片边界计算 2 的幂次采样率（`inSampleSize`），限制长边不超过 1200px。
     - 矩阵校正旋转并等比缩放后，以 85% 质量保存为 JPEG。
     - 保留原始流直接拷贝作为解码失败时的安全降级兜底。

### 💡 决策

- 封面图最大尺寸定为 1200px：即便在 1440p / xxxhdpi 设备上全屏/卡片显示也能保持高清晰度，单张图片体积从 10MB+ 下降至 100~300KB，避免应用沙盒目录无限膨胀与列表滑动卡顿。
- `StatsScreen` 的 `dailyTrend` 增加 `today` 依赖，解决跨零点后 7 天消耗柱状图日期列不滑动的问题。

## 第 2 轮：引入 Version Catalog (B-02) 与平滑升级过期依赖 (B-03)

### ✅ 已完成

1. **B-02：引入 `gradle/libs.versions.toml`（Version Catalog）**
   - 新建 `gradle/libs.versions.toml`，将构建插件（AGP, Kotlin Compose, Kotlin Serialization, Foojay）、AndroidX/Compose 库、Miuix 组件库、第三方库（Coil, OkHttp, MaterialKolor, Kotlinx Serialization）及测试库统一版本化编排。
   - `build.gradle.kts`（根目录）改用 `alias(libs.plugins.*)`。
   - `app/build.gradle.kts` 插件与依赖全面迁移为 `libs.*` 类型安全访问器引用。
2. **B-03：平滑升级过期依赖**
   - `compose-bom`: `2026.01.01` $\rightarrow$ `2026.08.00`（跨 7 个月重大基线升级）
   - `androidx.core:core-ktx`: `1.15.0` $\rightarrow$ `1.19.0`
   - `androidx.core:core-splashscreen`: `1.0.1` $\rightarrow$ `1.2.0`
   - `androidx.activity:activity-compose`: `1.12.2` $\rightarrow$ `1.13.0`
   - `androidx.lifecycle:lifecycle-viewmodel-compose`: `2.10.0` $\rightarrow$ `2.11.0`
   - `androidx.lifecycle:lifecycle-runtime-compose`: `2.10.0` $\rightarrow$ `2.11.0`
   - Gradle wrapper: `9.6.1` $\rightarrow$ `9.7.1`
3. **文档同步**
   - 同步更新 `docs/ARCHITECTURE.md` 与 `CLAUDE.md`，记录 Version Catalog 落地与新版本基线。

### 💡 决策

- Miuix 库（`miuix-nav-android`, `miuix-ui`, `miuix-preference`, `miuix-icons`）统一使用 `miuix` 单一版本变量控制，防止未来单独升级某一个坐标导致版本不一致。
- 依赖升级与 Version Catalog 组合落地，构建更稳健。

## 第 3 轮：MD3 与 MIUIX 双主题状态层抽离与去重 (B-08)

### ✅ 已完成

1. **状态容器全面落地**：
   - `ConsumptionLogState.kt`：抽离消耗记录页排序与删除状态流。
   - `ArchiveState.kt`：抽离归档历史筛选（原因/关键词）、清空弹窗与智能恢复回调。
   - `ManageState.kt`：抽离阈值管理、分类增删改与存放位置管理共享状态。
   - `FoodDetailState.kt`：抽离详情页食品查找、吃一份连击打卡动画（`bounceScale`/`floatOffset`/`floatAlpha`）、数量步进与归档删除。
   - `HomeScreenState.kt`：抽离首页总数/临期/过期统计、紧急需要处理列表与自动同步通知。
   - `FoodListState.kt`：抽离食品列表多条件组合筛选（状态/分类/位置/搜索）、多选批量模式与全选逻辑。
   - `StatsState.kt`：抽离统计页周/月消耗、近 7 天趋势、分类占比与 TOP5 排行计算。
   - `SettingsState.kt`：抽离设置页深色模式、动态取色、悬浮导航、坚果云 WebDAV 同步及备份导入导出状态。
2. **两套主题 Screen 瘦身为纯模板**：
   - 8 对（共 16 个）Screen 文件全部对接共享 `remember*UiState`。
   - 彻底消除了约 1,200 行重复的状态收集、排序过滤与业务分支代码。
   - 严格保留了两套主题各自的视觉组件与物理动效（MIUIX 弹簧 vs MD3 缓动）。

### 💡 决策

- 采用 State-Hoisting 容器模式，UI 只关心渲染呈现，业务与状态计算 100% 归一，后续功能迭代仅需编写一次业务逻辑。

## 第 4 轮：开源门面补充 (README / LICENSE) 与发版说明确认

### ✅ 已完成

1. **新增 `README.md`**：
   - 编写清晰精炼的项目主页说明，包含核心功能介绍（保质期管理、双主题 MD3/Miuix、打卡动效、坚果云 WebDAV 备份等）、技术栈清单、环境要求与编译命令。
2. **新增 `LICENSE`**：
   - 明确开源协议为 Apache License 2.0。
3. **发版说明确认**：
   - 将“Release Notes 提示签名变更”标记为已确认就绪，记录至待办总览。

## 第 5 轮：状态容器纯逻辑单测补全 (B-07) 与归档搜索恢复支持撤销

### ✅ 已完成

1. **归档搜索与列表恢复全面支持「撤销」**：
   - `ArchiveScreen.kt` / `MiuixArchiveScreen.kt`：在归档历史页（含关键词搜索结果）点击恢复时，触发 6 秒带倒计时圆环的撤销 Snackbar；点击撤销即刻重新将食品移回归档。
   - `FoodListScreen.kt` / `MiuixFoodListScreen.kt`：在主列表搜索结果底部的“归档中找到 X 条”点击恢复时，通过 `AppViewModel.restoredArchivedEvent` 联动触发全屏撤销 Snackbar，点击撤销自动回滚归档。
2. **状态容器纯计算函数解耦与单测补全 (B-07)**：
   - 解耦纯函数：
     - `StatsState.kt`：抽离 `calculateConsumedThisWeek`、`calculateConsumedThisMonth`、`calculateDailyTrend`、`calculateCategoryShare`、`calculateTopConsumed`。
     - `FoodListState.kt`：抽离 `filterFoodItems`（多重状态/分类/位置/搜索与吃完沉底排序）、`filterArchivedMatches`。
     - `ArchiveState.kt`：抽离 `filterArchiveItems`（原因与关键词交集过滤）。
     - `HomeScreenState.kt`：抽离 `calculateUrgentItems`（临期/过期筛选与紧迫度排序）。
   - 新增 16 例纯 JVM 单元测试：
     - `StatsStateTest.kt`（5 例）
     - `FoodListStateTest.kt`（5 例）
     - `ArchiveStateTest.kt`（4 例）
     - `HomeScreenStateTest.kt`（2 例）
   - 全项目单元测试总数扩充至 **42 例**。

## 第 7 轮：食品卡片布局与紧凑日期优化 (MD3 + MIUIX)

### ✅ 已完成

1. **食品卡片右上角双胶囊垂直堆叠布局**：
   - `Common.kt`（MD3 与 MIUIX 两套 `FoodCard` 实现）：
     - **左侧核心文本流**（`Column(Modifier.weight(1f))`）：
       - 第 1 行：食品标题 `item.name` 独占整行宽度。
       - 第 2 行：分类与生产日期 `${category.label} · 生产 ${item.productionDate.dot()}`。
       - 第 3 行：到期日期 `到期 ${item.expiryDate.dot()}`。
     - **右侧元数据标签组**（`Column(horizontalAlignment = Alignment.End)`）：
       - 上方为状态胶囊 `StatusBadge`（`[ ① 已过期 ]` / `[ ⏰ 临期 ]` / `[ ✔ 安全 ]`）。
       - 下方间隔 4dp 放置存放位置胶囊 `LocationTag`（`📍 楼梯`），若未设置存放位置则自然隐去。
2. **紧凑点分日期格式 `LocalDate.dot()` (yyyy.MM.dd)**：
   - `FoodModels.kt`：新增 `LocalDate.dot()` 扩展函数，将日期格式化为规整紧凑的 `yyyy.MM.dd`（如 `2026.07.27`）。
   - 将卡片中的生产日期与到期日期迁移为点分制（如 `乳制品 · 生产 2026.07.27` 与 `到期 2026.08.31`），字数从 17 字精简为 14 字，宽度缩短约 35%，彻底消除 `202...` 日期截断问题。
3. **单测同步扩充**：
   - `FoodModelsTest.kt`：新增 `LocalDate.dot()` 紧凑日期格式化单测，全项目单测总数增至 **43 例**。

## 第 8 轮：首页一键清理 6 秒撤销与步进器/打卡触感振动 (MD3 + MIUIX)

### ✅ 已完成

1. **首页「一键清理全部过期食品」支持 6 秒撤销**：
   - `HomeScreen.kt` / `MiuixHomeScreen.kt`：点击清理全部过期食品后，触发带 6 秒倒计时圆环的撤销条（`showUndoSnackbar`），点击撤销通过 `restoreArchivedBatch` 原子化回滚，过期食品即刻放回库存。
2. **列表卡片步进器与吃掉一份打卡触觉反馈（HapticFeedback）**：
   - `Common.kt`（`QuantityStepper`）：步进器点击 `+` / `-` 时触发细腻的 `HapticFeedbackType.TextHandleMove` 触感震动反馈。
   - `FoodDetailScreen.kt` / `MiuixFoodDetailScreen.kt`：点击“吃掉一份！”大按钮时触发打卡振动，吃完最后一份时触发 `LongPress` 振动，提升打卡爽快感。

## 第 9 轮：全页面过渡动效补全 (MD3 + MIUIX)

### ✅ 已完成

1. **列表项增删位移动效全面覆盖 (`Modifier.animateItem()`)**：
   - `ArchiveScreen.kt` / `MiuixArchiveScreen.kt`：为归档记录项添加 `animateItem()`，恢复/删除时平滑淡出并带动下方卡片优雅滑动上移，消除硬跳。
   - `HomeScreen.kt` / `MiuixHomeScreen.kt`：为首页「需要处理」列表项添加 `animateItem()`，处理食品时平滑重排。
   - `ManageScreens.kt` / `MiuixManageScreens.kt`：为分类阈值管理、分类管理、位置管理 3 个子页面的所有列表项全面添加 `animateItem()`，增删时具备完整进出场动效。
2. **条件视图展开/折叠过渡动效**：
   - `HomeScreen.kt` / `MiuixHomeScreen.kt`：为「一键清理 X 件过期食品」大按钮添加 `AnimatedVisibility(expandVertically + fadeIn / shrinkVertically + fadeOut)`，清理完毕后高度平滑收起。
3. **空态与列表视图平滑切换 (`Crossfade`)**：
   - `FoodListScreen.kt` / `MiuixFoodListScreen.kt` 与 `ArchiveScreen.kt` / `MiuixArchiveScreen.kt`：在「内容列表」与「搜索无结果/空箱子空态」之间引入 `Crossfade`，切换时 200ms 自然渐变过渡。
4. **统计页 TOP 5 排行条形图平滑生长动画**：
   - `StatsScreen.kt` / `MiuixStatsScreen.kt`：为 TOP 5 水平进度条加入 `animateFloatAsState`（600ms `EmphasizedDecelerate`），进场时条形图平滑展开。

## 第 10 轮：批量修改位置、CSV 导出与本地内部滚动冷备 (MD3 + MIUIX)

### ✅ 已完成

1. **多选模式增加「批量修改存放位置」（MD3 + MIUIX）**：
   - `MainActivity.kt`：在底部批量操作栏（`BatchActionBar`）新增「移动位置」操作按钮。
   - 点击弹出批量位置选择弹窗（MD3 `AlertDialog` / MIUIX `OverlayDialog`），支持从现有存放位置 Chip 中一键选取，或直接输入新位置名称。
   - 确认后通过 `viewModel.updateLocationBatch` 原子化更新全部所选食品位置并自动清空多选态。
2. **导出 CSV 表格功能（`CsvExport.kt`）**：
   - 针对家庭资产导出需求，新增 `buildCsvExport` 导出标准 CSV 表格。
   - 包含 UTF-8 BOM 标识符，确保 Windows/Mac/手机端 Microsoft Excel、WPS 直接打开中文不乱码。
   - 在设置页（MD3 + MIUIX）增加「导出 CSV 表格」入口。
3. **本地内部滚动冷备与快照还原（`LocalSnapshotStore.kt`）**：
   - 新增 `LocalSnapshotStore`，在应用私有存储目录 `files/snapshots/` 内部自动滚动维护最近 3 份本地冷备快照。
   - 每日首次启动时自动备份快照，防误删防数据意外丢失。
   - 在设置页（MD3 + MIUIX）提供「本地历史快照」管理与还原弹窗，点击任意快照经二次确认后可整体替换恢复。
4. **单测扩充**：
   - 新增 `CsvExportTest.kt`（3 例）与 `LocalSnapshotTest.kt`（2 例），全项目单测总数提升至 **48 例**。

### 💡 决策

- 本地滚动冷备为未配置坚果云 WebDAV 的用户提供了最坚实的防误删底层安全网；CSV 导出与批量改位置大幅提升了多食品管理效率。








