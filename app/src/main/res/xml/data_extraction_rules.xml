<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ 的备份/迁移规则（android:dataExtractionRules）。API 26–30 那份在 backup_rules.xml，
  两份的排除清单**必须保持一致**（唯一被允许的例外见下面 device-transfer 的 covers/ 一条），
  守卫见 `BackupRulesTest`。

  2026-09-19（M1-1）：从"排除整个 datastore/ 目录"改成"只排除凭据那一个文件"。
  动机、官方依据与旧做法的代价都写在 `backup_rules.xml` 的注释里 与 `docs/ARCHITECTURE.md` §5
  「备份排除规则」；凭据 key 由启动期的 `migrateLegacyCredentials()` 从业务文件搬进
  `credentials_store`，所以老用户不会在这次改动后丢账号。

  两条通道的差别只有一处，是刻意留的：

  - cloud-backup（上传到用户 Google 账号）排除 `covers/`：封面是体积不可控的二进制，
    而库存 JSON 很小；恢复后 `photoPath` 悬空时渲染侧回落 emoji/coverText，不会坏图。
  - device-transfer（换机直传，局域网、不占云空间）**放行** `covers/`：
    同一时刻 `pantry_store` 也过去了 ⇒ 封面与引用它的库存数据一起到位，不会像以前那样
    "图到了、JSON 没到 ⇒ `cleanupOrphanCovers()` 把它们当孤儿全删掉"（这正是 2026-09-16 记在
    ARCHITECTURE.md 里那条"已知不一致"，本次连带修掉）。

  两条通道都排除 `snapshots/`：每日全量快照与库存 JSON 内容重复，系统备份已经覆盖主数据了，
  快照的定位是本机滚动冷备（`LocalSnapshotStore`）。README 里此前那句"每日快照会随系统备份进入
  你的 Google 账号"随之作废 —— 现在进备份的是主数据本身，那句话已按新行为改写。
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" path="datastore/credentials_store.preferences_pb" />
        <exclude domain="file" path="covers/" />
        <exclude domain="file" path="corrupt/" />
        <exclude domain="file" path="snapshots/" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="datastore/credentials_store.preferences_pb" />
        <exclude domain="file" path="corrupt/" />
        <exclude domain="file" path="snapshots/" />
    </device-transfer>
</data-extraction-rules>
